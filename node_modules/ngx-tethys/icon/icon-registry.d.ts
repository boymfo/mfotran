import { Observable } from 'rxjs';
import { HttpClient } from '@angular/common/http';
import { DomSanitizer, SafeHtml, SafeResourceUrl } from '@angular/platform-browser';
import * as i0 from "@angular/core";
export type IconMode = 'font' | 'svg';
export type SvgResourceUrl = SafeResourceUrl | string;
export type SvgHtml = SafeHtml | string;
/**
 * @order 20
 */
export declare class ThyIconRegistry {
    private sanitizer;
    private httpClient;
    private document;
    private defaultFontSetClass;
    private internalIconMode;
    private svgIconConfigs;
    private svgIconSetConfigs;
    private inProgressUrlFetches;
    get iconMode(): IconMode;
    constructor(sanitizer: DomSanitizer, httpClient: HttpClient, document: any);
    private getIconNameNotFoundError;
    private getIconFailedToSanitizeLiteralError;
    private internalAddSvgIconSet;
    private cloneSvg;
    private fetchUrl;
    private toSvgElement;
    private extractSvgIconFromIconSet;
    private extractIconWithNameFromIconSetConfigs;
    private svgElementFromString;
    private setSvgAttributes;
    private createSvgElementForSingleIcon;
    private loadSvgIconFromConfig;
    private loadSvgIconSetFromConfig;
    private getSvgFromConfig;
    private getSvgFromIconSetConfigs;
    private internalAddSvgIconConfig;
    buildIconKey(namespace: string, name: string): string;
    splitIconName(iconName: string): [string, string];
    addSvgIconSetInNamespace(namespace: string, url: SvgResourceUrl): this;
    /**
     * 添加SVG图标集，添加到默认命名空间
     */
    addSvgIconSet(url: SvgResourceUrl): this;
    addSvgIconSetLiteralInNamespace(namespace: string, literal: SafeHtml): this;
    addSvgIconSetLiteral(literal: SafeHtml): this;
    /**
     * @description.en-us Registers an icon by URL in the specified namespace.
     * @description 添加单个SVG图标到指定的命名空间
     * @param namespace Namespace in which the icon should be registered.
     * @param iconName Name under which the icon should be registered.
     * @param url
     */
    addSvgIconInNamespace(namespace: string, iconName: string, url: SvgResourceUrl): this;
    /**
     * @description.en-us Registers an icon by URL in the default namespace.
     * @description 添加单个SVG图标
     * @param iconName Name under which the icon should be registered.
     * @param url
     */
    addSvgIcon(iconName: string, url: SvgResourceUrl): this;
    /**
     * @description.en-us Registers an icon using an HTML string in the default namespace.
     * @description 添加单个SVG图标字符串，直接传入 SVG HTML 字符串
     * @param iconName Name under which the icon should be registered.
     * @param literal SVG source of the icon.
     */
    addSvgIconLiteral(iconName: string, literal: SvgHtml): this;
    /**
     * @description.en-us Registers an icon using an HTML string in the specified namespace.
     * @description 添加单个SVG图标字符串到指定的命名空间，直接传入 SVG HTML 字符串
     * @param namespace Namespace in which the icon should be registered.
     * @param iconName Name under which the icon should be registered.
     * @param literal SVG source of the icon.
     */
    addSvgIconLiteralInNamespace(namespace: string, iconName: string, literal: SvgHtml): this;
    getDefaultFontSetClass(): string;
    getFontSetClassByAlias(fontSet: string): string;
    /**
     * 获取某个图标
     */
    getSvgIcon(name: string, namespace?: string): Observable<SVGElement>;
    setIconMode(mode: IconMode): void;
    static ɵfac: i0.ɵɵFactoryDeclaration<ThyIconRegistry, never>;
    static ɵprov: i0.ɵɵInjectableDeclaration<ThyIconRegistry>;
}
