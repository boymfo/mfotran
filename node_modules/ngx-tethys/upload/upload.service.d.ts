import { Observable } from 'rxjs';
import { XhrFactory } from '@angular/common';
import * as i0 from "@angular/core";
export declare enum ThyUploadStatus {
    pending = "pending",
    started = "started",
    uploading = "uploading",
    done = "done"
}
export interface ThyUploadResponse {
    status: ThyUploadStatus;
    uploadFile?: ThyUploadFile;
}
/**
 * 文件上传进度
 * @public
 * @order 40
 */
export interface ThyUploadFileProgress {
    /**
     * 上传状态
     * @default pending
     */
    status: ThyUploadStatus;
    /**
     * 进度百分比
     */
    percentage: number;
    /**
     * 上传速度
     */
    speed?: number;
    /**
     * 人类可读的上传速度，比如: 120 kb/s
     */
    speedHuman?: string;
    /**
     * 开始上传时间
     */
    startTime: number | null;
    /**
     * 结束上传时间
     */
    endTime?: number | null;
    /**
     * 上传时间
     */
    estimatedTime?: number | null;
    /**
     * 人类可读的上传时间，比如: 00:12:23
     */
    estimatedTimeHuman?: string | null;
}
/**
 * 文件上传对象
 * @public
 * @order 30
 */
export interface ThyUploadFile {
    /**
     * 文件唯一标识
     */
    identifier?: string;
    /**
     * 上传方法
     */
    method: string;
    /**
     * 远程上传地址
     */
    url: string;
    /**
     * 是否携带认证信息
     */
    withCredentials?: boolean;
    /**
     * 原始文件
     */
    nativeFile: File;
    /**
     * 文件字段
     */
    fileField?: string;
    /**
     * 文件名
     */
    fileName?: string;
    /**
     * 请求头
     */
    headers?: Record<string, string>;
    /**
     * 请求数据
     */
    data?: Record<string, string>;
    /**
     * 响应状态
     */
    responseStatus?: any;
    /**
     * 上传响应结果
     */
    response?: any;
    /**
     * 响应头
     */
    responseHeaders?: any;
    /**
     * 上传进度
     */
    progress?: ThyUploadFileProgress;
}
export interface ThyUploadFilesOptions {
    onStarted?: (item: ThyUploadFile) => void;
    onDone?: (item: ThyUploadFile) => void;
}
/**
 * 文件远程上传服务
 * @order 20
 */
export declare class ThyUploadService {
    private xhrFactory;
    constructor(xhrFactory: XhrFactory);
    private secondsToHuman;
    private normalizeUploadFiles;
    private uploadByXhr;
    private ensureFileName;
    /**
     * 上传单个文件
     * @param uploadFile 上传文件对象
     */
    upload(uploadFile: ThyUploadFile): Observable<ThyUploadResponse>;
    /**
     * 并发上传多个文件
     * @param uploadFiles 上传文件列表
     * @param concurrent 并发上传数, 默认 5
     * @param options onStared, onDone 回调
     */
    uploadBulk(uploadFiles: ThyUploadFile[], concurrent?: number, options?: ThyUploadFilesOptions): Observable<ThyUploadResponse>;
    static ɵfac: i0.ɵɵFactoryDeclaration<ThyUploadService, never>;
    static ɵprov: i0.ɵɵInjectableDeclaration<ThyUploadService>;
}
