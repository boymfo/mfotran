import { ComponentTypeOrTemplateRef, ThyAbstractOverlayRef, ThyAbstractOverlayService } from 'ngx-tethys/core';
import { FunctionProp } from 'ngx-tethys/util';
import { Overlay, OverlayConfig, OverlayContainer, OverlayRef, ScrollStrategy, ViewportRuler } from '@angular/cdk/overlay';
import { Platform } from '@angular/cdk/platform';
import { Injector, NgZone, OnDestroy } from '@angular/core';
import { ThyPopoverContainer } from './popover-container.component';
import { ThyPopoverRef } from './popover-ref';
import { ThyPopoverConfig } from './popover.config';
import { SafeAny } from 'ngx-tethys/types';
import * as i0 from "@angular/core";
/**
 * @public
 * @order 10
 */
export declare class ThyPopover extends ThyAbstractOverlayService<ThyPopoverConfig, ThyPopoverContainer> implements OnDestroy {
    private ngZone;
    private _viewportRuler;
    private _document;
    private _platform;
    private _overlayContainer;
    private readonly ngUnsubscribe$;
    private originInstancesMap;
    private buildPositionStrategy;
    private buildScrollStrategy;
    protected buildOverlayConfig<TData>(config: ThyPopoverConfig<TData>): OverlayConfig;
    protected attachOverlayContainer(overlay: OverlayRef, config: ThyPopoverConfig<any>): ThyPopoverContainer;
    protected createAbstractOverlayRef<T, TResult = unknown>(overlayRef: OverlayRef, containerInstance: ThyPopoverContainer, config: ThyPopoverConfig): ThyAbstractOverlayRef<T, ThyPopoverContainer, TResult>;
    protected createInjector<T>(config: ThyPopoverConfig, popoverRef: ThyPopoverRef<T>, popoverContainer: ThyPopoverContainer): Injector;
    private originElementAddActiveClass;
    private originElementRemoveActiveClass;
    constructor(overlay: Overlay, injector: Injector, defaultConfig: ThyPopoverConfig, scrollStrategy: FunctionProp<ScrollStrategy>, ngZone: NgZone, _viewportRuler: ViewportRuler, _document: any, _platform: Platform, _overlayContainer: OverlayContainer);
    private ensureCloseClosest;
    /**
     * 打开悬浮层
     */
    open<T, TData = unknown, TResult = unknown>(componentOrTemplateRef: ComponentTypeOrTemplateRef<T>, config?: ThyPopoverConfig<TData>): ThyPopoverRef<T, TResult>;
    /**
     * 根据Id获取打开的悬浮层
     */
    getPopoverById(id: string): ThyPopoverRef<any> | undefined;
    /**
     * 获取已经打开的悬浮层
     */
    getOpenedPopovers(): ThyPopoverRef<SafeAny>[];
    /**
     * @description.en-us Finds the closest ThyPopoverRef to an element by looking at the DOM.
     * @description 通过 Dom 元素查找最近弹出的悬浮层
     */
    getClosestPopover(element: HTMLElement): ThyPopoverRef<any> | undefined;
    /**
     * 关闭悬浮层
     */
    close<T>(result?: T, force?: boolean): void;
    /**
     * @internal
     */
    ngOnDestroy(): void;
    static ɵfac: i0.ɵɵFactoryDeclaration<ThyPopover, [null, null, { optional: true; }, null, null, null, { optional: true; }, null, null]>;
    static ɵprov: i0.ɵɵInjectableDeclaration<ThyPopover>;
}
