import { ThyAbstractInternalOverlayRef, ThyAbstractOverlayRef } from 'ngx-tethys/core';
import { OverlayRef } from '@angular/cdk/overlay';
import { ThyPopoverContainer } from './popover-container.component';
import { ThyPopoverConfig } from './popover.config';
export declare abstract class ThyPopoverRef<T, TResult = unknown, TData = unknown> extends ThyAbstractOverlayRef<T, ThyPopoverContainer<TData>, TResult> {
}
export declare class ThyInternalPopoverRef<T, TResult = unknown> extends ThyAbstractInternalOverlayRef<T, ThyPopoverContainer, TResult> implements ThyPopoverRef<T, TResult> {
    constructor(overlayRef: OverlayRef, containerInstance: ThyPopoverContainer, config: ThyPopoverConfig);
    /**
     * Updates the popover's position.
     * @param position New popover position.
     */
    updatePosition(): this;
}
