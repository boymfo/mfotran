import { ThyAbstractOverlayContainer, ThyClickDispatcher, ThyPortalOutlet } from 'ngx-tethys/core';
import { Observable } from 'rxjs';
import { AnimationEvent } from '@angular/animations';
import { ContentObserver } from '@angular/cdk/observers';
import { AfterViewInit, ChangeDetectorRef, ElementRef, EventEmitter, NgZone, OnDestroy } from '@angular/core';
import { ThyPopoverConfig } from './popover.config';
import * as i0 from "@angular/core";
/**
 * @internal
 */
export declare class ThyPopoverContainer<TData = unknown> extends ThyAbstractOverlayContainer<TData> implements AfterViewInit, OnDestroy {
    private elementRef;
    config: ThyPopoverConfig<TData>;
    private thyClickDispatcher;
    private contentObserver;
    private ngZone;
    portalOutlet: ThyPortalOutlet;
    /** State of the popover animation. */
    animationState: 'void' | 'enter' | 'exit';
    /** Emits when an animation state changes. */
    animationStateChanged: EventEmitter<AnimationEvent>;
    animationOpeningDone: Observable<AnimationEvent>;
    animationClosingDone: Observable<AnimationEvent>;
    insideClicked: EventEmitter<any>;
    updatePosition: EventEmitter<any>;
    outsideClicked: EventEmitter<any>;
    beforeAttachPortal(): void;
    constructor(elementRef: ElementRef, config: ThyPopoverConfig<TData>, changeDetectorRef: ChangeDetectorRef, thyClickDispatcher: ThyClickDispatcher, contentObserver: ContentObserver, ngZone: NgZone);
    ngAfterViewInit(): void;
    /** Callback, invoked whenever an animation on the host completes. */
    onAnimationDone(event: AnimationEvent): void;
    /** Callback, invoked when an animation on the host starts. */
    onAnimationStart(event: AnimationEvent): void;
    startExitAnimation(): void;
    onInsideClick(): void;
    ngOnDestroy(): void;
    static ɵfac: i0.ɵɵFactoryDeclaration<ThyPopoverContainer<any>, never>;
    static ɵcmp: i0.ɵɵComponentDeclaration<ThyPopoverContainer<any>, "thy-popover-container", never, {}, {}, never, never, true, never>;
}
