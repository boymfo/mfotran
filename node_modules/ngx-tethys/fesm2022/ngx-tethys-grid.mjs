import * as i0 from '@angular/core';
import { Directive, Input, Optional, Host, InjectionToken, inject, DestroyRef, Component, ChangeDetectionStrategy, Inject, ContentChildren, NgModule } from '@angular/core';
import { ReplaySubject, Subject, Observable } from 'rxjs';
import { isString, hasLaterChange } from 'ngx-tethys/util';
import { useHostRenderer } from '@tethys/cdk/dom';
import { takeUntilDestroyed } from '@angular/core/rxjs-interop';
import * as i1 from '@angular/cdk/scrolling';
import { throttleTime } from 'rxjs/operators';
import { isUndefinedOrNull } from '@tethys/cdk/is';

/**
 * 栅格行指令
 * @name thyRow
 * @order 30
 */
class ThyRowDirective {
    constructor() {
        this.actualGutter$ = new ReplaySubject(1);
        this.hostRenderer = useHostRenderer();
    }
    ngOnInit() {
        this.setGutterStyle();
    }
    ngOnChanges() {
        this.setGutterStyle();
    }
    ngAfterViewInit() { }
    setGutterStyle() {
        const [horizontalGutter, verticalGutter] = this.getGutter();
        this.actualGutter$.next([horizontalGutter, verticalGutter]);
        const renderGutter = (name, gutter) => {
            this.hostRenderer.setStyle(name, `-${gutter / 2}px`);
        };
        if (horizontalGutter > 0) {
            renderGutter('margin-left', horizontalGutter);
            renderGutter('margin-right', horizontalGutter);
        }
        if (verticalGutter > 0) {
            renderGutter('margin-top', verticalGutter);
            renderGutter('margin-bottom', verticalGutter);
        }
    }
    getGutter() {
        if (isString(this.thyGutter)) {
            throw Error(`thyGutter value can not be string type`);
        }
        return [this.thyGutter, 0];
    }
    static { this.ɵfac = i0.ɵɵngDeclareFactory({ minVersion: "12.0.0", version: "17.2.3", ngImport: i0, type: ThyRowDirective, deps: [], target: i0.ɵɵFactoryTarget.Directive }); }
    static { this.ɵdir = i0.ɵɵngDeclareDirective({ minVersion: "14.0.0", version: "17.2.3", type: ThyRowDirective, isStandalone: true, selector: "[thyRow]", inputs: { thyGutter: "thyGutter" }, host: { classAttribute: "thy-row" }, usesOnChanges: true, ngImport: i0 }); }
}
i0.ɵɵngDeclareClassMetadata({ minVersion: "12.0.0", version: "17.2.3", ngImport: i0, type: ThyRowDirective, decorators: [{
            type: Directive,
            args: [{
                    selector: '[thyRow]',
                    host: {
                        class: 'thy-row'
                    },
                    standalone: true
                }]
        }], ctorParameters: () => [], propDecorators: { thyGutter: [{
                type: Input
            }] } });

/**
 * 栅格列指令
 * @name thyCol
 * @order 35
 */
class ThyColDirective {
    get span() {
        const span = this.thySpan ?? this.thyCol;
        return span || 24;
    }
    constructor(thyRowDirective) {
        this.thyRowDirective = thyRowDirective;
        this.hostRenderer = useHostRenderer();
        this.takeUntilDestroyed = takeUntilDestroyed();
    }
    ngOnInit() {
        this.updateHostClass();
    }
    ngOnChanges(changes) {
        this.updateHostClass();
    }
    ngAfterViewInit() {
        if (this.thyRowDirective) {
            this.thyRowDirective.actualGutter$.pipe(this.takeUntilDestroyed).subscribe(([horizontalGutter, verticalGutter]) => {
                const renderGutter = (name, gutter) => {
                    this.hostRenderer.setStyle(name, `${gutter / 2}px`);
                };
                if (horizontalGutter > 0) {
                    renderGutter('padding-left', horizontalGutter);
                    renderGutter('padding-right', horizontalGutter);
                }
                if (verticalGutter > 0) {
                    renderGutter('padding-top', verticalGutter);
                    renderGutter('padding-bottom', verticalGutter);
                }
            });
        }
    }
    updateHostClass() {
        this.hostRenderer.updateClassByMap({
            [`thy-col-${this.span}`]: true
        });
    }
    static { this.ɵfac = i0.ɵɵngDeclareFactory({ minVersion: "12.0.0", version: "17.2.3", ngImport: i0, type: ThyColDirective, deps: [{ token: ThyRowDirective, host: true, optional: true }], target: i0.ɵɵFactoryTarget.Directive }); }
    static { this.ɵdir = i0.ɵɵngDeclareDirective({ minVersion: "14.0.0", version: "17.2.3", type: ThyColDirective, isStandalone: true, selector: "[thyCol]", inputs: { thyCol: "thyCol", thySpan: "thySpan" }, host: { classAttribute: "thy-col" }, usesOnChanges: true, ngImport: i0 }); }
}
i0.ɵɵngDeclareClassMetadata({ minVersion: "12.0.0", version: "17.2.3", ngImport: i0, type: ThyColDirective, decorators: [{
            type: Directive,
            args: [{
                    selector: '[thyCol]',
                    host: {
                        class: 'thy-col'
                    },
                    standalone: true
                }]
        }], ctorParameters: () => [{ type: ThyRowDirective, decorators: [{
                    type: Optional
                }, {
                    type: Host
                }] }], propDecorators: { thyCol: [{
                type: Input
            }], thySpan: [{
                type: Input
            }] } });

class ThyGridToken {
}
const THY_GRID_COMPONENT = new InjectionToken('THY_GRID_COMPONENT');

/**
 * 栅格项组件
 * @name thy-grid-item,[thyGridItem]
 * @order 15
 */
class ThyGridItem {
    constructor(elementRef, grid) {
        this.elementRef = elementRef;
        this.grid = grid;
        /**
         * 栅格项的占位列数，为 0 时会隐藏该栅格项
         * @default 1
         */
        this.thySpan = THY_GRID_ITEM_DEFAULT_SPAN;
        /**
         * 栅格项左侧的偏移列数
         */
        this.thyOffset = 0;
        this.destroyRef = inject(DestroyRef);
        this.hostRenderer = useHostRenderer();
        this.span = THY_GRID_ITEM_DEFAULT_SPAN;
        this.offset = 0;
    }
    ngOnInit() {
        this.grid.gridItemPropValueChange$.pipe(takeUntilDestroyed(this.destroyRef)).subscribe(() => {
            this.setGridItemStyle();
        });
    }
    setGridItemStyle() {
        const xGap = this.grid?.xGap || 0;
        this.hostRenderer.setStyle('display', this.span === 0 ? 'none' : '');
        this.hostRenderer.setStyle('grid-column', `span ${this.span}`);
        this.hostRenderer.setStyle('margin-left', this.offset ? `calc(((100% - ${(this.span - 1) * xGap}px) / ${this.span} + ${xGap}px) * ${this.offset})` : '');
    }
    static { this.ɵfac = i0.ɵɵngDeclareFactory({ minVersion: "12.0.0", version: "17.2.3", ngImport: i0, type: ThyGridItem, deps: [{ token: i0.ElementRef }, { token: THY_GRID_COMPONENT, optional: true }], target: i0.ɵɵFactoryTarget.Component }); }
    static { this.ɵcmp = i0.ɵɵngDeclareComponent({ minVersion: "14.0.0", version: "17.2.3", type: ThyGridItem, isStandalone: true, selector: "thy-grid-item,[thyGridItem]", inputs: { thySpan: "thySpan", thyOffset: "thyOffset" }, host: { classAttribute: "thy-grid-item" }, ngImport: i0, template: '<ng-content></ng-content>', isInline: true, changeDetection: i0.ChangeDetectionStrategy.OnPush }); }
}
i0.ɵɵngDeclareClassMetadata({ minVersion: "12.0.0", version: "17.2.3", ngImport: i0, type: ThyGridItem, decorators: [{
            type: Component,
            args: [{
                    selector: 'thy-grid-item,[thyGridItem]',
                    template: '<ng-content></ng-content>',
                    changeDetection: ChangeDetectionStrategy.OnPush,
                    host: {
                        class: 'thy-grid-item'
                    },
                    standalone: true
                }]
        }], ctorParameters: () => [{ type: i0.ElementRef }, { type: ThyGridToken, decorators: [{
                    type: Optional
                }, {
                    type: Inject,
                    args: [THY_GRID_COMPONENT]
                }] }], propDecorators: { thySpan: [{
                type: Input
            }], thyOffset: [{
                type: Input
            }] } });

const THY_GRID_DEFAULT_COLUMNS = 24;
const THY_GRID_ITEM_DEFAULT_SPAN = 1;
const screenBreakpointsMap = {
    xs: 0,
    sm: 576,
    md: 768,
    lg: 992,
    xl: 1200
};
/**
 * 栅格组件
 * @name thy-grid, [thyGrid]
 * @order 10
 */
// eslint-disable-next-line @angular-eslint/directive-class-suffix
class ThyGrid {
    constructor(elementRef, viewportRuler, ngZone) {
        this.elementRef = elementRef;
        this.viewportRuler = viewportRuler;
        this.ngZone = ngZone;
        /**
         * 栅格的列数
         * @default 24
         */
        this.thyCols = THY_GRID_DEFAULT_COLUMNS;
        /**
         * 栅格的水平间隔
         */
        this.thyXGap = 0;
        /**
         * 栅格的垂直间隔
         */
        this.thyYGap = 0;
        /**
         * 栅格的水平和垂直间隔
         */
        this.thyGap = 0;
        /**
         * 响应式栅格列数<br/>
         * none: 不进行响应式布局。<br/>
         * self：根据grid的自身宽度进行响应式布局。<br/>
         * screen：根据屏幕断点进行响应式布局，目前预设了5种响应式尺寸：`xs: 0, sm: 576, md: 768, lg: 992, xl: 1200`。
         */
        this.thyResponsive = 'none';
        this.hostRenderer = useHostRenderer();
        this.numRegex = /^\d+$/;
        this.gridItemPropValueChange$ = new Subject();
        this.takeUntilDestroyed = takeUntilDestroyed();
    }
    ngOnInit() {
        this.setGridStyle();
        if (this.thyResponsive !== 'none') {
            this.listenResizeEvent();
        }
    }
    ngOnChanges(changes) { }
    ngAfterContentInit() {
        this.handleGridItems();
        this.gridItems.changes.pipe(this.takeUntilDestroyed).subscribe(() => {
            Promise.resolve().then(() => {
                this.handleGridItems();
            });
        });
    }
    setGridStyle() {
        this.cols = this.calculateActualValue(this.thyCols || THY_GRID_DEFAULT_COLUMNS, THY_GRID_DEFAULT_COLUMNS);
        if (!this.thyXGap && !this.thyYGap) {
            this.xGap = this.calculateActualValue(this.thyGap || 0);
            this.yGap = this.xGap;
        }
        else {
            this.xGap = this.calculateActualValue(this.thyXGap || this.thyGap);
            this.yGap = this.calculateActualValue(this.thyYGap || this.thyGap);
        }
        this.hostRenderer.setStyle('display', 'grid');
        this.hostRenderer.setStyle('grid-template-columns', `repeat(${this.cols}, minmax(0, 1fr))`);
        this.hostRenderer.setStyle('gap', `${this.yGap}px ${this.xGap}px`);
    }
    listenResizeEvent() {
        if (this.thyResponsive === 'screen') {
            this.viewportRuler
                .change(100)
                .pipe(this.takeUntilDestroyed)
                .subscribe(() => {
                this.responsiveContainerWidth = this.viewportRuler.getViewportSize().width;
                this.setGridStyle();
                this.handleGridItems();
            });
        }
        else {
            this.ngZone.runOutsideAngular(() => {
                this.gridResizeObserver(this.elementRef.nativeElement)
                    .pipe(throttleTime(100), this.takeUntilDestroyed)
                    .subscribe(data => {
                    this.responsiveContainerWidth = data[0]?.contentRect?.width;
                    this.setGridStyle();
                    this.handleGridItems();
                });
            });
        }
    }
    handleGridItems() {
        this.gridItems.forEach((gridItem) => {
            const rawSpan = getRawSpan(gridItem.thySpan);
            const span = this.calculateActualValue(rawSpan, THY_GRID_ITEM_DEFAULT_SPAN);
            const offset = this.calculateActualValue(gridItem.thyOffset || 0);
            gridItem.span = Math.min(span + offset, this.cols);
            gridItem.offset = offset;
        });
        this.gridItemPropValueChange$.next();
    }
    calculateActualValue(rawValue, defaultValue) {
        if (this.numRegex.test(rawValue.toString().trim())) {
            return Number(rawValue);
        }
        else {
            const responsiveValueMap = this.getResponsiveValueMap(rawValue);
            const breakpointKeys = Object.keys(responsiveValueMap);
            const breakpoint = this.calculateBreakPoint(breakpointKeys);
            if (this.thyResponsive !== 'none' && breakpoint) {
                return responsiveValueMap[breakpoint];
            }
            else if (breakpointKeys.includes('0')) {
                return responsiveValueMap['0'];
            }
            else {
                return defaultValue || 0;
            }
        }
    }
    getResponsiveValueMap(responsiveValue) {
        return responsiveValue.split(' ').reduce((map, item) => {
            if (this.numRegex.test(item.toString())) {
                item = `0:${item}`;
            }
            const [key, value] = item.split(':');
            map[key] = Number(value);
            return map;
        }, {});
    }
    calculateBreakPoint(breakpointKeys) {
        if (this.thyResponsive === 'screen') {
            const width = this.responsiveContainerWidth || this.viewportRuler.getViewportSize().width;
            return breakpointKeys.find((key, index) => {
                return index < breakpointKeys.length - 1
                    ? width >= screenBreakpointsMap[key] && width < screenBreakpointsMap[breakpointKeys[index + 1]]
                    : width >= screenBreakpointsMap[key];
            });
        }
        else {
            const width = this.responsiveContainerWidth || this.elementRef.nativeElement.getBoundingClientRect().width;
            return breakpointKeys.find((key, index) => {
                return index < breakpointKeys.length - 1
                    ? width >= Number(key) && width < Number(breakpointKeys[index + 1])
                    : width >= Number(key);
            });
        }
    }
    gridResizeObserver(element) {
        return new Observable(observer => {
            const resize = new ResizeObserver((entries) => {
                observer.next(entries);
            });
            resize.observe(element);
            return () => {
                resize.disconnect();
            };
        });
    }
    static { this.ɵfac = i0.ɵɵngDeclareFactory({ minVersion: "12.0.0", version: "17.2.3", ngImport: i0, type: ThyGrid, deps: [{ token: i0.ElementRef }, { token: i1.ViewportRuler }, { token: i0.NgZone }], target: i0.ɵɵFactoryTarget.Directive }); }
    static { this.ɵdir = i0.ɵɵngDeclareDirective({ minVersion: "14.0.0", version: "17.2.3", type: ThyGrid, isStandalone: true, selector: "[thyGrid]", inputs: { thyCols: "thyCols", thyXGap: "thyXGap", thyYGap: "thyYGap", thyGap: "thyGap", thyResponsive: "thyResponsive" }, host: { classAttribute: "thy-grid" }, providers: [
            {
                provide: THY_GRID_COMPONENT,
                useExisting: ThyGrid
            }
        ], queries: [{ propertyName: "gridItems", predicate: ThyGridItem }], usesOnChanges: true, ngImport: i0 }); }
}
i0.ɵɵngDeclareClassMetadata({ minVersion: "12.0.0", version: "17.2.3", ngImport: i0, type: ThyGrid, decorators: [{
            type: Directive,
            args: [{
                    selector: '[thyGrid]',
                    providers: [
                        {
                            provide: THY_GRID_COMPONENT,
                            useExisting: ThyGrid
                        }
                    ],
                    host: {
                        class: 'thy-grid'
                    },
                    standalone: true
                }]
        }], ctorParameters: () => [{ type: i0.ElementRef }, { type: i1.ViewportRuler }, { type: i0.NgZone }], propDecorators: { gridItems: [{
                type: ContentChildren,
                args: [ThyGridItem]
            }], thyCols: [{
                type: Input
            }], thyXGap: [{
                type: Input
            }], thyYGap: [{
                type: Input
            }], thyGap: [{
                type: Input
            }], thyResponsive: [{
                type: Input
            }] } });
/**
 * @internal
 */
class ThyGridComponent {
    constructor() {
        this.grid = inject(ThyGrid);
    }
    static { this.ɵfac = i0.ɵɵngDeclareFactory({ minVersion: "12.0.0", version: "17.2.3", ngImport: i0, type: ThyGridComponent, deps: [], target: i0.ɵɵFactoryTarget.Component }); }
    static { this.ɵcmp = i0.ɵɵngDeclareComponent({ minVersion: "14.0.0", version: "17.2.3", type: ThyGridComponent, isStandalone: true, selector: "thy-grid", providers: [
            {
                provide: THY_GRID_COMPONENT,
                useExisting: ThyGrid
            }
        ], hostDirectives: [{ directive: ThyGrid, inputs: ["thyCols", "thyCols", "thyXGap", "thyXGap", "thyYGap", "thyYGap", "thyGap", "thyGap", "thyResponsive", "thyResponsive"] }], ngImport: i0, template: '<ng-content></ng-content>', isInline: true, changeDetection: i0.ChangeDetectionStrategy.OnPush }); }
}
i0.ɵɵngDeclareClassMetadata({ minVersion: "12.0.0", version: "17.2.3", ngImport: i0, type: ThyGridComponent, decorators: [{
            type: Component,
            args: [{
                    selector: 'thy-grid',
                    template: '<ng-content></ng-content>',
                    changeDetection: ChangeDetectionStrategy.OnPush,
                    standalone: true,
                    imports: [ThyGrid],
                    providers: [
                        {
                            provide: THY_GRID_COMPONENT,
                            useExisting: ThyGrid
                        }
                    ],
                    hostDirectives: [
                        {
                            directive: ThyGrid,
                            inputs: ['thyCols', 'thyXGap', 'thyYGap', 'thyGap', 'thyResponsive']
                        }
                    ]
                }]
        }] });
function getRawSpan(span) {
    return span === undefined || span === null ? THY_GRID_ITEM_DEFAULT_SPAN : span;
}

/**
 * 设置容器为 Flex 布局组件
 * @name thy-flex, [thyFlex]
 * @order 20
 */
// eslint-disable-next-line @angular-eslint/directive-class-suffix
class ThyFlex {
    get direction() {
        const direction = this.thyDirection ?? this.thyDirection;
        return direction || 'row';
    }
    constructor() {
        this.hostRenderer = useHostRenderer();
    }
    ngOnInit() {
        this.updateClasses();
    }
    ngOnChanges(changes) {
        if (hasLaterChange(changes)) {
            this.updateClasses();
        }
    }
    updateClasses() {
        const classes = [];
        if (!isUndefinedOrNull(this.thyJustifyContent)) {
            classes.push(`justify-content-${normalizeStartEnd(this.thyJustifyContent)}`);
        }
        if (!isUndefinedOrNull(this.thyAlignItems)) {
            classes.push(`align-items-${normalizeStartEnd(this.thyAlignItems)}`);
        }
        if (!isUndefinedOrNull(this.thyWrap)) {
            classes.push(`flex-${this.thyWrap}`);
        }
        if (!isUndefinedOrNull(this.direction)) {
            classes.push(`flex-${this.direction}`);
        }
        this.hostRenderer.updateClass(classes);
        this.hostRenderer.setStyle('gap', `${this.thyGap ?? '0'}px`);
    }
    static { this.ɵfac = i0.ɵɵngDeclareFactory({ minVersion: "12.0.0", version: "17.2.3", ngImport: i0, type: ThyFlex, deps: [], target: i0.ɵɵFactoryTarget.Directive }); }
    static { this.ɵdir = i0.ɵɵngDeclareDirective({ minVersion: "14.0.0", version: "17.2.3", type: ThyFlex, isStandalone: true, selector: "[thyFlex]", inputs: { thyDirection: "thyDirection", thyWrap: "thyWrap", thyJustifyContent: "thyJustifyContent", thyAlignItems: "thyAlignItems", thyGap: "thyGap" }, host: { classAttribute: "thy-flex d-flex" }, usesOnChanges: true, ngImport: i0 }); }
}
i0.ɵɵngDeclareClassMetadata({ minVersion: "12.0.0", version: "17.2.3", ngImport: i0, type: ThyFlex, decorators: [{
            type: Directive,
            args: [{
                    selector: '[thyFlex]',
                    standalone: true,
                    host: {
                        class: 'thy-flex d-flex'
                    }
                }]
        }], ctorParameters: () => [], propDecorators: { thyDirection: [{
                type: Input
            }], thyWrap: [{
                type: Input
            }], thyJustifyContent: [{
                type: Input
            }], thyAlignItems: [{
                type: Input
            }], thyGap: [{
                type: Input
            }] } });
/**
 * @internal
 */
class ThyFlexComponent {
    static { this.ɵfac = i0.ɵɵngDeclareFactory({ minVersion: "12.0.0", version: "17.2.3", ngImport: i0, type: ThyFlexComponent, deps: [], target: i0.ɵɵFactoryTarget.Component }); }
    static { this.ɵcmp = i0.ɵɵngDeclareComponent({ minVersion: "14.0.0", version: "17.2.3", type: ThyFlexComponent, isStandalone: true, selector: "thy-flex", hostDirectives: [{ directive: ThyFlex, inputs: ["thyDirection", "thyDirection", "thyWrap", "thyWrap", "thyJustifyContent", "thyJustifyContent", "thyAlignItems", "thyAlignItems", "thyGap", "thyGap"] }], ngImport: i0, template: `<ng-content></ng-content>`, isInline: true, changeDetection: i0.ChangeDetectionStrategy.OnPush }); }
}
i0.ɵɵngDeclareClassMetadata({ minVersion: "12.0.0", version: "17.2.3", ngImport: i0, type: ThyFlexComponent, decorators: [{
            type: Component,
            args: [{
                    selector: 'thy-flex',
                    template: `<ng-content></ng-content>`,
                    standalone: true,
                    changeDetection: ChangeDetectionStrategy.OnPush,
                    hostDirectives: [
                        {
                            directive: ThyFlex,
                            inputs: ['thyDirection', 'thyWrap', 'thyJustifyContent', 'thyAlignItems', 'thyGap']
                        }
                    ],
                    imports: [ThyFlex]
                }]
        }] });
/**
 * 设置为 Flex Item 组件
 * @name thy-flex-item, [thyFlexItem]
 * @order 25
 */
// eslint-disable-next-line @angular-eslint/directive-class-suffix
class ThyFlexItem {
    constructor() {
        this.hostRenderer = useHostRenderer();
    }
    ngOnInit() {
        this.updateClasses();
    }
    ngOnChanges(changes) {
        if (hasLaterChange(changes)) {
            this.updateClasses();
        }
    }
    updateClasses() {
        const classes = [];
        this.hostRenderer.setStyle('flex', '');
        this.hostRenderer.setStyle('basis', '');
        if (this.thyFlexItem) {
            if (this.thyFlexItem === 'fill') {
                classes.push(`flex-${this.thyFlexItem}`);
            }
            else {
                this.hostRenderer.setStyle('flex', this.thyFlexItem);
            }
        }
        if (!isUndefinedOrNull(this.thyGrow)) {
            classes.push(`flex-grow-${this.thyGrow}`);
        }
        if (!isUndefinedOrNull(this.thyShrink)) {
            classes.push(`flex-shrink-${this.thyShrink}`);
        }
        if (!isUndefinedOrNull(this.thyBasis)) {
            this.hostRenderer.setStyle('flex-basis', this.thyBasis);
        }
        this.hostRenderer.updateClass(classes);
    }
    static { this.ɵfac = i0.ɵɵngDeclareFactory({ minVersion: "12.0.0", version: "17.2.3", ngImport: i0, type: ThyFlexItem, deps: [], target: i0.ɵɵFactoryTarget.Directive }); }
    static { this.ɵdir = i0.ɵɵngDeclareDirective({ minVersion: "14.0.0", version: "17.2.3", type: ThyFlexItem, isStandalone: true, selector: "[thyFlexItem]", inputs: { thyFlexItem: "thyFlexItem", thyGrow: "thyGrow", thyShrink: "thyShrink", thyBasis: "thyBasis" }, host: { classAttribute: "thy-flex-item" }, usesOnChanges: true, ngImport: i0 }); }
}
i0.ɵɵngDeclareClassMetadata({ minVersion: "12.0.0", version: "17.2.3", ngImport: i0, type: ThyFlexItem, decorators: [{
            type: Directive,
            args: [{
                    selector: '[thyFlexItem]',
                    standalone: true,
                    host: {
                        class: 'thy-flex-item'
                    }
                }]
        }], ctorParameters: () => [], propDecorators: { thyFlexItem: [{
                type: Input
            }], thyGrow: [{
                type: Input
            }], thyShrink: [{
                type: Input
            }], thyBasis: [{
                type: Input
            }] } });
/**
 * @internal
 */
class ThyFlexItemComponent {
    static { this.ɵfac = i0.ɵɵngDeclareFactory({ minVersion: "12.0.0", version: "17.2.3", ngImport: i0, type: ThyFlexItemComponent, deps: [], target: i0.ɵɵFactoryTarget.Component }); }
    static { this.ɵcmp = i0.ɵɵngDeclareComponent({ minVersion: "14.0.0", version: "17.2.3", type: ThyFlexItemComponent, isStandalone: true, selector: "thy-flex-item", hostDirectives: [{ directive: ThyFlexItem, inputs: ["thyFlexItem", "thyFlexItem", "thyGrow", "thyGrow", "thyShrink", "thyShrink", "thyBasis", "thyBasis"] }], ngImport: i0, template: `<ng-content></ng-content>`, isInline: true, changeDetection: i0.ChangeDetectionStrategy.OnPush }); }
}
i0.ɵɵngDeclareClassMetadata({ minVersion: "12.0.0", version: "17.2.3", ngImport: i0, type: ThyFlexItemComponent, decorators: [{
            type: Component,
            args: [{
                    selector: 'thy-flex-item',
                    template: `<ng-content></ng-content>`,
                    standalone: true,
                    changeDetection: ChangeDetectionStrategy.OnPush,
                    imports: [ThyFlexItem],
                    hostDirectives: [
                        {
                            directive: ThyFlexItem,
                            inputs: ['thyFlexItem', 'thyGrow', 'thyShrink', 'thyBasis']
                        }
                    ]
                }]
        }] });
function normalizeStartEnd(value) {
    return value === 'flex-start' ? 'start' : value === 'flex-end' ? 'end' : value;
}

class ThyGridModule {
    static { this.ɵfac = i0.ɵɵngDeclareFactory({ minVersion: "12.0.0", version: "17.2.3", ngImport: i0, type: ThyGridModule, deps: [], target: i0.ɵɵFactoryTarget.NgModule }); }
    static { this.ɵmod = i0.ɵɵngDeclareNgModule({ minVersion: "14.0.0", version: "17.2.3", ngImport: i0, type: ThyGridModule, imports: [ThyGrid,
            ThyGridComponent,
            ThyGridItem,
            ThyRowDirective,
            ThyColDirective,
            ThyFlex,
            ThyFlexItem,
            ThyFlexComponent,
            ThyFlexItemComponent], exports: [ThyGrid,
            ThyGridComponent,
            ThyGridItem,
            ThyRowDirective,
            ThyColDirective,
            ThyFlex,
            ThyFlexItem,
            ThyFlexComponent,
            ThyFlexItemComponent] }); }
    static { this.ɵinj = i0.ɵɵngDeclareInjector({ minVersion: "12.0.0", version: "17.2.3", ngImport: i0, type: ThyGridModule }); }
}
i0.ɵɵngDeclareClassMetadata({ minVersion: "12.0.0", version: "17.2.3", ngImport: i0, type: ThyGridModule, decorators: [{
            type: NgModule,
            args: [{
                    exports: [
                        ThyGrid,
                        ThyGridComponent,
                        ThyGridItem,
                        ThyRowDirective,
                        ThyColDirective,
                        ThyFlex,
                        ThyFlexItem,
                        ThyFlexComponent,
                        ThyFlexItemComponent
                    ],
                    imports: [
                        ThyGrid,
                        ThyGridComponent,
                        ThyGridItem,
                        ThyRowDirective,
                        ThyColDirective,
                        ThyFlex,
                        ThyFlexItem,
                        ThyFlexComponent,
                        ThyFlexItemComponent
                    ]
                }]
        }] });

/**
 * Generated bundle index. Do not edit.
 */

export { THY_GRID_DEFAULT_COLUMNS, THY_GRID_ITEM_DEFAULT_SPAN, ThyColDirective, ThyFlex, ThyFlexComponent, ThyFlexItem, ThyFlexItemComponent, ThyGrid, ThyGridComponent, ThyGridItem, ThyGridModule, ThyRowDirective, screenBreakpointsMap };
//# sourceMappingURL=ngx-tethys-grid.mjs.map
