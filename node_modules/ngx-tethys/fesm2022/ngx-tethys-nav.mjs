import { NgIf, NgClass, NgTemplateOutlet, NgFor, CommonModule } from '@angular/common';
import * as i0 from '@angular/core';
import { booleanAttribute, Component, HostBinding, Input, ChangeDetectionStrategy, inject, DestroyRef, Directive, Optional, ContentChildren, forwardRef, ANIMATION_MODULE_TYPE, Inject, Pipe, ContentChild, ViewChild, NgModule } from '@angular/core';
import { ThyIcon, ThyIconModule } from 'ngx-tethys/icon';
import { useHostRenderer } from '@tethys/cdk/dom';
import { warnDeprecation } from 'ngx-tethys/util';
import { takeUntilDestroyed } from '@angular/core/rxjs-interop';
import * as i1 from '@angular/router';
import { RouterLinkActive, RouterModule } from '@angular/router';
import * as i1$2 from 'ngx-tethys/popover';
import { ThyPopoverModule } from 'ngx-tethys/popover';
import { merge, of, Observable } from 'rxjs';
import { take, debounceTime, tap } from 'rxjs/operators';
import * as i1$1 from '@angular/platform-browser';
import { ThyDropdownMenuComponent, ThyDropdownMenuItemDirective, ThyDropdownMenuItemActiveDirective, ThyDropdownModule } from 'ngx-tethys/dropdown';

/**
 * @private
 */
class ThyIconNavLink {
    constructor() {
        this.navLinkActive = false;
        this.navLinkClass = true;
    }
    set thyIconNavLinkIcon(icon) {
        this.icon = icon;
    }
    set thyIconNavLinkActive(active) {
        this.navLinkActive = active;
    }
    static { this.ɵfac = i0.ɵɵngDeclareFactory({ minVersion: "12.0.0", version: "17.2.3", ngImport: i0, type: ThyIconNavLink, deps: [], target: i0.ɵɵFactoryTarget.Component }); }
    static { this.ɵcmp = i0.ɵɵngDeclareComponent({ minVersion: "16.1.0", version: "17.2.3", type: ThyIconNavLink, isStandalone: true, selector: "[thyIconNavLink]", inputs: { thyIconNavLinkIcon: "thyIconNavLinkIcon", thyIconNavLinkActive: ["thyIconNavLinkActive", "thyIconNavLinkActive", booleanAttribute] }, host: { properties: { "class.active": "this.navLinkActive", "class.thy-icon-nav-link": "this.navLinkClass" } }, ngImport: i0, template: '<ng-content></ng-content><thy-icon *ngIf="icon" [thyIconName]="icon"></thy-icon>', isInline: true, dependencies: [{ kind: "directive", type: NgIf, selector: "[ngIf]", inputs: ["ngIf", "ngIfThen", "ngIfElse"] }, { kind: "component", type: ThyIcon, selector: "thy-icon, [thy-icon]", inputs: ["thyIconType", "thyTwotoneColor", "thyIconName", "thyIconRotate", "thyIconSet", "thyIconLegging", "thyIconLinearGradient"] }] }); }
}
i0.ɵɵngDeclareClassMetadata({ minVersion: "12.0.0", version: "17.2.3", ngImport: i0, type: ThyIconNavLink, decorators: [{
            type: Component,
            args: [{
                    // eslint-disable-next-line @angular-eslint/component-selector
                    selector: '[thyIconNavLink]',
                    template: '<ng-content></ng-content><thy-icon *ngIf="icon" [thyIconName]="icon"></thy-icon>',
                    standalone: true,
                    imports: [NgIf, ThyIcon]
                }]
        }], propDecorators: { navLinkActive: [{
                type: HostBinding,
                args: ['class.active']
            }], navLinkClass: [{
                type: HostBinding,
                args: ['class.thy-icon-nav-link']
            }], thyIconNavLinkIcon: [{
                type: Input
            }], thyIconNavLinkActive: [{
                type: Input,
                args: [{ transform: booleanAttribute }]
            }] } });

/**
 * @private
 */
class ThyIconNav {
    set thyType(type) {
        this.type = type;
        this.updateClasses();
        this.changeDetectorRef.markForCheck();
    }
    updateClasses(bypassInitialized) {
        if (!bypassInitialized && !this.initialized) {
            return;
        }
        this.hostRenderer.updateClass(this.type ? [`thy-icon-nav-${this.type}`] : []);
    }
    constructor(changeDetectorRef) {
        this.changeDetectorRef = changeDetectorRef;
        this.initialized = false;
        this.hostRenderer = useHostRenderer();
        this.isIconNav = true;
        if (typeof ngDevMode === 'undefined' || ngDevMode) {
            warnDeprecation('thy-icon-nav has been deprecated, please use thyAction and thy-space components instead of it');
        }
    }
    ngOnInit() {
        this.initialized = true;
        this.updateClasses(true);
    }
    static { this.ɵfac = i0.ɵɵngDeclareFactory({ minVersion: "12.0.0", version: "17.2.3", ngImport: i0, type: ThyIconNav, deps: [{ token: i0.ChangeDetectorRef }], target: i0.ɵɵFactoryTarget.Component }); }
    static { this.ɵcmp = i0.ɵɵngDeclareComponent({ minVersion: "14.0.0", version: "17.2.3", type: ThyIconNav, isStandalone: true, selector: "thy-icon-nav", inputs: { thyType: "thyType" }, host: { properties: { "class.thy-icon-nav": "this.isIconNav" } }, ngImport: i0, template: "<ng-content></ng-content>\n", changeDetection: i0.ChangeDetectionStrategy.OnPush }); }
}
i0.ɵɵngDeclareClassMetadata({ minVersion: "12.0.0", version: "17.2.3", ngImport: i0, type: ThyIconNav, decorators: [{
            type: Component,
            args: [{ selector: 'thy-icon-nav', changeDetection: ChangeDetectionStrategy.OnPush, standalone: true, template: "<ng-content></ng-content>\n" }]
        }], ctorParameters: () => [{ type: i0.ChangeDetectorRef }], propDecorators: { isIconNav: [{
                type: HostBinding,
                args: [`class.thy-icon-nav`]
            }], thyType: [{
                type: Input
            }] } });

/**
 * 导航项
 * @name thyNavItem
 * @order 20
 */
class ThyNavItemDirective {
    constructor(elementRef, routerLinkActive, ngZone) {
        this.elementRef = elementRef;
        this.routerLinkActive = routerLinkActive;
        this.ngZone = ngZone;
        // @HostBinding('attr.href') navLinkHref = 'javascript:;';
        this.offset = {
            width: 0,
            height: 0,
            left: 0,
            top: 0
        };
        this.hostRenderer = useHostRenderer();
        this.destroyRef = inject(DestroyRef);
    }
    ngAfterViewInit() {
        this.setOffset();
        this.content = this.elementRef.nativeElement.outerHTML;
        this.ngZone.onStable.pipe(takeUntilDestroyed(this.destroyRef)).subscribe(() => {
            this.isActive = this.linkIsActive();
        });
    }
    setOffset() {
        this.offset = {
            width: this.elementRef.nativeElement.offsetWidth,
            height: this.elementRef.nativeElement.offsetHeight,
            left: this.elementRef.nativeElement.offsetLeft,
            top: this.elementRef.nativeElement.offsetTop
        };
    }
    linkIsActive() {
        return (this.thyNavItemActive ||
            this.thyNavLinkActive ||
            (this.routerLinkActive && this.routerLinkActive.isActive) ||
            this.routers.some(router => router.isActive) ||
            this.links.some(item => item.thyNavItemActive) ||
            this.links.some(item => item.thyNavLinkActive));
    }
    setNavLinkHidden(value) {
        if (value) {
            this.hostRenderer.addClass('thy-nav-item-hidden');
        }
        else {
            this.hostRenderer.removeClass('thy-nav-item-hidden');
        }
    }
    static { this.ɵfac = i0.ɵɵngDeclareFactory({ minVersion: "12.0.0", version: "17.2.3", ngImport: i0, type: ThyNavItemDirective, deps: [{ token: i0.ElementRef }, { token: i1.RouterLinkActive, optional: true }, { token: i0.NgZone }], target: i0.ɵɵFactoryTarget.Directive }); }
    static { this.ɵdir = i0.ɵɵngDeclareDirective({ minVersion: "16.1.0", version: "17.2.3", type: ThyNavItemDirective, isStandalone: true, selector: "[thyNavLink],[thyNavItem]", inputs: { thyNavItemActive: ["thyNavItemActive", "thyNavItemActive", booleanAttribute], thyNavLinkActive: ["thyNavLinkActive", "thyNavLinkActive", booleanAttribute], thyNavItemDisabled: ["thyNavItemDisabled", "thyNavItemDisabled", booleanAttribute] }, host: { properties: { "class.active": "thyNavItemActive || thyNavLinkActive", "class.disabled": "thyNavItemDisabled" }, classAttribute: "thy-nav-item" }, queries: [{ propertyName: "links", predicate: i0.forwardRef(() => ThyNavItemDirective), descendants: true }, { propertyName: "routers", predicate: RouterLinkActive, descendants: true }], ngImport: i0 }); }
}
i0.ɵɵngDeclareClassMetadata({ minVersion: "12.0.0", version: "17.2.3", ngImport: i0, type: ThyNavItemDirective, decorators: [{
            type: Directive,
            args: [{
                    selector: '[thyNavLink],[thyNavItem]',
                    host: {
                        class: 'thy-nav-item',
                        '[class.active]': 'thyNavItemActive || thyNavLinkActive',
                        '[class.disabled]': 'thyNavItemDisabled'
                    },
                    standalone: true
                }]
        }], ctorParameters: () => [{ type: i0.ElementRef }, { type: i1.RouterLinkActive, decorators: [{
                    type: Optional
                }] }, { type: i0.NgZone }], propDecorators: { thyNavItemActive: [{
                type: Input,
                args: [{ transform: booleanAttribute }]
            }], thyNavLinkActive: [{
                type: Input,
                args: [{ transform: booleanAttribute }]
            }], thyNavItemDisabled: [{
                type: Input,
                args: [{ transform: booleanAttribute }]
            }], links: [{
                type: ContentChildren,
                args: [forwardRef(() => ThyNavItemDirective), { descendants: true }]
            }], routers: [{
                type: ContentChildren,
                args: [RouterLinkActive, { descendants: true }]
            }] } });

/**
 * @internal
 */
class ThyNavInkBarDirective {
    get animated() {
        return this.animationMode !== 'NoopAnimations' && this.showInkBar;
    }
    constructor(elementRef, ngZone, animationMode) {
        this.elementRef = elementRef;
        this.ngZone = ngZone;
        this.animationMode = animationMode;
    }
    alignToElement(element) {
        this.show();
        this.ngZone.run(() => {
            this.ngZone.onStable.pipe(take(1)).subscribe(() => {
                this.setStyles(element);
            });
        });
    }
    setStyles(element) {
        const inkBar = this.elementRef.nativeElement;
        if (!this.isVertical) {
            inkBar.style.top = '';
            inkBar.style.bottom = '0px';
            inkBar.style.height = '2px';
            inkBar.style.left = this.getLeftPosition(element);
            inkBar.style.width = this.getElementWidth(element);
        }
        else {
            inkBar.style.left = '';
            inkBar.style.width = '2px';
            inkBar.style.top = this.getTopPosition(element);
            inkBar.style.height = this.getElementHeight(element);
        }
    }
    getLeftPosition(element) {
        return element ? `${element.offsetLeft || 0}px` : '0';
    }
    getElementWidth(element) {
        return element ? `${element.offsetWidth || 0}px` : '0';
    }
    getTopPosition(element) {
        return element ? `${element.offsetTop || 0}px` : '0';
    }
    getElementHeight(element) {
        return element ? `${element.offsetHeight || 0}px` : '0';
    }
    show() {
        this.elementRef.nativeElement.style.visibility = 'visible';
    }
    hide() {
        this.elementRef.nativeElement.style.visibility = 'hidden';
    }
    static { this.ɵfac = i0.ɵɵngDeclareFactory({ minVersion: "12.0.0", version: "17.2.3", ngImport: i0, type: ThyNavInkBarDirective, deps: [{ token: i0.ElementRef }, { token: i0.NgZone }, { token: ANIMATION_MODULE_TYPE, optional: true }], target: i0.ɵɵFactoryTarget.Directive }); }
    static { this.ɵdir = i0.ɵɵngDeclareDirective({ minVersion: "16.1.0", version: "17.2.3", type: ThyNavInkBarDirective, isStandalone: true, selector: "thy-nav-ink-bar, [thyNavInkBar]", inputs: { isVertical: ["isVertical", "isVertical", booleanAttribute], showInkBar: ["showInkBar", "showInkBar", booleanAttribute] }, host: { properties: { "class.thy-nav-ink-bar-animated": "animated" }, classAttribute: "thy-nav-ink-bar" }, ngImport: i0 }); }
}
i0.ɵɵngDeclareClassMetadata({ minVersion: "12.0.0", version: "17.2.3", ngImport: i0, type: ThyNavInkBarDirective, decorators: [{
            type: Directive,
            args: [{
                    selector: 'thy-nav-ink-bar, [thyNavInkBar]',
                    host: {
                        class: 'thy-nav-ink-bar',
                        '[class.thy-nav-ink-bar-animated]': 'animated'
                    },
                    standalone: true
                }]
        }], ctorParameters: () => [{ type: i0.ElementRef }, { type: i0.NgZone }, { type: undefined, decorators: [{
                    type: Optional
                }, {
                    type: Inject,
                    args: [ANIMATION_MODULE_TYPE]
                }] }], propDecorators: { isVertical: [{
                type: Input,
                args: [{ transform: booleanAttribute }]
            }], showInkBar: [{
                type: Input,
                args: [{ transform: booleanAttribute }]
            }] } });

/**
 * @private
 */
class BypassSecurityTrustHtmlPipe {
    constructor(sanitizer) {
        this.sanitizer = sanitizer;
    }
    transform(format) {
        return this.sanitizer.bypassSecurityTrustHtml(format);
    }
    static { this.ɵfac = i0.ɵɵngDeclareFactory({ minVersion: "12.0.0", version: "17.2.3", ngImport: i0, type: BypassSecurityTrustHtmlPipe, deps: [{ token: i1$1.DomSanitizer }], target: i0.ɵɵFactoryTarget.Pipe }); }
    static { this.ɵpipe = i0.ɵɵngDeclarePipe({ minVersion: "14.0.0", version: "17.2.3", ngImport: i0, type: BypassSecurityTrustHtmlPipe, isStandalone: true, name: "bypassSecurityTrustHtml" }); }
}
i0.ɵɵngDeclareClassMetadata({ minVersion: "12.0.0", version: "17.2.3", ngImport: i0, type: BypassSecurityTrustHtmlPipe, decorators: [{
            type: Pipe,
            args: [{
                    name: 'bypassSecurityTrustHtml',
                    standalone: true
                }]
        }], ctorParameters: () => [{ type: i1$1.DomSanitizer }] });

const navTypeClassesMap = {
    pulled: ['thy-nav-pulled'],
    tabs: ['thy-nav-tabs'],
    pills: ['thy-nav-pills'],
    lite: ['thy-nav-lite'],
    //如下类型已经废弃
    primary: ['thy-nav-primary'],
    secondary: ['thy-nav-secondary'],
    thirdly: ['thy-nav-thirdly'],
    'secondary-divider': ['thy-nav-secondary-divider']
};
const navSizeClassesMap = {
    lg: 'thy-nav-lg',
    md: 'thy-nav-md',
    sm: 'thy-nav-sm'
};
const tabItemRight = 20;
/**
 * 导航组件
 * @name thy-nav
 * @order 10
 */
class ThyNav {
    /**
     * 导航类型
     * @type pulled | tabs | pills | lite | primary | secondary | thirdly | secondary-divider
     * @default pulled
     */
    set thyType(type) {
        this.type = type || 'pulled';
        if (this.initialized) {
            this.updateClasses();
        }
    }
    /**
     * 导航大小
     * @type lg | md | sm
     * @default md
     */
    set thySize(size) {
        this.size = size;
        if (this.initialized) {
            this.updateClasses();
        }
    }
    /**
     * 水平排列
     * @type '' | 'start' | 'center' | 'end'
     * @default false
     */
    set thyHorizontal(horizontal) {
        this.horizontal = horizontal === 'right' ? 'end' : horizontal;
    }
    get showInkBar() {
        const showTypes = ['pulled', 'tabs'];
        return showTypes.includes(this.type);
    }
    updateClasses() {
        let classNames = [];
        if (navTypeClassesMap[this.type]) {
            classNames = [...navTypeClassesMap[this.type]];
        }
        if (navSizeClassesMap[this.size]) {
            classNames.push(navSizeClassesMap[this.size]);
        }
        this.hostRenderer.updateClass(classNames);
    }
    constructor(elementRef, ngZone, changeDetectorRef, popover) {
        this.elementRef = elementRef;
        this.ngZone = ngZone;
        this.changeDetectorRef = changeDetectorRef;
        this.popover = popover;
        this.destroyRef = inject(DestroyRef);
        this.type = 'pulled';
        this.size = 'md';
        this.initialized = false;
        this.wrapperOffset = {
            height: 0,
            width: 0,
            left: 0,
            top: 0
        };
        this.hiddenItems = [];
        this.showMore = true;
        this.moreBtnOffset = { height: 0, width: 0 };
        this.hostRenderer = useHostRenderer();
        /**
         * 是否是填充模式
         */
        this.thyFill = false;
        /**
         * 更多操作的菜单点击内部是否可关闭
         */
        this.thyInsideClosable = true;
        this.prevActiveIndex = NaN;
    }
    ngOnInit() {
        if (!this.thyResponsive) {
            this.initialized = true;
        }
        this.updateClasses();
    }
    ngAfterViewInit() {
        if (this.thyResponsive) {
            this.setMoreBtnOffset();
            this.ngZone.onStable.pipe(take(1)).subscribe(() => {
                this.links.toArray().forEach(link => link.setOffset());
                this.setHiddenItems();
            });
        }
        this.ngZone.runOutsideAngular(() => {
            merge(this.links.changes, this.createResizeObserver(this.elementRef.nativeElement).pipe(debounceTime(100)), ...this.links.map(item => this.createResizeObserver(item.elementRef.nativeElement).pipe(debounceTime(100))), ...(this.routers || []).map(router => router?.isActiveChange))
                .pipe(takeUntilDestroyed(this.destroyRef), tap(() => {
                if (this.thyResponsive) {
                    this.resetSizes();
                    this.setHiddenItems();
                    this.calculateMoreIsActive();
                }
            }))
                .subscribe(() => {
                this.alignInkBarToSelectedTab();
            });
        });
    }
    ngAfterContentInit() {
        if (this.thyResponsive) {
            this.ngZone.onStable.pipe(take(1)).subscribe(() => {
                this.resetSizes();
            });
        }
    }
    ngAfterContentChecked() {
        this.calculateMoreIsActive();
        this.curActiveIndex = this.links && this.links.length ? this.links.toArray().findIndex(item => item.linkIsActive()) : -1;
        if (this.curActiveIndex < 0) {
            this.inkBar.hide();
        }
        else if (this.curActiveIndex !== this.prevActiveIndex) {
            this.alignInkBarToSelectedTab();
        }
    }
    setMoreBtnOffset() {
        this.moreBtnOffset = {
            height: this.defaultMoreOperation?.nativeElement?.offsetHeight,
            width: this.defaultMoreOperation?.nativeElement?.offsetWidth
        };
    }
    createResizeObserver(element) {
        return typeof ResizeObserver === 'undefined'
            ? of(null)
            : new Observable(observer => {
                const resize = new ResizeObserver(entries => {
                    observer.next(entries);
                });
                resize.observe(element);
                return () => {
                    resize.disconnect();
                };
            });
    }
    calculateMoreIsActive() {
        this.moreActive = this.hiddenItems.some(item => {
            return item.linkIsActive();
        });
        this.changeDetectorRef.detectChanges();
    }
    setHiddenItems() {
        this.moreActive = false;
        const tabs = this.links.toArray();
        if (!tabs.length) {
            this.hiddenItems = [];
            this.showMore = this.hiddenItems.length > 0;
            return;
        }
        const endIndex = this.thyVertical ? this.getShowItemsEndIndexWhenVertical(tabs) : this.getShowItemsEndIndexWhenHorizontal(tabs);
        const showItems = tabs.slice(0, endIndex + 1);
        (showItems || []).forEach(item => {
            item.setNavLinkHidden(false);
        });
        this.hiddenItems = endIndex === tabs.length - 1 ? [] : tabs.slice(endIndex + 1);
        (this.hiddenItems || []).forEach(item => {
            item.setNavLinkHidden(true);
        });
        this.showMore = this.hiddenItems.length > 0;
        this.initialized = true;
    }
    getShowItemsEndIndexWhenHorizontal(tabs) {
        const tabsLength = tabs.length;
        let endIndex = tabsLength;
        let totalWidth = 0;
        for (let i = 0; i < tabsLength; i += 1) {
            const _totalWidth = i === tabsLength - 1 ? totalWidth + tabs[i].offset.width : totalWidth + tabs[i].offset.width + tabItemRight;
            if (_totalWidth > this.wrapperOffset.width) {
                let moreOperationWidth = this.moreBtnOffset.width;
                if (totalWidth + moreOperationWidth <= this.wrapperOffset.width) {
                    endIndex = i - 1;
                }
                else {
                    endIndex = i - 2;
                }
                break;
            }
            else {
                totalWidth = _totalWidth;
                endIndex = i;
            }
        }
        return endIndex;
    }
    getShowItemsEndIndexWhenVertical(tabs) {
        const tabsLength = tabs.length;
        let endIndex = tabsLength;
        let totalHeight = 0;
        for (let i = 0; i < tabsLength; i += 1) {
            const _totalHeight = totalHeight + tabs[i].offset.height;
            if (_totalHeight > this.wrapperOffset.height) {
                let moreOperationHeight = this.moreBtnOffset.height;
                if (totalHeight + moreOperationHeight <= this.wrapperOffset.height) {
                    endIndex = i - 1;
                }
                else {
                    endIndex = i - 2;
                }
                break;
            }
            else {
                totalHeight = _totalHeight;
                endIndex = i;
            }
        }
        return endIndex;
    }
    resetSizes() {
        this.wrapperOffset = {
            height: this.elementRef.nativeElement.offsetHeight || 0,
            width: this.elementRef.nativeElement.offsetWidth || 0,
            left: this.elementRef.nativeElement.offsetLeft || 0,
            top: this.elementRef.nativeElement.offsetTop || 0
        };
    }
    openMore(event, template) {
        this.popover.open(template, {
            origin: event.currentTarget,
            hasBackdrop: true,
            backdropClosable: true,
            insideClosable: this.thyInsideClosable,
            placement: 'bottom',
            panelClass: 'thy-nav-list-popover',
            originActiveClass: 'thy-nav-origin-active'
        });
    }
    navItemClick(item) {
        item.elementRef.nativeElement.click();
    }
    alignInkBarToSelectedTab() {
        if (!this.showInkBar) {
            this.inkBar.hide();
            return;
        }
        const tabs = this.links?.toArray() ?? [];
        const selectedItem = tabs.find(item => item.linkIsActive());
        let selectedItemElement = selectedItem && selectedItem.elementRef.nativeElement;
        if (selectedItem && this.moreActive) {
            selectedItemElement = this.defaultMoreOperation.nativeElement;
        }
        if (selectedItemElement) {
            this.prevActiveIndex = this.curActiveIndex;
            this.inkBar.alignToElement(selectedItemElement);
        }
    }
    ngOnChanges(changes) {
        const { thyVertical, thyType } = changes;
        if (thyType?.currentValue !== thyType?.previousValue || thyVertical?.currentValue !== thyVertical?.previousValue) {
            this.alignInkBarToSelectedTab();
        }
    }
    static { this.ɵfac = i0.ɵɵngDeclareFactory({ minVersion: "12.0.0", version: "17.2.3", ngImport: i0, type: ThyNav, deps: [{ token: i0.ElementRef }, { token: i0.NgZone }, { token: i0.ChangeDetectorRef }, { token: i1$2.ThyPopover }], target: i0.ɵɵFactoryTarget.Component }); }
    static { this.ɵcmp = i0.ɵɵngDeclareComponent({ minVersion: "16.1.0", version: "17.2.3", type: ThyNav, isStandalone: true, selector: "thy-nav", inputs: { thyType: "thyType", thySize: "thySize", thyHorizontal: "thyHorizontal", thyVertical: ["thyVertical", "thyVertical", booleanAttribute], thyFill: ["thyFill", "thyFill", booleanAttribute], thyResponsive: ["thyResponsive", "thyResponsive", booleanAttribute], thyInsideClosable: ["thyInsideClosable", "thyInsideClosable", booleanAttribute], thyExtra: "thyExtra" }, host: { properties: { "class.thy-nav--vertical": "this.thyVertical", "class.thy-nav--fill": "this.thyFill" }, classAttribute: "thy-nav" }, queries: [{ propertyName: "moreOperation", first: true, predicate: ["more"], descendants: true }, { propertyName: "morePopover", first: true, predicate: ["morePopover"], descendants: true }, { propertyName: "extra", first: true, predicate: ["extra"], descendants: true }, { propertyName: "links", predicate: ThyNavItemDirective, descendants: true }, { propertyName: "routers", predicate: RouterLinkActive, descendants: true }], viewQueries: [{ propertyName: "defaultMoreOperation", first: true, predicate: ["moreOperationContainer"], descendants: true }, { propertyName: "inkBar", first: true, predicate: ThyNavInkBarDirective, descendants: true, static: true }], usesOnChanges: true, ngImport: i0, template: "<div class=\"thy-nav-list\" [ngClass]=\"horizontal ? 'justify-content-' + horizontal : ''\" #navList>\n  <ng-container [ngTemplateOutlet]=\"content\"></ng-container>\n  <a\n    *ngIf=\"thyResponsive\"\n    href=\"javascript:;\"\n    class=\"thy-nav-more-container\"\n    [class.d-none]=\"!showMore\"\n    [class.invisible]=\"!initialized\"\n    #moreOperationContainer\n    thyNavLink\n    [thyNavLinkActive]=\"moreActive\"\n    (click)=\"openMore($event, navListPopover)\"\n  >\n    <ng-container *ngIf=\"moreOperation; else defaultMoreOperation\" [ngTemplateOutlet]=\"moreOperation\"></ng-container>\n    <ng-template #defaultMoreOperation>\n      \u66F4\u591A\n      <thy-icon thyIconName=\"angle-down\" class=\"thy-nav-more-icon\"></thy-icon>\n    </ng-template>\n  </a>\n  <thy-nav-ink-bar [showInkBar]=\"showInkBar\" [isVertical]=\"thyVertical\"></thy-nav-ink-bar>\n</div>\n\n<ng-template #navListPopover>\n  <ng-container\n    *ngIf=\"morePopover; else default\"\n    [ngTemplateOutlet]=\"morePopover\"\n    [ngTemplateOutletContext]=\"{ $implicit: hiddenItems }\"\n  ></ng-container>\n  <ng-template #default>\n    <thy-dropdown-menu thyImmediateRender>\n      <ng-container *ngFor=\"let item of hiddenItems\">\n        <span\n          class=\"thy-nav-item-more\"\n          thyDropdownMenuItem\n          [thyDropdownMenuItemActive]=\"item.isActive\"\n          (click)=\"navItemClick(item)\"\n          [innerHTML]=\"item.elementRef.nativeElement.innerHTML | bypassSecurityTrustHtml\"\n        ></span>\n      </ng-container>\n    </thy-dropdown-menu>\n  </ng-template>\n</ng-template>\n\n<ng-template #content>\n  <ng-content></ng-content>\n</ng-template>\n\n<div *ngIf=\"thyExtra || extra\" class=\"thy-nav-extra\">\n  <ng-container [ngTemplateOutlet]=\"thyExtra || extra\"></ng-container>\n</div>\n", dependencies: [{ kind: "directive", type: NgClass, selector: "[ngClass]", inputs: ["class", "ngClass"] }, { kind: "directive", type: NgTemplateOutlet, selector: "[ngTemplateOutlet]", inputs: ["ngTemplateOutletContext", "ngTemplateOutlet", "ngTemplateOutletInjector"] }, { kind: "directive", type: NgIf, selector: "[ngIf]", inputs: ["ngIf", "ngIfThen", "ngIfElse"] }, { kind: "directive", type: ThyNavItemDirective, selector: "[thyNavLink],[thyNavItem]", inputs: ["thyNavItemActive", "thyNavLinkActive", "thyNavItemDisabled"] }, { kind: "component", type: ThyIcon, selector: "thy-icon, [thy-icon]", inputs: ["thyIconType", "thyTwotoneColor", "thyIconName", "thyIconRotate", "thyIconSet", "thyIconLegging", "thyIconLinearGradient"] }, { kind: "directive", type: ThyNavInkBarDirective, selector: "thy-nav-ink-bar, [thyNavInkBar]", inputs: ["isVertical", "showInkBar"] }, { kind: "component", type: ThyDropdownMenuComponent, selector: "thy-dropdown-menu", inputs: ["thyWidth", "thyImmediateRender"] }, { kind: "directive", type: NgFor, selector: "[ngFor][ngForOf]", inputs: ["ngForOf", "ngForTrackBy", "ngForTemplate"] }, { kind: "directive", type: ThyDropdownMenuItemDirective, selector: "[thyDropdownMenuItem]", inputs: ["thyType", "thyDisabled"] }, { kind: "directive", type: ThyDropdownMenuItemActiveDirective, selector: "[thyDropdownMenuItemActive]", inputs: ["thyDropdownMenuItemActive"] }, { kind: "pipe", type: BypassSecurityTrustHtmlPipe, name: "bypassSecurityTrustHtml" }], changeDetection: i0.ChangeDetectionStrategy.OnPush }); }
}
i0.ɵɵngDeclareClassMetadata({ minVersion: "12.0.0", version: "17.2.3", ngImport: i0, type: ThyNav, decorators: [{
            type: Component,
            args: [{ selector: 'thy-nav', host: {
                        class: 'thy-nav'
                    }, changeDetection: ChangeDetectionStrategy.OnPush, standalone: true, imports: [
                        NgClass,
                        NgTemplateOutlet,
                        NgIf,
                        ThyNavItemDirective,
                        ThyIcon,
                        ThyNavInkBarDirective,
                        ThyDropdownMenuComponent,
                        NgFor,
                        ThyDropdownMenuItemDirective,
                        ThyDropdownMenuItemActiveDirective,
                        BypassSecurityTrustHtmlPipe
                    ], template: "<div class=\"thy-nav-list\" [ngClass]=\"horizontal ? 'justify-content-' + horizontal : ''\" #navList>\n  <ng-container [ngTemplateOutlet]=\"content\"></ng-container>\n  <a\n    *ngIf=\"thyResponsive\"\n    href=\"javascript:;\"\n    class=\"thy-nav-more-container\"\n    [class.d-none]=\"!showMore\"\n    [class.invisible]=\"!initialized\"\n    #moreOperationContainer\n    thyNavLink\n    [thyNavLinkActive]=\"moreActive\"\n    (click)=\"openMore($event, navListPopover)\"\n  >\n    <ng-container *ngIf=\"moreOperation; else defaultMoreOperation\" [ngTemplateOutlet]=\"moreOperation\"></ng-container>\n    <ng-template #defaultMoreOperation>\n      \u66F4\u591A\n      <thy-icon thyIconName=\"angle-down\" class=\"thy-nav-more-icon\"></thy-icon>\n    </ng-template>\n  </a>\n  <thy-nav-ink-bar [showInkBar]=\"showInkBar\" [isVertical]=\"thyVertical\"></thy-nav-ink-bar>\n</div>\n\n<ng-template #navListPopover>\n  <ng-container\n    *ngIf=\"morePopover; else default\"\n    [ngTemplateOutlet]=\"morePopover\"\n    [ngTemplateOutletContext]=\"{ $implicit: hiddenItems }\"\n  ></ng-container>\n  <ng-template #default>\n    <thy-dropdown-menu thyImmediateRender>\n      <ng-container *ngFor=\"let item of hiddenItems\">\n        <span\n          class=\"thy-nav-item-more\"\n          thyDropdownMenuItem\n          [thyDropdownMenuItemActive]=\"item.isActive\"\n          (click)=\"navItemClick(item)\"\n          [innerHTML]=\"item.elementRef.nativeElement.innerHTML | bypassSecurityTrustHtml\"\n        ></span>\n      </ng-container>\n    </thy-dropdown-menu>\n  </ng-template>\n</ng-template>\n\n<ng-template #content>\n  <ng-content></ng-content>\n</ng-template>\n\n<div *ngIf=\"thyExtra || extra\" class=\"thy-nav-extra\">\n  <ng-container [ngTemplateOutlet]=\"thyExtra || extra\"></ng-container>\n</div>\n" }]
        }], ctorParameters: () => [{ type: i0.ElementRef }, { type: i0.NgZone }, { type: i0.ChangeDetectorRef }, { type: i1$2.ThyPopover }], propDecorators: { thyType: [{
                type: Input
            }], thySize: [{
                type: Input
            }], thyHorizontal: [{
                type: Input
            }], thyVertical: [{
                type: HostBinding,
                args: ['class.thy-nav--vertical']
            }, {
                type: Input,
                args: [{ transform: booleanAttribute }]
            }], thyFill: [{
                type: HostBinding,
                args: ['class.thy-nav--fill']
            }, {
                type: Input,
                args: [{ transform: booleanAttribute }]
            }], thyResponsive: [{
                type: Input,
                args: [{ transform: booleanAttribute }]
            }], thyInsideClosable: [{
                type: Input,
                args: [{ transform: booleanAttribute }]
            }], thyExtra: [{
                type: Input
            }], links: [{
                type: ContentChildren,
                args: [ThyNavItemDirective, { descendants: true }]
            }], routers: [{
                type: ContentChildren,
                args: [RouterLinkActive, { descendants: true }]
            }], moreOperation: [{
                type: ContentChild,
                args: ['more']
            }], morePopover: [{
                type: ContentChild,
                args: ['morePopover']
            }], extra: [{
                type: ContentChild,
                args: ['extra']
            }], defaultMoreOperation: [{
                type: ViewChild,
                args: ['moreOperationContainer']
            }], inkBar: [{
                type: ViewChild,
                args: [ThyNavInkBarDirective, { static: true }]
            }] } });

class ThyNavModule {
    static { this.ɵfac = i0.ɵɵngDeclareFactory({ minVersion: "12.0.0", version: "17.2.3", ngImport: i0, type: ThyNavModule, deps: [], target: i0.ɵɵFactoryTarget.NgModule }); }
    static { this.ɵmod = i0.ɵɵngDeclareNgModule({ minVersion: "14.0.0", version: "17.2.3", ngImport: i0, type: ThyNavModule, imports: [CommonModule,
            ThyIconModule,
            ThyPopoverModule,
            ThyDropdownModule,
            RouterModule,
            ThyNav,
            ThyNavItemDirective,
            ThyIconNav,
            ThyIconNavLink,
            BypassSecurityTrustHtmlPipe,
            ThyNavInkBarDirective], exports: [ThyNav, ThyNavItemDirective, ThyIconNav, ThyIconNavLink, BypassSecurityTrustHtmlPipe] }); }
    static { this.ɵinj = i0.ɵɵngDeclareInjector({ minVersion: "12.0.0", version: "17.2.3", ngImport: i0, type: ThyNavModule, imports: [CommonModule,
            ThyIconModule,
            ThyPopoverModule,
            ThyDropdownModule,
            RouterModule,
            ThyNav,
            ThyIconNavLink] }); }
}
i0.ɵɵngDeclareClassMetadata({ minVersion: "12.0.0", version: "17.2.3", ngImport: i0, type: ThyNavModule, decorators: [{
            type: NgModule,
            args: [{
                    imports: [
                        CommonModule,
                        ThyIconModule,
                        ThyPopoverModule,
                        ThyDropdownModule,
                        RouterModule,
                        ThyNav,
                        ThyNavItemDirective,
                        ThyIconNav,
                        ThyIconNavLink,
                        BypassSecurityTrustHtmlPipe,
                        ThyNavInkBarDirective
                    ],
                    exports: [ThyNav, ThyNavItemDirective, ThyIconNav, ThyIconNavLink, BypassSecurityTrustHtmlPipe]
                }]
        }] });

/**
 * Generated bundle index. Do not edit.
 */

export { BypassSecurityTrustHtmlPipe, ThyIconNav, ThyIconNavLink, ThyNav, ThyNavItemDirective, ThyNavModule };
//# sourceMappingURL=ngx-tethys-nav.mjs.map
