import { isThemeColor } from 'ngx-tethys/core';
import * as i0 from '@angular/core';
import { Component, ViewEncapsulation, ChangeDetectionStrategy, Input, NgModule } from '@angular/core';
import { CommonModule } from '@angular/common';

const COMPONENT_CLASS_NAME = 'thy-dot';
const DEFAULT_COLOR_NAME = 'primary';
const DEFAULT_SIZE_NAME = 'sm';
const DEFAULT_THEME_NAME = 'fill';
const DEFAULT_SHAPE_NAME = 'circle';
/**
 * 显示一个点的组件
 * @name thy-dot,[thy-dot],[thyDot]
 * @order 10
 */
class ThyDot {
    constructor(el, renderer) {
        this.el = el;
        this.renderer = renderer;
        this.size = DEFAULT_SIZE_NAME;
        this.theme = DEFAULT_THEME_NAME;
        this.shape = DEFAULT_SHAPE_NAME;
        this.color = DEFAULT_COLOR_NAME;
        this.nativeElement = this.el.nativeElement;
        this.updateColorStyle();
    }
    /**
     * 颜色，可选值为：`primary` `success` `info` `warning` `danger` `default` `light`和自定义颜色，如`#2cccda` `red`  `rgb(153, 153, 153)`
     * @type ThyThemeColor | string
     * @default primary
     */
    set thyColor(value) {
        if (value) {
            this.color = value;
            this.updateColorStyle();
        }
    }
    /**
     * 大小
     * @type xs | sm | md | lg | xlg
     * @default sm
     */
    set thySize(value) {
        if (value) {
            this.size = value;
        }
    }
    /**
     * 主题
     * @type outline(线框) | fill(填充)
     * @default fill
     */
    set thyTheme(value) {
        if (value) {
            this.theme = value;
        }
    }
    /**
     * 形状
     * @type circle(圆形) | square(方形)
     * @default circle
     */
    set thyShape(value) {
        if (value) {
            this.shape = value;
        }
    }
    updateColorStyle() {
        Array.from(this.nativeElement.classList)
            .filter(it => /^dot-color-[\w]+$/.test(it))
            .forEach(it => this.renderer.removeClass(this.nativeElement, it));
        if (isThemeColor(this.color)) {
            this.renderer.setStyle(this.nativeElement, 'borderColor', 'none');
            this.renderer.addClass(this.nativeElement, `dot-color-${this.color}`);
        }
        else {
            this.renderer.setStyle(this.nativeElement, 'borderColor', this.color);
        }
    }
    static { this.ɵfac = i0.ɵɵngDeclareFactory({ minVersion: "12.0.0", version: "17.2.3", ngImport: i0, type: ThyDot, deps: [{ token: i0.ElementRef }, { token: i0.Renderer2 }], target: i0.ɵɵFactoryTarget.Component }); }
    static { this.ɵcmp = i0.ɵɵngDeclareComponent({ minVersion: "14.0.0", version: "17.2.3", type: ThyDot, isStandalone: true, selector: "thy-dot,[thy-dot],[thyDot]", inputs: { thyColor: "thyColor", thySize: "thySize", thyTheme: "thyTheme", thyShape: "thyShape" }, host: { properties: { "class.dot-theme-fill": "theme === \"fill\"", "class.dot-theme-outline": "theme === \"outline\"", "class.dot-shape-square": "shape === \"square\"", "class.dot-shape-circle": "shape === \"circle\"", "class.dot-size-md": "size === \"md\"", "class.dot-size-sm": "size === \"sm\"", "class.dot-size-xs": "size === \"xs\"", "class.dot-size-lg": "size === \"lg\"", "class.dot-size-xlg": "size === \"xlg\"" }, classAttribute: "thy-dot" }, ngImport: i0, template: '', isInline: true, changeDetection: i0.ChangeDetectionStrategy.OnPush, encapsulation: i0.ViewEncapsulation.None }); }
}
i0.ɵɵngDeclareClassMetadata({ minVersion: "12.0.0", version: "17.2.3", ngImport: i0, type: ThyDot, decorators: [{
            type: Component,
            args: [{
                    selector: 'thy-dot,[thy-dot],[thyDot]',
                    template: '',
                    encapsulation: ViewEncapsulation.None,
                    changeDetection: ChangeDetectionStrategy.OnPush,
                    host: {
                        class: 'thy-dot',
                        '[class.dot-theme-fill]': 'theme === "fill"',
                        '[class.dot-theme-outline]': 'theme === "outline"',
                        '[class.dot-shape-square]': 'shape === "square"',
                        '[class.dot-shape-circle]': 'shape === "circle"',
                        '[class.dot-size-md]': 'size === "md"',
                        '[class.dot-size-sm]': 'size === "sm"',
                        '[class.dot-size-xs]': 'size === "xs"',
                        '[class.dot-size-lg]': 'size === "lg"',
                        '[class.dot-size-xlg]': 'size === "xlg"'
                    },
                    standalone: true
                }]
        }], ctorParameters: () => [{ type: i0.ElementRef }, { type: i0.Renderer2 }], propDecorators: { thyColor: [{
                type: Input
            }], thySize: [{
                type: Input
            }], thyTheme: [{
                type: Input
            }], thyShape: [{
                type: Input
            }] } });

class ThyDotModule {
    static { this.ɵfac = i0.ɵɵngDeclareFactory({ minVersion: "12.0.0", version: "17.2.3", ngImport: i0, type: ThyDotModule, deps: [], target: i0.ɵɵFactoryTarget.NgModule }); }
    static { this.ɵmod = i0.ɵɵngDeclareNgModule({ minVersion: "14.0.0", version: "17.2.3", ngImport: i0, type: ThyDotModule, imports: [CommonModule, ThyDot], exports: [ThyDot] }); }
    static { this.ɵinj = i0.ɵɵngDeclareInjector({ minVersion: "12.0.0", version: "17.2.3", ngImport: i0, type: ThyDotModule, imports: [CommonModule] }); }
}
i0.ɵɵngDeclareClassMetadata({ minVersion: "12.0.0", version: "17.2.3", ngImport: i0, type: ThyDotModule, decorators: [{
            type: NgModule,
            args: [{
                    imports: [CommonModule, ThyDot],
                    exports: [ThyDot]
                }]
        }] });

/**
 * Generated bundle index. Do not edit.
 */

export { COMPONENT_CLASS_NAME, DEFAULT_COLOR_NAME, DEFAULT_SHAPE_NAME, DEFAULT_SIZE_NAME, DEFAULT_THEME_NAME, ThyDot, ThyDotModule };
//# sourceMappingURL=ngx-tethys-dot.mjs.map
