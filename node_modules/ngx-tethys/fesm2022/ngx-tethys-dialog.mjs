import * as i1 from '@angular/cdk/overlay';
import { OverlayModule } from '@angular/cdk/overlay';
import { PortalModule, ComponentPortal } from '@angular/cdk/portal';
import { DOCUMENT, NgIf, NgTemplateOutlet, NgClass, CommonModule } from '@angular/common';
import * as i0 from '@angular/core';
import { InjectionToken, EventEmitter, Component, ChangeDetectionStrategy, Inject, ViewChild, HostBinding, Injector, Injectable, Optional, booleanAttribute, Input, ContentChild, Output, NgModule } from '@angular/core';
import * as i2$2 from '@angular/forms';
import { FormsModule } from '@angular/forms';
import { ThyAction, ThyActionModule } from 'ngx-tethys/action';
import { ThyButton, ThyButtonModule } from 'ngx-tethys/button';
import { ThyFormDirective, ThyFormGroupFooter, ThyFormModule } from 'ngx-tethys/form';
import { ThyIcon, ThyIconModule } from 'ngx-tethys/icon';
import { ThySharedModule } from 'ngx-tethys/shared';
import * as i2 from 'ngx-tethys/core';
import { ThyAbstractOverlayConfig, ThyAbstractOverlayContainer, reqAnimFrame, ThyPortalOutlet, ThyAbstractOverlayRef, ThyAbstractInternalOverlayRef, ThyAbstractOverlayService } from 'ngx-tethys/core';
import { of } from 'rxjs';
import { Directionality } from '@angular/cdk/bidi';
import { filter, finalize } from 'rxjs/operators';
import * as i3 from '@angular/cdk/a11y';
import { state, style, transition, animate, trigger } from '@angular/animations';
import * as i2$1 from '@angular/cdk/scrolling';
import { CdkScrollable } from '@angular/cdk/scrolling';
import { useHostRenderer } from '@tethys/cdk/dom';

const THY_CONFIRM_COMPONENT_TOKEN = new InjectionToken('thy-confirm-component-token');

const animationBody = [
    // Note: The `enter` animation transitions to `transform: none`, because for some reason
    // specifying the transform explicitly, causes IE both to blur the dialog content and
    // decimate the animation performance. Leaving it as `none` solves both issues. translate(-300px,-100px)
    state('void, exit', style({ opacity: 0, transform: 'scale(0.1)' })),
    state('enter', style({ transform: 'none' })),
    transition('* => enter', animate('200ms cubic-bezier(0, 0, 0.57, 1)', style({ transform: 'none', opacity: 1 }))),
    transition('* => exit', animate('150ms cubic-bezier(0.24, 0.18, 0.96, 0.52)', style({ transform: 'scale(0.3)', opacity: 0 })))
];
/**
 * Animations used by MatDialog.
 * @docs-private
 */
const thyDialogAnimations = {
    dialogContainer: trigger('dialogContainer', animationBody)
};
// export const setAnimationStart

var ThyDialogSizes;
(function (ThyDialogSizes) {
    ThyDialogSizes["lg"] = "lg";
    /**
     * 已废弃，命名错误，请使用 superLg
     * @deprecated
     */
    ThyDialogSizes["supperLg"] = "supper-lg";
    ThyDialogSizes["superLg"] = "super-lg";
    ThyDialogSizes["maxLg"] = "max-lg";
    ThyDialogSizes["md"] = "md";
    ThyDialogSizes["sm"] = "sm";
    ThyDialogSizes["full"] = "full";
})(ThyDialogSizes || (ThyDialogSizes = {}));
/**
 * 通过ThyDialog服务端打开模态框的配置
 * @description.en-us Configuration for opening a modal dialog with the ThyDialog service.
 * @public
 * @order 20
 */
class ThyDialogConfig extends ThyAbstractOverlayConfig {
    constructor() {
        super(...arguments);
        /**
         * 对话框元素的 ARIA 角色
         * @description.en-us The ARIA role of the dialog element.
         */
        this.role = 'dialog';
    }
}
/** Injection token that can be used to specify default dialog options. */
const THY_DIALOG_DEFAULT_OPTIONS = new InjectionToken('thy-dialog-default-options');
const THY_DIALOG_DEFAULT_OPTIONS_PROVIDER = {
    provide: THY_DIALOG_DEFAULT_OPTIONS,
    useValue: {
        role: 'dialog',
        hasBackdrop: true,
        backdropClass: '',
        panelClass: '',
        backdropClosable: true,
        closeOnNavigation: true,
        autoFocus: true,
        restoreFocus: true,
        restoreFocusOptions: {
            preventScroll: true
        },
        hostClass: 'thy-dialog-content'
    }
};
class ThyDialogLayoutConfig {
}
const THY_DIALOG_LAYOUT_CONFIG = new InjectionToken('thy-dialog-layout-config');
const THY_DIALOG_LAYOUT_CONFIG_PROVIDER = {
    provide: THY_DIALOG_LAYOUT_CONFIG,
    useValue: {
        footerAlign: 'left',
        footerDivided: false
    }
};

const dialogAbstractOverlayOptions = {
    name: 'dialog',
    animationEnabled: true,
    disposeWhenClose: true
};

/**
 * @private
 */
class ThyDialogContainer extends ThyAbstractOverlayContainer {
    savePreviouslyFocusedElement() {
        if (this.document) {
            this.elementFocusedBeforeDialogWasOpened = this.document.activeElement;
            // Note that there is no focus method when rendering on the server.
            if (this.elementRef.nativeElement.focus) {
                // Note: this is being run outside of the Angular zone because `element.focus()` doesn't require
                // running change detection.
                this.ngZone.runOutsideAngular(() => 
                // Move focus onto the dialog immediately in order to prevent the user from accidentally
                // opening multiple dialogs at the same time. Needs to be async, because the element
                // may not be focusable immediately.
                // Note: `element.focus()` causes re-layout and this may lead to frame drop on slower devices.
                // https://gist.github.com/paulirish/5d52fb081b3570c81e3a#setting-focus
                // `setTimeout` is a macrotask and macrotasks are executed within the current rendering frame.
                // Animation tasks are executed within the next rendering frame.
                reqAnimFrame(() => this.elementRef.nativeElement.focus()));
            }
        }
    }
    /** Moves the focus inside the focus trap. */
    trapFocus() {
        const element = this.elementRef.nativeElement;
        if (!this.focusTrap) {
            this.focusTrap = this.focusTrapFactory.create(element);
        }
        // If we were to attempt to focus immediately, then the content of the dialog would not yet be
        // ready in instances where change detection has to run first. To deal with this, we simply
        // wait for the microtask queue to be empty.
        if (this.config.autoFocus) {
            this.focusTrap.focusInitialElementWhenReady();
        }
        else {
            const activeElement = this.document.activeElement;
            // Otherwise ensure that focus is on the dialog container. It's possible that a different
            // component tried to move focus while the open animation was running. See:
            // https://github.com/angular/components/issues/16215. Note that we only want to do this
            // if the focus isn't inside the dialog already, because it's possible that the consumer
            // turned off `autoFocus` in order to move focus themselves.
            if (activeElement !== element && !element.contains(activeElement)) {
                element.focus();
            }
        }
    }
    restoreFocus() {
        const toFocus = this.elementFocusedBeforeDialogWasOpened;
        // We need the extra check, because IE can set the `activeElement` to null in some cases.
        if (this.config.restoreFocus && toFocus && typeof toFocus.focus === 'function') {
            toFocus.focus(this.config.restoreFocusOptions);
        }
        if (this.focusTrap) {
            this.focusTrap.destroy();
        }
    }
    setTransformOrigin() {
        this.clickPositioner.runTaskUseLastPosition(lastPosition => {
            if (lastPosition) {
                const containerElement = this.elementRef.nativeElement;
                const transformOrigin = `${lastPosition.x - containerElement.offsetLeft}px ${lastPosition.y - containerElement.offsetTop}px 0px`;
                containerElement.style['transform-origin'] = transformOrigin;
                // 手动修改动画状态为从 void 到 enter, 开启动画
            }
            this.animationState = 'enter';
            this.changeDetectorRef.markForCheck();
        });
    }
    constructor(elementRef, document, config, changeDetectorRef, clickPositioner, focusTrapFactory, ngZone, renderer) {
        super(dialogAbstractOverlayOptions, changeDetectorRef);
        this.elementRef = elementRef;
        this.document = document;
        this.config = config;
        this.clickPositioner = clickPositioner;
        this.focusTrapFactory = focusTrapFactory;
        this.ngZone = ngZone;
        this.renderer = renderer;
        /** State of the dialog animation. */
        this.animationState = 'void';
        /** Emits when an animation state changes. */
        this.animationStateChanged = new EventEmitter();
        /** ID of the element that should be considered as the dialog's label. */
        this.ariaLabelledBy = null;
        /** Element that was focused before the dialog was opened. Save this to restore upon close. */
        this.elementFocusedBeforeDialogWasOpened = null;
        this.animationOpeningDone = this.animationStateChanged.pipe(filter((event) => {
            return event.phaseName === 'done' && event.toState === 'void';
        }));
        this.animationClosingDone = this.animationStateChanged.pipe(filter((event) => {
            return event.phaseName === 'done' && event.toState === 'exit';
        }));
        /* Prohibit operations on elements inside the container during animation execution */
        this.animationStateChanged
            .pipe(filter((event) => {
            return event.phaseName === 'start' && event.toState === 'exit';
        }))
            .subscribe(() => {
            this.renderer.setStyle(this.elementRef.nativeElement, 'pointer-events', 'none');
        });
    }
    beforeAttachPortal() {
        this.setTransformOrigin();
        this.savePreviouslyFocusedElement();
    }
    /** Callback, invoked whenever an animation on the host completes. */
    onAnimationDone(event) {
        if (event.toState === 'void') {
            this.trapFocus();
        }
        else if (event.toState === 'exit') {
            this.restoreFocus();
        }
        this.animationStateChanged.emit(event);
    }
    /** Callback, invoked when an animation on the host starts. */
    onAnimationStart(event) {
        this.animationStateChanged.emit(event);
    }
    ngOnDestroy() {
        super.destroy();
    }
    static { this.ɵfac = i0.ɵɵngDeclareFactory({ minVersion: "12.0.0", version: "17.2.3", ngImport: i0, type: ThyDialogContainer, deps: [{ token: i0.ElementRef }, { token: DOCUMENT }, { token: ThyDialogConfig }, { token: i0.ChangeDetectorRef }, { token: i2.ThyClickPositioner }, { token: i3.FocusTrapFactory }, { token: i0.NgZone }, { token: i0.Renderer2 }], target: i0.ɵɵFactoryTarget.Component }); }
    static { this.ɵcmp = i0.ɵɵngDeclareComponent({ minVersion: "14.0.0", version: "17.2.3", type: ThyDialogContainer, isStandalone: true, selector: "thy-dialog-container", host: { attributes: { "tabindex": "-1", "aria-modal": "true" }, listeners: { "@dialogContainer.start": "onAnimationStart($event)", "@dialogContainer.done": "onAnimationDone($event)" }, properties: { "attr.id": "this.id", "attr.role": "config.role", "attr.aria-labelledby": "config.ariaLabel ? null : ariaLabelledBy", "attr.aria-label": "config.ariaLabel", "attr.aria-describedby": "config.ariaDescribedBy || null", "@dialogContainer": "animationState" }, classAttribute: "thy-dialog-container" }, viewQueries: [{ propertyName: "portalOutlet", first: true, predicate: ThyPortalOutlet, descendants: true, static: true }], usesInheritance: true, ngImport: i0, template: ` <ng-template thyPortalOutlet></ng-template> `, isInline: true, dependencies: [{ kind: "ngmodule", type: PortalModule }, { kind: "directive", type: ThyPortalOutlet, selector: "[thyPortalOutlet]", inputs: ["thyPortalOutlet"], exportAs: ["thyPortalOutlet"] }], animations: [thyDialogAnimations.dialogContainer], changeDetection: i0.ChangeDetectionStrategy.Default }); }
}
i0.ɵɵngDeclareClassMetadata({ minVersion: "12.0.0", version: "17.2.3", ngImport: i0, type: ThyDialogContainer, decorators: [{
            type: Component,
            args: [{
                    selector: 'thy-dialog-container',
                    template: ` <ng-template thyPortalOutlet></ng-template> `,
                    // Using OnPush for dialogs caused some sync issues, e.g. custom ngModel can't to detect changes
                    // Disabled until we can track them down.
                    changeDetection: ChangeDetectionStrategy.Default,
                    animations: [thyDialogAnimations.dialogContainer],
                    host: {
                        class: 'thy-dialog-container',
                        tabindex: '-1',
                        'aria-modal': 'true',
                        '[attr.id]': 'id',
                        '[attr.role]': 'config.role',
                        '[attr.aria-labelledby]': 'config.ariaLabel ? null : ariaLabelledBy',
                        '[attr.aria-label]': 'config.ariaLabel',
                        '[attr.aria-describedby]': 'config.ariaDescribedBy || null',
                        '[@dialogContainer]': 'animationState',
                        '(@dialogContainer.start)': 'onAnimationStart($event)',
                        '(@dialogContainer.done)': 'onAnimationDone($event)'
                    },
                    standalone: true,
                    imports: [PortalModule, ThyPortalOutlet]
                }]
        }], ctorParameters: () => [{ type: i0.ElementRef }, { type: undefined, decorators: [{
                    type: Inject,
                    args: [DOCUMENT]
                }] }, { type: ThyDialogConfig }, { type: i0.ChangeDetectorRef }, { type: i2.ThyClickPositioner }, { type: i3.FocusTrapFactory }, { type: i0.NgZone }, { type: i0.Renderer2 }], propDecorators: { portalOutlet: [{
                type: ViewChild,
                args: [ThyPortalOutlet, { static: true }]
            }], id: [{
                type: HostBinding,
                args: [`attr.id`]
            }] } });

/**
 * @publicApi
 * @order 30
 */
class ThyDialogRef extends ThyAbstractOverlayRef {
}
class ThyInternalDialogRef extends ThyAbstractInternalOverlayRef {
    constructor(overlayRef, containerInstance, config) {
        super(dialogAbstractOverlayOptions, overlayRef, containerInstance, config);
    }
    /**
     * Updates the dialog's position.
     * @param position New dialog position.
     */
    updatePosition(position) {
        this.updateGlobalPosition(position);
        return this;
    }
    /**
     * Updates the dialog's width and height.
     * @param width New width of the dialog.
     * @param height New height of the dialog.
     */
    updateSizeAndPosition(width = '', height = '', position) {
        this.getPositionStrategy().width(width).height(height);
        this.updatePosition(position);
        return this;
    }
}

/**
 * @public
 * @order 10
 */
class ThyDialog extends ThyAbstractOverlayService {
    buildOverlayConfig(config) {
        const size = config.size || ThyDialogSizes.md;
        const overlayConfig = this.buildBaseOverlayConfig(config, [`dialog-${size}`]);
        overlayConfig.positionStrategy = this.overlay.position().global();
        overlayConfig.scrollStrategy = config.scrollStrategy || this.overlay.scrollStrategies.block();
        return overlayConfig;
    }
    attachOverlayContainer(overlay, config) {
        const userInjector = config && config.viewContainerRef && config.viewContainerRef.injector;
        const injector = Injector.create({
            parent: userInjector || this.injector,
            providers: [{ provide: ThyDialogConfig, useValue: config }]
        });
        const containerPortal = new ComponentPortal(ThyDialogContainer, config.viewContainerRef, injector);
        const containerRef = overlay.attach(containerPortal);
        return containerRef.instance;
    }
    createAbstractOverlayRef(overlayRef, containerInstance, config) {
        return new ThyInternalDialogRef(overlayRef, containerInstance, config);
    }
    createInjector(config, dialogRef, dialogContainer) {
        const userInjector = config && config.viewContainerRef && config.viewContainerRef.injector;
        const injectionTokens = [
            { provide: ThyDialogContainer, useValue: dialogContainer },
            {
                provide: ThyDialogRef,
                useValue: dialogRef
            }
        ];
        if (config.direction && (!userInjector || !userInjector.get(Directionality, null))) {
            injectionTokens.push({
                provide: Directionality,
                useValue: {
                    value: config.direction,
                    change: of()
                }
            });
        }
        return Injector.create({ parent: userInjector || this.injector, providers: injectionTokens });
    }
    constructor(overlay, injector, defaultConfig, clickPositioner, confirmComponentType) {
        super(dialogAbstractOverlayOptions, overlay, injector, defaultConfig);
        this.confirmComponentType = confirmComponentType;
        clickPositioner.initialize();
    }
    /**
     * 打开 Dialog
     */
    open(componentOrTemplateRef, config) {
        const dialogRef = this.openOverlay(componentOrTemplateRef, config);
        const dialogRefInternal = dialogRef;
        dialogRefInternal.updateSizeAndPosition(dialogRef.containerInstance.config.width, dialogRef.containerInstance.config.height, dialogRef.containerInstance.config.position);
        return dialogRef;
    }
    /**
     * 打开 Confirm
     */
    confirm(options) {
        return this.open(this.confirmComponentType, {
            initialState: {
                options: options
            }
        });
    }
    /**
     * 根据 id 获取 Dialog
     */
    getDialogById(id) {
        return this.getAbstractOverlayById(id);
    }
    /**
     * 获取所有打开的 Dialog
     */
    getOpenedDialogs() {
        return this.getAbstractOverlays();
    }
    /**
     * @description.en-us Finds the closest ThyDialogRef to an element by looking at the DOM.
     * @description 获取与指定元素最接近的 ThyDialogRef
     */
    getClosestDialog(element) {
        let parent = element.parentElement;
        while (parent && !parent.classList.contains('thy-dialog-container')) {
            parent = parent.parentElement;
        }
        if (parent && parent.id) {
            return this.getDialogById(parent.id);
        }
        return null;
    }
    ngOnDestroy() {
        this.dispose();
    }
    static { this.ɵfac = i0.ɵɵngDeclareFactory({ minVersion: "12.0.0", version: "17.2.3", ngImport: i0, type: ThyDialog, deps: [{ token: i1.Overlay }, { token: i0.Injector }, { token: THY_DIALOG_DEFAULT_OPTIONS, optional: true }, { token: i2.ThyClickPositioner }, { token: THY_CONFIRM_COMPONENT_TOKEN }], target: i0.ɵɵFactoryTarget.Injectable }); }
    static { this.ɵprov = i0.ɵɵngDeclareInjectable({ minVersion: "12.0.0", version: "17.2.3", ngImport: i0, type: ThyDialog }); }
}
i0.ɵɵngDeclareClassMetadata({ minVersion: "12.0.0", version: "17.2.3", ngImport: i0, type: ThyDialog, decorators: [{
            type: Injectable
        }], ctorParameters: () => [{ type: i1.Overlay }, { type: i0.Injector }, { type: ThyDialogConfig, decorators: [{
                    type: Optional
                }, {
                    type: Inject,
                    args: [THY_DIALOG_DEFAULT_OPTIONS]
                }] }, { type: i2.ThyClickPositioner }, { type: undefined, decorators: [{
                    type: Inject,
                    args: [THY_CONFIRM_COMPONENT_TOKEN]
                }] }] });

/**
 * 模态框的主体组件
 * @name thy-dialog-body
 * @order 50
 */
class ThyDialogBody {
    /**
     * 清除间距
     * @default false
     */
    set thyClearPadding(value) {
        this.thyClearPaddingClassName = value;
    }
    constructor(dialog) {
        this.dialog = dialog;
        this._isDialogBody = true;
        this.thyClearPaddingClassName = false;
    }
    ngOnInit() { }
    static { this.ɵfac = i0.ɵɵngDeclareFactory({ minVersion: "12.0.0", version: "17.2.3", ngImport: i0, type: ThyDialogBody, deps: [{ token: ThyDialog }], target: i0.ɵɵFactoryTarget.Component }); }
    static { this.ɵcmp = i0.ɵɵngDeclareComponent({ minVersion: "16.1.0", version: "17.2.3", type: ThyDialogBody, isStandalone: true, selector: "thy-dialog-body", inputs: { thyClearPadding: ["thyClearPadding", "thyClearPadding", booleanAttribute] }, host: { properties: { "class.dialog-body": "this._isDialogBody", "class.dialog-body-clear-padding": "this.thyClearPaddingClassName" } }, exportAs: ["thyDialogBody"], hostDirectives: [{ directive: i2$1.CdkScrollable }], ngImport: i0, template: '<ng-content></ng-content>', isInline: true }); }
}
i0.ɵɵngDeclareClassMetadata({ minVersion: "12.0.0", version: "17.2.3", ngImport: i0, type: ThyDialogBody, decorators: [{
            type: Component,
            args: [{
                    selector: 'thy-dialog-body',
                    template: '<ng-content></ng-content>',
                    // changeDetection: ChangeDetectionStrategy.OnPush,
                    exportAs: 'thyDialogBody',
                    standalone: true,
                    hostDirectives: [CdkScrollable]
                }]
        }], ctorParameters: () => [{ type: ThyDialog }], propDecorators: { _isDialogBody: [{
                type: HostBinding,
                args: [`class.dialog-body`]
            }], thyClearPaddingClassName: [{
                type: HostBinding,
                args: [`class.dialog-body-clear-padding`]
            }], thyClearPadding: [{
                type: Input,
                args: [{ transform: booleanAttribute }]
            }] } });

const THY_CONFIRM_DEFAULT_OPTIONS = new InjectionToken('thy-confirm-default-options');
const THY_CONFIRM_DEFAULT_OPTIONS_VALUE = {
    title: '确认删除',
    okText: '确定',
    okType: 'danger',
    cancelText: '取消',
    footerAlign: 'left'
};
const THY_CONFIRM_DEFAULT_OPTIONS_PROVIDER = {
    provide: THY_CONFIRM_DEFAULT_OPTIONS,
    useValue: THY_CONFIRM_DEFAULT_OPTIONS_VALUE
};

/**
 * 模态框头部组件
 * @name thy-dialog-header
 * @order 40
 */
class ThyDialogHeader {
    /**
     * 标题的多语言 Key
     */
    set thyTitleTranslationKey(key) {
        if (key && !this.thyTitle) {
            this.thyTitle = this.translate.instant(key);
        }
    }
    constructor(elementRef, dialog, translate, dialogContainer) {
        this.elementRef = elementRef;
        this.dialog = dialog;
        this.translate = translate;
        this.dialogContainer = dialogContainer;
        /**
         * 关闭事件
         */
        this.thyOnClose = new EventEmitter();
    }
    ngOnInit() {
        if (!this.dialogContainer) {
            // When this directive is included in a dialog via TemplateRef (rather than being
            // in a Component), the ThyDialogContainerComponent isn't available via injection because embedded
            // views cannot be given a custom injector. Instead, we look up the ThyDialogContainerComponent by
            // ID. This must occur in `onInit`, as the ID binding for the dialog container won't
            // be resolved at constructor time.
            const dialogRef = this.dialog.getClosestDialog(this.elementRef.nativeElement);
            this.dialogContainer = dialogRef ? dialogRef.containerInstance : null;
        }
        // change in next microtask avoid throw ExpressionChangedAfterItHasBeenCheckedError
        // because sub component change parent's HostBinding property (ariaLabelledBy)
        Promise.resolve().then(() => {
            if (this.dialogContainer) {
                this.dialogContainer.ariaLabelledBy = this.thyTitle;
            }
        });
    }
    close(event) {
        if (this.thyOnClose.observers.length > 0) {
            this.thyOnClose.emit(event);
        }
        else {
            this.dialog.close();
        }
    }
    static { this.ɵfac = i0.ɵɵngDeclareFactory({ minVersion: "12.0.0", version: "17.2.3", ngImport: i0, type: ThyDialogHeader, deps: [{ token: i0.ElementRef }, { token: ThyDialog }, { token: i2.ThyTranslate }, { token: ThyDialogContainer, optional: true }], target: i0.ɵɵFactoryTarget.Component }); }
    static { this.ɵcmp = i0.ɵɵngDeclareComponent({ minVersion: "16.1.0", version: "17.2.3", type: ThyDialogHeader, isStandalone: true, selector: "thy-dialog-header", inputs: { thyTitle: "thyTitle", thySize: "thySize", thyDivided: ["thyDivided", "thyDivided", booleanAttribute], thyTitleTranslationKey: "thyTitleTranslationKey", thyIcon: "thyIcon" }, outputs: { thyOnClose: "thyOnClose" }, host: { properties: { "class.thy-dialog-header-lg": "thySize === 'lg'", "class.thy-dialog-header-divided": "thyDivided" }, classAttribute: "dialog-header thy-dialog-header" }, queries: [{ propertyName: "headerTemplate", first: true, predicate: ["dialogHeader"], descendants: true }], exportAs: ["thyDialogHeader"], ngImport: i0, template: "<ng-container *ngIf=\"headerTemplate; else defaultHeader\">\n  <template [ngTemplateOutlet]=\"headerTemplate\"></template>\n</ng-container>\n<ng-template #defaultHeader>\n  <h3 class=\"dialog-title\"><thy-icon [thyIconName]=\"thyIcon\"></thy-icon>{{ thyTitle }}</h3>\n  <a href=\"javascript:;\" class=\"mr-n2\" thyAction thyActionIcon=\"close\" (click)=\"close($event)\"></a>\n</ng-template>\n", dependencies: [{ kind: "directive", type: NgIf, selector: "[ngIf]", inputs: ["ngIf", "ngIfThen", "ngIfElse"] }, { kind: "directive", type: NgTemplateOutlet, selector: "[ngTemplateOutlet]", inputs: ["ngTemplateOutletContext", "ngTemplateOutlet", "ngTemplateOutletInjector"] }, { kind: "component", type: ThyIcon, selector: "thy-icon, [thy-icon]", inputs: ["thyIconType", "thyTwotoneColor", "thyIconName", "thyIconRotate", "thyIconSet", "thyIconLegging", "thyIconLinearGradient"] }, { kind: "component", type: ThyAction, selector: "thy-action, [thyAction]", inputs: ["thyType", "thyIcon", "thyActionIcon", "thyActive", "thyActionActive", "thyTheme", "thyHoverIcon", "thyDisabled"] }] }); }
}
i0.ɵɵngDeclareClassMetadata({ minVersion: "12.0.0", version: "17.2.3", ngImport: i0, type: ThyDialogHeader, decorators: [{
            type: Component,
            args: [{ selector: 'thy-dialog-header', exportAs: 'thyDialogHeader', host: {
                        class: 'dialog-header thy-dialog-header',
                        '[class.thy-dialog-header-lg]': `thySize === 'lg'`,
                        '[class.thy-dialog-header-divided]': `thyDivided`
                    }, standalone: true, imports: [NgIf, NgTemplateOutlet, ThyIcon, ThyAction], template: "<ng-container *ngIf=\"headerTemplate; else defaultHeader\">\n  <template [ngTemplateOutlet]=\"headerTemplate\"></template>\n</ng-container>\n<ng-template #defaultHeader>\n  <h3 class=\"dialog-title\"><thy-icon [thyIconName]=\"thyIcon\"></thy-icon>{{ thyTitle }}</h3>\n  <a href=\"javascript:;\" class=\"mr-n2\" thyAction thyActionIcon=\"close\" (click)=\"close($event)\"></a>\n</ng-template>\n" }]
        }], ctorParameters: () => [{ type: i0.ElementRef }, { type: ThyDialog }, { type: i2.ThyTranslate }, { type: ThyDialogContainer, decorators: [{
                    type: Optional
                }] }], propDecorators: { headerTemplate: [{
                type: ContentChild,
                args: ['dialogHeader']
            }], thyTitle: [{
                type: Input
            }], thySize: [{
                type: Input
            }], thyDivided: [{
                type: Input,
                args: [{ transform: booleanAttribute }]
            }], thyTitleTranslationKey: [{
                type: Input
            }], thyIcon: [{
                type: Input
            }], thyOnClose: [{
                type: Output
            }] } });

/**
 * @private
 */
class ThyConfirm {
    constructor(dialogRef, changeDetectorRef, defaultConfig) {
        this.dialogRef = dialogRef;
        this.changeDetectorRef = changeDetectorRef;
        this.defaultConfig = defaultConfig;
        this.defaultConfig = { ...THY_CONFIRM_DEFAULT_OPTIONS_VALUE, ...this.defaultConfig };
    }
    ngOnInit() {
        this.defaultConfig = { ...this.defaultConfig, ...this.options };
        this.title = this.defaultConfig.title;
        this.content = this.defaultConfig.content;
        this.okText = this.defaultConfig.okText;
        this.okType = this.defaultConfig.okType;
        this.cancelText = this.defaultConfig.cancelText;
        this.okLoadingText = this.defaultConfig.okLoadingText || this.okText;
        this.footerAlign = this.defaultConfig.footerAlign;
    }
    confirm() {
        this.loading = true;
        const result = this.options.onOk();
        if (result && result.subscribe) {
            result
                .pipe(finalize(() => {
                this.loading = false;
                this.changeDetectorRef.markForCheck();
            }))
                .subscribe(success => {
                if (success) {
                    this.close();
                }
            });
        }
        else {
            this.close();
        }
    }
    close() {
        this.options?.onCancel?.();
        this.dialogRef.close();
    }
    ngOnDestroy() { }
    static { this.ɵfac = i0.ɵɵngDeclareFactory({ minVersion: "12.0.0", version: "17.2.3", ngImport: i0, type: ThyConfirm, deps: [{ token: ThyDialogRef }, { token: i0.ChangeDetectorRef }, { token: THY_CONFIRM_DEFAULT_OPTIONS }], target: i0.ɵɵFactoryTarget.Component }); }
    static { this.ɵcmp = i0.ɵɵngDeclareComponent({ minVersion: "14.0.0", version: "17.2.3", type: ThyConfirm, isStandalone: true, selector: "thy-confirm-default", inputs: { options: "options" }, ngImport: i0, template: "<thy-dialog-header [thyTitle]=\"title || 'Install Angular'\" (thyOnClose)=\"close()\"> </thy-dialog-header>\n<thy-dialog-body class=\"thy-form\">\n  <form thyForm name=\"thyConfirm\" thyLayout=\"vertical\">\n    <div class=\"form-group\">\n      <p [innerHTML]=\"content\"></p>\n    </div>\n    <thy-form-group-footer [thyAlign]=\"footerAlign\">\n      <div class=\"thy-confirm-footer\" [ngClass]=\"'thy-confirm-footer-' + footerAlign\">\n        <button [thyButton]=\"okType\" (click)=\"confirm()\" [thyLoading]=\"loading\" [thyLoadingText]=\"okLoadingText\">{{ okText }}</button>\n        <button thyButton=\"link-secondary\" (click)=\"close()\" class=\"thy-confirm-cancel\">{{ cancelText }}</button>\n      </div>\n    </thy-form-group-footer>\n  </form>\n</thy-dialog-body>\n", dependencies: [{ kind: "component", type: ThyDialogHeader, selector: "thy-dialog-header", inputs: ["thyTitle", "thySize", "thyDivided", "thyTitleTranslationKey", "thyIcon"], outputs: ["thyOnClose"], exportAs: ["thyDialogHeader"] }, { kind: "component", type: ThyDialogBody, selector: "thy-dialog-body", inputs: ["thyClearPadding"], exportAs: ["thyDialogBody"] }, { kind: "ngmodule", type: FormsModule }, { kind: "directive", type: i2$2.ɵNgNoValidate, selector: "form:not([ngNoForm]):not([ngNativeValidate])" }, { kind: "directive", type: i2$2.NgControlStatusGroup, selector: "[formGroupName],[formArrayName],[ngModelGroup],[formGroup],form:not([ngNoForm]),[ngForm]" }, { kind: "directive", type: i2$2.NgForm, selector: "form:not([ngNoForm]):not([formGroup]),ng-form,[ngForm]", inputs: ["ngFormOptions"], outputs: ["ngSubmit"], exportAs: ["ngForm"] }, { kind: "directive", type: ThyFormDirective, selector: "[thyForm],[thy-form]", inputs: ["thyLayout", "thyEnterKeyMode", "thyFormValidatorConfig"], exportAs: ["thyForm"] }, { kind: "component", type: ThyFormGroupFooter, selector: "thy-form-group-footer", inputs: ["thyAlign"] }, { kind: "directive", type: NgClass, selector: "[ngClass]", inputs: ["class", "ngClass"] }, { kind: "component", type: ThyButton, selector: "thy-button,[thy-button],[thyButton]", inputs: ["thyButton", "thyType", "thyLoading", "thyLoadingText", "thySize", "thyIcon", "thyBlock"] }], changeDetection: i0.ChangeDetectionStrategy.OnPush }); }
}
i0.ɵɵngDeclareClassMetadata({ minVersion: "12.0.0", version: "17.2.3", ngImport: i0, type: ThyConfirm, decorators: [{
            type: Component,
            args: [{ selector: 'thy-confirm-default', changeDetection: ChangeDetectionStrategy.OnPush, standalone: true, imports: [ThyDialogHeader, ThyDialogBody, FormsModule, ThyFormDirective, ThyFormGroupFooter, NgClass, ThyButton], template: "<thy-dialog-header [thyTitle]=\"title || 'Install Angular'\" (thyOnClose)=\"close()\"> </thy-dialog-header>\n<thy-dialog-body class=\"thy-form\">\n  <form thyForm name=\"thyConfirm\" thyLayout=\"vertical\">\n    <div class=\"form-group\">\n      <p [innerHTML]=\"content\"></p>\n    </div>\n    <thy-form-group-footer [thyAlign]=\"footerAlign\">\n      <div class=\"thy-confirm-footer\" [ngClass]=\"'thy-confirm-footer-' + footerAlign\">\n        <button [thyButton]=\"okType\" (click)=\"confirm()\" [thyLoading]=\"loading\" [thyLoadingText]=\"okLoadingText\">{{ okText }}</button>\n        <button thyButton=\"link-secondary\" (click)=\"close()\" class=\"thy-confirm-cancel\">{{ cancelText }}</button>\n      </div>\n    </thy-form-group-footer>\n  </form>\n</thy-dialog-body>\n" }]
        }], ctorParameters: () => [{ type: ThyDialogRef }, { type: i0.ChangeDetectorRef }, { type: undefined, decorators: [{
                    type: Inject,
                    args: [THY_CONFIRM_DEFAULT_OPTIONS]
                }] }], propDecorators: { options: [{
                type: Input
            }] } });

/**
 * 模态框底部组件
 * @name thy-dialog-footer
 * @order 60
 */
class ThyDialogFooter {
    /**
     * 底部是否有分割线，可全局配置默认值
     * @type boolean
     * @default false
     */
    set thyDivided(value) {
        this.divided = value;
    }
    get align() {
        return !!this.thyAlign ? this.thyAlign : this.dialogLayoutConfig.footerAlign;
    }
    constructor(dialogLayoutConfig) {
        this.dialogLayoutConfig = dialogLayoutConfig;
        this.hostRenderer = useHostRenderer();
        this.divided = this.dialogLayoutConfig.footerDivided;
    }
    ngOnInit() {
        this.hostRenderer.updateClassByMap({
            'dialog-footer': true,
            [`dialog-footer-actions-align-${this.align}`]: true,
            'dialog-footer-border-top': this.divided
        });
    }
    static { this.ɵfac = i0.ɵɵngDeclareFactory({ minVersion: "12.0.0", version: "17.2.3", ngImport: i0, type: ThyDialogFooter, deps: [{ token: THY_DIALOG_LAYOUT_CONFIG }], target: i0.ɵɵFactoryTarget.Component }); }
    static { this.ɵcmp = i0.ɵɵngDeclareComponent({ minVersion: "16.1.0", version: "17.2.3", type: ThyDialogFooter, isStandalone: true, selector: "thy-dialog-footer", inputs: { thyDivided: ["thyDivided", "thyDivided", booleanAttribute], thyAlign: "thyAlign" }, queries: [{ propertyName: "description", first: true, predicate: ["description"], descendants: true }], exportAs: ["thyDialogFooter"], ngImport: i0, template: "<div class=\"dialog-footer-actions btn-pair\">\n  <ng-content></ng-content>\n</div>\n<div class=\"dialog-footer-description\" *ngIf=\"description\">\n  <ng-container [ngTemplateOutlet]=\"description\"></ng-container>\n</div>\n", dependencies: [{ kind: "directive", type: NgIf, selector: "[ngIf]", inputs: ["ngIf", "ngIfThen", "ngIfElse"] }, { kind: "directive", type: NgTemplateOutlet, selector: "[ngTemplateOutlet]", inputs: ["ngTemplateOutletContext", "ngTemplateOutlet", "ngTemplateOutletInjector"] }] }); }
}
i0.ɵɵngDeclareClassMetadata({ minVersion: "12.0.0", version: "17.2.3", ngImport: i0, type: ThyDialogFooter, decorators: [{
            type: Component,
            args: [{ selector: 'thy-dialog-footer', exportAs: 'thyDialogFooter', standalone: true, imports: [NgIf, NgTemplateOutlet], template: "<div class=\"dialog-footer-actions btn-pair\">\n  <ng-content></ng-content>\n</div>\n<div class=\"dialog-footer-description\" *ngIf=\"description\">\n  <ng-container [ngTemplateOutlet]=\"description\"></ng-container>\n</div>\n" }]
        }], ctorParameters: () => [{ type: ThyDialogLayoutConfig, decorators: [{
                    type: Inject,
                    args: [THY_DIALOG_LAYOUT_CONFIG]
                }] }], propDecorators: { description: [{
                type: ContentChild,
                args: ['description']
            }], thyDivided: [{
                type: Input,
                args: [{ transform: booleanAttribute }]
            }], thyAlign: [{
                type: Input
            }] } });

class ThyDialogModule {
    static { this.ɵfac = i0.ɵɵngDeclareFactory({ minVersion: "12.0.0", version: "17.2.3", ngImport: i0, type: ThyDialogModule, deps: [], target: i0.ɵɵFactoryTarget.NgModule }); }
    static { this.ɵmod = i0.ɵɵngDeclareNgModule({ minVersion: "14.0.0", version: "17.2.3", ngImport: i0, type: ThyDialogModule, imports: [CommonModule,
            ThySharedModule,
            PortalModule,
            OverlayModule,
            ThyButtonModule,
            ThyIconModule,
            ThyFormModule,
            FormsModule,
            ThyActionModule,
            ThyDialogContainer,
            ThyDialogHeader,
            ThyDialogBody,
            ThyDialogFooter,
            ThyConfirm], exports: [ThyDialogContainer, ThyDialogHeader, ThyDialogBody, ThyDialogFooter] }); }
    static { this.ɵinj = i0.ɵɵngDeclareInjector({ minVersion: "12.0.0", version: "17.2.3", ngImport: i0, type: ThyDialogModule, providers: [
            ThyDialog,
            THY_DIALOG_DEFAULT_OPTIONS_PROVIDER,
            THY_CONFIRM_DEFAULT_OPTIONS_PROVIDER,
            THY_DIALOG_LAYOUT_CONFIG_PROVIDER,
            {
                provide: THY_CONFIRM_COMPONENT_TOKEN,
                useValue: ThyConfirm
            }
        ], imports: [CommonModule,
            ThySharedModule,
            PortalModule,
            OverlayModule,
            ThyButtonModule,
            ThyIconModule,
            ThyFormModule,
            FormsModule,
            ThyActionModule,
            ThyDialogContainer,
            ThyDialogHeader,
            ThyConfirm] }); }
}
i0.ɵɵngDeclareClassMetadata({ minVersion: "12.0.0", version: "17.2.3", ngImport: i0, type: ThyDialogModule, decorators: [{
            type: NgModule,
            args: [{
                    imports: [
                        CommonModule,
                        ThySharedModule,
                        PortalModule,
                        OverlayModule,
                        ThyButtonModule,
                        ThyIconModule,
                        ThyFormModule,
                        FormsModule,
                        ThyActionModule,
                        ThyDialogContainer,
                        ThyDialogHeader,
                        ThyDialogBody,
                        ThyDialogFooter,
                        ThyConfirm
                    ],
                    providers: [
                        ThyDialog,
                        THY_DIALOG_DEFAULT_OPTIONS_PROVIDER,
                        THY_CONFIRM_DEFAULT_OPTIONS_PROVIDER,
                        THY_DIALOG_LAYOUT_CONFIG_PROVIDER,
                        {
                            provide: THY_CONFIRM_COMPONENT_TOKEN,
                            useValue: ThyConfirm
                        }
                    ],
                    exports: [ThyDialogContainer, ThyDialogHeader, ThyDialogBody, ThyDialogFooter]
                }]
        }] });

/**
 * Generated bundle index. Do not edit.
 */

export { THY_CONFIRM_DEFAULT_OPTIONS, THY_CONFIRM_DEFAULT_OPTIONS_PROVIDER, THY_CONFIRM_DEFAULT_OPTIONS_VALUE, THY_DIALOG_DEFAULT_OPTIONS, THY_DIALOG_DEFAULT_OPTIONS_PROVIDER, THY_DIALOG_LAYOUT_CONFIG, THY_DIALOG_LAYOUT_CONFIG_PROVIDER, ThyConfirm as ThyConfirmComponent$1, ThyDialog, ThyDialogBody, ThyDialogConfig, ThyDialogContainer, ThyDialogFooter, ThyDialogHeader, ThyDialogLayoutConfig, ThyDialogModule, ThyDialogRef, ThyDialogSizes, thyDialogAnimations };
//# sourceMappingURL=ngx-tethys-dialog.mjs.map
