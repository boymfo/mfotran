import * as i0 from '@angular/core';
import { booleanAttribute, Component, ChangeDetectionStrategy, Input, ContentChildren, NgModule } from '@angular/core';
import { useHostRenderer } from '@tethys/cdk/dom';
import { ThyIcon, ThyIconModule } from 'ngx-tethys/icon';
import { NgIf, CommonModule } from '@angular/common';
import { timer } from 'rxjs';
import { getNumericSize } from 'ngx-tethys/core';

const defaultFeedbackOptions = {
    success: {
        icon: 'check-circle-fill',
        class: 'text-success',
        duration: 3000
    },
    error: {
        icon: 'close-circle-fill',
        class: 'text-danger',
        duration: 3000
    }
};
/**
 * 立即操作组件
 * @name thy-action,[thyAction]
 */
class ThyAction {
    /**
     * 操作图标的类型
     * @type primary | success | danger | warning
     * @default primary
     */
    set thyType(value) {
        this.setActionType(value || 'primary');
    }
    /**
     * 操作图标，支持传参同时也支持在投影中写 thy-icon 组件
     */
    set thyIcon(icon) {
        this.icon = icon;
    }
    /**
     * 操作图标，当 thyIcon 和其他指令参数名有冲突时使用 thyActionIcon
     */
    set thyActionIcon(icon) {
        this.icon = icon;
    }
    /**
     * 操作的图标 Active 状态，设置为 true 时会在 Item 上添加 active class
     * @default false
     */
    set thyActive(value) {
        this.active = value;
    }
    /**
     * 操作的图标 Active 状态，当 thyActive 和其他指令参数名有冲突时使用 thyActionActive
     * @default false
     */
    set thyActionActive(value) {
        this.active = value;
    }
    constructor(elementRef, renderer, cdr) {
        this.elementRef = elementRef;
        this.renderer = renderer;
        this.cdr = cdr;
        this.feedback = null;
        this.active = false;
        this.type = 'primary';
        this.hostRenderer = useHostRenderer();
        /**
         * 操作图标的主题
         * @type fill(背景色填充) | lite(简单文本颜色变化)
         */
        this.thyTheme = 'fill';
    }
    ngOnInit() {
        this.updateClasses();
    }
    ngAfterViewInit() {
        this.wrapSpanForText(this.elementRef.nativeElement.childNodes);
    }
    ngOnChanges(changes) {
        if ((changes.thyType && !changes.thyType.firstChange) || (changes.thyTheme && !changes.thyTheme.firstChange)) {
            this.updateClasses();
        }
    }
    setMarginRight(marginRight) {
        this.elementRef.nativeElement.style.marginRight = marginRight;
    }
    /**
     * 触发成功反馈操作
     */
    success(options) {
        this.setFeedback('success', options);
    }
    /**
     * 触发失败反馈操作
     */
    error(options) {
        this.setFeedback('error', options);
    }
    setFeedback(feedback, options) {
        if (this.thyDisabled) {
            return;
        }
        options = Object.assign({}, defaultFeedbackOptions[feedback], options);
        this.feedback = feedback;
        this.feedbackOptions = options;
        this.cdr.markForCheck();
        if (options.duration) {
            if (this.feedbackTimer) {
                this.feedbackTimer.unsubscribe();
            }
            this.feedbackTimer = timer(options.duration).subscribe(() => {
                this.feedback = null;
                this.feedbackOptions = null;
                this.cdr.markForCheck();
            });
        }
    }
    wrapSpanForText(nodes) {
        nodes.forEach(node => {
            if (node.nodeName === '#text') {
                const span = this.renderer.createElement('span');
                const parent = this.renderer.parentNode(node);
                // this.renderer.addClass(span, 'thy-action-wrap-span');
                this.renderer.insertBefore(parent, span, node);
                this.renderer.appendChild(span, node);
            }
        });
    }
    setActionType(value) {
        this.type = value;
    }
    updateClasses() {
        let classNames = [];
        classNames.push(`action-${this.type}`);
        if (this.thyTheme === 'lite') {
            classNames.push('thy-action-lite');
        }
        this.hostRenderer.updateClass(classNames);
    }
    ngOnDestroy() {
        this.feedbackTimer?.unsubscribe();
    }
    static { this.ɵfac = i0.ɵɵngDeclareFactory({ minVersion: "12.0.0", version: "17.2.3", ngImport: i0, type: ThyAction, deps: [{ token: i0.ElementRef }, { token: i0.Renderer2 }, { token: i0.ChangeDetectorRef }], target: i0.ɵɵFactoryTarget.Component }); }
    static { this.ɵcmp = i0.ɵɵngDeclareComponent({ minVersion: "16.1.0", version: "17.2.3", type: ThyAction, isStandalone: true, selector: "thy-action, [thyAction]", inputs: { thyType: "thyType", thyIcon: "thyIcon", thyActionIcon: "thyActionIcon", thyActive: ["thyActive", "thyActive", booleanAttribute], thyActionActive: ["thyActionActive", "thyActionActive", booleanAttribute], thyTheme: "thyTheme", thyHoverIcon: "thyHoverIcon", thyDisabled: ["thyDisabled", "thyDisabled", booleanAttribute] }, host: { properties: { "class.active": "active", "class.thy-action-hover-icon": "thyHoverIcon", "class.thy-action-has-feedback": "!!feedback", "class.disabled": "thyDisabled" }, classAttribute: "thy-action" }, usesOnChanges: true, ngImport: i0, template: "<thy-icon *ngIf=\"icon && !feedback\" [thyIconName]=\"icon\"></thy-icon>\n<thy-icon *ngIf=\"feedbackOptions?.icon\" [class]=\"feedbackOptions.class\" [thyIconName]=\"feedbackOptions.icon\"></thy-icon>\n<thy-icon class=\"hover-icon\" *ngIf=\"thyHoverIcon\" [thyIconName]=\"thyHoverIcon\"></thy-icon>\n<ng-content></ng-content>\n", dependencies: [{ kind: "directive", type: NgIf, selector: "[ngIf]", inputs: ["ngIf", "ngIfThen", "ngIfElse"] }, { kind: "component", type: ThyIcon, selector: "thy-icon, [thy-icon]", inputs: ["thyIconType", "thyTwotoneColor", "thyIconName", "thyIconRotate", "thyIconSet", "thyIconLegging", "thyIconLinearGradient"] }], changeDetection: i0.ChangeDetectionStrategy.OnPush }); }
}
i0.ɵɵngDeclareClassMetadata({ minVersion: "12.0.0", version: "17.2.3", ngImport: i0, type: ThyAction, decorators: [{
            type: Component,
            args: [{ selector: 'thy-action, [thyAction]', changeDetection: ChangeDetectionStrategy.OnPush, host: {
                        class: 'thy-action',
                        '[class.active]': 'active',
                        '[class.thy-action-hover-icon]': 'thyHoverIcon',
                        '[class.thy-action-has-feedback]': '!!feedback',
                        '[class.disabled]': 'thyDisabled'
                    }, standalone: true, imports: [NgIf, ThyIcon], template: "<thy-icon *ngIf=\"icon && !feedback\" [thyIconName]=\"icon\"></thy-icon>\n<thy-icon *ngIf=\"feedbackOptions?.icon\" [class]=\"feedbackOptions.class\" [thyIconName]=\"feedbackOptions.icon\"></thy-icon>\n<thy-icon class=\"hover-icon\" *ngIf=\"thyHoverIcon\" [thyIconName]=\"thyHoverIcon\"></thy-icon>\n<ng-content></ng-content>\n" }]
        }], ctorParameters: () => [{ type: i0.ElementRef }, { type: i0.Renderer2 }, { type: i0.ChangeDetectorRef }], propDecorators: { thyType: [{
                type: Input
            }], thyIcon: [{
                type: Input
            }], thyActionIcon: [{
                type: Input
            }], thyActive: [{
                type: Input,
                args: [{ transform: booleanAttribute }]
            }], thyActionActive: [{
                type: Input,
                args: [{ transform: booleanAttribute }]
            }], thyTheme: [{
                type: Input
            }], thyHoverIcon: [{
                type: Input
            }], thyDisabled: [{
                type: Input,
                args: [{ transform: booleanAttribute }]
            }] } });

/**
 * Actions 组件
 * @name thy-actions
 */
class ThyActions {
    constructor() {
        /**
         * 大小，支持 `zero` | `xxs` | `xs` | `sm` | `md` | `lg` | `xlg` 和自定义数字大小
         * @type string | number
         */
        this.thySize = 'md';
    }
    ngOnInit() { }
    ngOnChanges(changes) {
        if (changes.thySize && !changes.thySize.firstChange && this.actions) {
            this.setActionsSize(this.actions.toArray());
        }
    }
    ngAfterContentInit() {
        this.actions.changes.subscribe((actions) => {
            this.setActionsSize(actions);
        });
        this.setActionsSize(this.actions.toArray());
    }
    setActionsSize(actions) {
        actions.forEach((action, index) => {
            // can't set marginRight value for last item
            if (index !== actions.length - 1) {
                action.setMarginRight(getNumericSize(this.thySize, 'md') + 'px');
            }
        });
    }
    static { this.ɵfac = i0.ɵɵngDeclareFactory({ minVersion: "12.0.0", version: "17.2.3", ngImport: i0, type: ThyActions, deps: [], target: i0.ɵɵFactoryTarget.Component }); }
    static { this.ɵcmp = i0.ɵɵngDeclareComponent({ minVersion: "14.0.0", version: "17.2.3", type: ThyActions, isStandalone: true, selector: "thy-actions", inputs: { thySize: "thySize" }, host: { classAttribute: "thy-actions" }, queries: [{ propertyName: "actions", predicate: ThyAction }], usesOnChanges: true, ngImport: i0, template: ` <ng-content></ng-content> `, isInline: true, changeDetection: i0.ChangeDetectionStrategy.OnPush }); }
}
i0.ɵɵngDeclareClassMetadata({ minVersion: "12.0.0", version: "17.2.3", ngImport: i0, type: ThyActions, decorators: [{
            type: Component,
            args: [{
                    selector: 'thy-actions',
                    template: ` <ng-content></ng-content> `,
                    changeDetection: ChangeDetectionStrategy.OnPush,
                    host: {
                        class: 'thy-actions'
                    },
                    standalone: true
                }]
        }], ctorParameters: () => [], propDecorators: { actions: [{
                type: ContentChildren,
                args: [ThyAction]
            }], thySize: [{
                type: Input
            }] } });

class ThyActionModule {
    static { this.ɵfac = i0.ɵɵngDeclareFactory({ minVersion: "12.0.0", version: "17.2.3", ngImport: i0, type: ThyActionModule, deps: [], target: i0.ɵɵFactoryTarget.NgModule }); }
    static { this.ɵmod = i0.ɵɵngDeclareNgModule({ minVersion: "14.0.0", version: "17.2.3", ngImport: i0, type: ThyActionModule, imports: [CommonModule, ThyIconModule, ThyAction, ThyActions], exports: [ThyAction, ThyActions] }); }
    static { this.ɵinj = i0.ɵɵngDeclareInjector({ minVersion: "12.0.0", version: "17.2.3", ngImport: i0, type: ThyActionModule, imports: [CommonModule, ThyIconModule, ThyAction] }); }
}
i0.ɵɵngDeclareClassMetadata({ minVersion: "12.0.0", version: "17.2.3", ngImport: i0, type: ThyActionModule, decorators: [{
            type: NgModule,
            args: [{
                    imports: [CommonModule, ThyIconModule, ThyAction, ThyActions],
                    exports: [ThyAction, ThyActions]
                }]
        }] });

/**
 * Generated bundle index. Do not edit.
 */

export { ThyAction, ThyActionModule, ThyActions };
//# sourceMappingURL=ngx-tethys-action.mjs.map
