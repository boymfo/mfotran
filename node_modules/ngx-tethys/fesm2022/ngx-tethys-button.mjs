import { NgIf, NgClass, CommonModule } from '@angular/common';
import * as i0 from '@angular/core';
import { Component, ViewEncapsulation, Input, HostBinding, booleanAttribute, ChangeDetectionStrategy, NgModule } from '@angular/core';
import { ThyIcon, ThyIconModule } from 'ngx-tethys/icon';
import { coerceBooleanProperty } from '@angular/cdk/coercion';
import { useHostRenderer } from '@tethys/cdk/dom';
import { assertIconOnly } from 'ngx-tethys/util';

const buttonGroupSizeMap = {
    sm: ['btn-group-sm'],
    md: ['btn-group-md'],
    lg: ['btn-group-lg'],
    xs: ['btn-group-xs']
};
/**
 * 按钮分组组件
 * @name thy-button-group
 * @order 30
 */
class ThyButtonGroup {
    /**
     * 大小
     * @type xs | sm | md | lg
     * @default md
     */
    set thySize(size) {
        this.size = size;
        if (this.initialized) {
            this.setClasses();
        }
    }
    /**
     * 类型
     * @type outline-default | outline-primary
     * @default outline-default
     */
    set thyType(type) {
        this.type = type;
        if (this.initialized) {
            this.setClasses();
        }
    }
    /**
     * 是否需要最小宽度，默认按钮最小宽度为80px
     * @default false
     */
    set thyClearMinWidth(value) {
        this.thyClearMinWidthClassName = coerceBooleanProperty(value);
    }
    constructor() {
        this.initialized = false;
        this.hostRenderer = useHostRenderer();
        this._isButtonGroup = true;
        this.thyClearMinWidthClassName = false;
    }
    ngOnInit() {
        this.setClasses();
        this.initialized = true;
    }
    setClasses() {
        let classNames = [];
        if (this.type) {
            classNames.push(`btn-group-${this.type}`);
        }
        if (buttonGroupSizeMap[this.size]) {
            classNames.push(buttonGroupSizeMap[this.size]);
        }
        this.hostRenderer.updateClass(classNames);
    }
    static { this.ɵfac = i0.ɵɵngDeclareFactory({ minVersion: "12.0.0", version: "17.2.3", ngImport: i0, type: ThyButtonGroup, deps: [], target: i0.ɵɵFactoryTarget.Component }); }
    static { this.ɵcmp = i0.ɵɵngDeclareComponent({ minVersion: "14.0.0", version: "17.2.3", type: ThyButtonGroup, isStandalone: true, selector: "thy-button-group", inputs: { thySize: "thySize", thyType: "thyType", thyClearMinWidth: "thyClearMinWidth" }, host: { properties: { "class.btn-group": "this._isButtonGroup", "class.btn-group-clear-min-width": "this.thyClearMinWidthClassName" } }, ngImport: i0, template: '<ng-content></ng-content>', isInline: true, encapsulation: i0.ViewEncapsulation.None }); }
}
i0.ɵɵngDeclareClassMetadata({ minVersion: "12.0.0", version: "17.2.3", ngImport: i0, type: ThyButtonGroup, decorators: [{
            type: Component,
            args: [{
                    selector: 'thy-button-group',
                    template: '<ng-content></ng-content>',
                    encapsulation: ViewEncapsulation.None,
                    standalone: true
                }]
        }], ctorParameters: () => [], propDecorators: { thySize: [{
                type: Input
            }], thyType: [{
                type: Input
            }], thyClearMinWidth: [{
                type: Input
            }], _isButtonGroup: [{
                type: HostBinding,
                args: ['class.btn-group']
            }], thyClearMinWidthClassName: [{
                type: HostBinding,
                args: [`class.btn-group-clear-min-width`]
            }] } });

const sizeClassesMap = {
    lg: ['btn-icon-lg'],
    md: ['btn-icon-md'],
    sm: ['btn-icon-sm'],
    xs: ['btn-icon-xs']
};
const shapeClassesMap = {
    'circle-dashed': ['btn-icon-circle', 'circle-dashed'],
    'circle-solid': ['btn-icon-circle', 'circle-solid'],
    'circle-thick-dashed': ['btn-icon-circle', 'circle-dashed', 'border-thick'],
    'circle-thick-solid': ['btn-icon-circle', 'circle-solid', 'border-thick'],
    'self-icon': ['btn-icon-self-circle']
};
const themeClassesMap = {
    'danger-weak': ['btn-icon-danger-weak']
};
/**
 * 操作按钮图标，支持`thy-button-icon`组件和`thyButtonIcon`指令两种形式
 * @name thy-button-icon,[thy-button-icon],[thyButtonIcon]
 * @order 20
 */
class ThyButtonIcon {
    /**
     * 大小
     * @type xs | sm | md | lg
     * @default 36px
     */
    set thySize(size) {
        this.size = size;
        this.setClasses();
    }
    /**
     * 图标, 和`thyButtonIcon`相同，当使用`thy-button-icon`时，只能使用 thyIcon 设置图标
     */
    set thyIcon(icon) {
        this.setIconClass(icon);
    }
    /**
     * 图标按钮的图标
     */
    set thyButtonIcon(icon) {
        this.setIconClass(icon);
    }
    /**
     * 展示的形状，默认只显示字体图标图标，circle-dashed, circle-solid 展示成虚线,实线边框圆形图标, circle-thick-dashed, circle-thick-solid 边框加粗
     */
    set thyShape(value) {
        this.shape = value;
        this.setClasses();
    }
    /**
     * 亮色，颜色更浅，适合左侧导航顶部的按钮
     * @default false
     */
    set thyLight(value) {
        this._isLighted = value;
    }
    /**
     * 设置为选中状态
     * @default false
     */
    set thyActive(value) {
        this._isActive = value;
    }
    /**
     * 按钮展示类型，默认图标移上去显示主色， danger-weak 鼠标移上去显示 danger 红色
     */
    set thyTheme(value) {
        this.theme = value;
        this.setClasses();
    }
    constructor() {
        this.initialized = false;
        this.hostRenderer = useHostRenderer();
        this.iconPrefix = 'wtf';
        this._isBtn = true;
        this._isBtnIcon = true;
        this._isLighted = false;
        this._isActive = false;
    }
    setIconClass(icon) {
        if (icon) {
            if (icon.includes('wtf')) {
                const classes = icon.split(' ');
                if (classes.length === 1) {
                    classes.unshift('wtf');
                }
                this.iconClasses = classes;
                this.svgIconName = null;
            }
            else {
                this.svgIconName = icon;
            }
        }
        else {
            this.iconClasses = null;
            this.svgIconName = null;
        }
    }
    setClasses(first = false) {
        // 设置样式判断是否已经初始化，未初始化直接返回，除非是初次调用
        // 只有 ngOnInit 调用会传入 first = true
        if (!first && !this.initialized) {
            return;
        }
        const classes = sizeClassesMap[this.size] ? [...sizeClassesMap[this.size]] : [];
        if (this.shape && shapeClassesMap[this.shape]) {
            shapeClassesMap[this.shape].forEach((className) => {
                classes.push(className);
            });
        }
        if (this.theme && themeClassesMap[this.theme]) {
            themeClassesMap[this.theme].forEach((className) => {
                classes.push(className);
            });
        }
        this.hostRenderer.updateClass(classes);
    }
    ngOnInit() {
        this.setClasses(true);
        this.initialized = true;
    }
    static { this.ɵfac = i0.ɵɵngDeclareFactory({ minVersion: "12.0.0", version: "17.2.3", ngImport: i0, type: ThyButtonIcon, deps: [], target: i0.ɵɵFactoryTarget.Component }); }
    static { this.ɵcmp = i0.ɵɵngDeclareComponent({ minVersion: "16.1.0", version: "17.2.3", type: ThyButtonIcon, isStandalone: true, selector: "thy-button-icon,[thy-button-icon],[thyButtonIcon]", inputs: { thySize: "thySize", thyIcon: "thyIcon", thyButtonIcon: "thyButtonIcon", thyShape: "thyShape", thyLight: ["thyLight", "thyLight", booleanAttribute], thyActive: ["thyActive", "thyActive", booleanAttribute], thyTheme: "thyTheme", thyColor: "thyColor" }, host: { properties: { "class.btn": "this._isBtn", "class.btn-icon": "this._isBtnIcon", "class.btn-icon-light": "this._isLighted", "class.btn-icon-active": "this._isActive" } }, ngImport: i0, template: "<thy-icon *ngIf=\"svgIconName; else iconFont\" [thyIconName]=\"svgIconName\" [style.color]=\"thyColor\" [style.borderColor]=\"thyColor\"></thy-icon>\n<ng-template #iconFont>\n  <i [ngClass]=\"iconClasses\" *ngIf=\"iconClasses\" [style.color]=\"thyColor\" [style.borderColor]=\"thyColor\"></i>\n</ng-template>\n<ng-content></ng-content>\n", dependencies: [{ kind: "directive", type: NgIf, selector: "[ngIf]", inputs: ["ngIf", "ngIfThen", "ngIfElse"] }, { kind: "component", type: ThyIcon, selector: "thy-icon, [thy-icon]", inputs: ["thyIconType", "thyTwotoneColor", "thyIconName", "thyIconRotate", "thyIconSet", "thyIconLegging", "thyIconLinearGradient"] }, { kind: "directive", type: NgClass, selector: "[ngClass]", inputs: ["class", "ngClass"] }], changeDetection: i0.ChangeDetectionStrategy.OnPush, encapsulation: i0.ViewEncapsulation.None }); }
}
i0.ɵɵngDeclareClassMetadata({ minVersion: "12.0.0", version: "17.2.3", ngImport: i0, type: ThyButtonIcon, decorators: [{
            type: Component,
            args: [{ selector: 'thy-button-icon,[thy-button-icon],[thyButtonIcon]', encapsulation: ViewEncapsulation.None, changeDetection: ChangeDetectionStrategy.OnPush, standalone: true, imports: [NgIf, ThyIcon, NgClass], template: "<thy-icon *ngIf=\"svgIconName; else iconFont\" [thyIconName]=\"svgIconName\" [style.color]=\"thyColor\" [style.borderColor]=\"thyColor\"></thy-icon>\n<ng-template #iconFont>\n  <i [ngClass]=\"iconClasses\" *ngIf=\"iconClasses\" [style.color]=\"thyColor\" [style.borderColor]=\"thyColor\"></i>\n</ng-template>\n<ng-content></ng-content>\n" }]
        }], ctorParameters: () => [], propDecorators: { thySize: [{
                type: Input
            }], thyIcon: [{
                type: Input
            }], thyButtonIcon: [{
                type: Input
            }], thyShape: [{
                type: Input
            }], thyLight: [{
                type: Input,
                args: [{ transform: booleanAttribute }]
            }], thyActive: [{
                type: Input,
                args: [{ transform: booleanAttribute }]
            }], thyTheme: [{
                type: Input
            }], _isBtn: [{
                type: HostBinding,
                args: ['class.btn']
            }], _isBtnIcon: [{
                type: HostBinding,
                args: ['class.btn-icon']
            }], _isLighted: [{
                type: HostBinding,
                args: ['class.btn-icon-light']
            }], _isActive: [{
                type: HostBinding,
                args: ['class.btn-icon-active']
            }], thyColor: [{
                type: Input
            }] } });

const btnTypeClassesMap = {
    primary: ['btn-primary'],
    secondary: ['btn-primary', 'btn-md'],
    info: ['btn-info'],
    warning: ['btn-warning'],
    danger: ['btn-danger'],
    'outline-primary': ['btn-outline-primary'],
    'outline-default': ['btn-outline-default'],
    link: ['btn-link'], // 链接按钮
    'link-info': ['btn-link', 'btn-link-info'], // 幽灵链接按钮
    'link-secondary': ['btn-link', 'btn-link-primary-weak'], // 幽灵链接按钮
    'link-danger-weak': ['btn-link', 'btn-link-danger-weak'], // 幽灵危险按钮
    'link-danger': ['btn-link', 'btn-link-danger'], // 危险按钮
    'link-success': ['btn-link', 'btn-link-success'] // 成功按钮
};
const iconOnlyClass = 'thy-btn-icon-only';
/**
 * 操作按钮，支持组件`thy-button`和`thyButton`指令两种形式
 * @name thy-button,[thy-button],[thyButton]
 * @order 10
 */
class ThyButton {
    get nativeElement() {
        return this.elementRef.nativeElement;
    }
    /**
     * 按钮类型，支持添加前缀`outline-`实现线框按钮，支持添加前缀`link-`实现按钮链接
     * @type primary | info | warning | danger | success
     * @default primary
     */
    set thyButton(value) {
        this.setBtnType(value);
    }
    /**
     * 和`thyButton`参数一样，一般使用`thyButton`，为了减少参数输入, 当通过`thy-button`使用时，只能使用该参数控制类型
     * @default primary
     */
    set thyType(value) {
        this.setBtnType(value);
    }
    /**
     * 加载状态
     * @default false
     */
    set thyLoading(value) {
        if (!this._loading && value) {
            this._loading = value;
            const textElement = this.nativeElement.querySelector('span');
            this._originalText = textElement ? textElement.innerText : '';
            this.setLoadingStatus();
        }
        else {
            this._loading = value;
            this.setLoadingStatus();
        }
    }
    /**
     * 加载状态时显示的文案
     */
    set thyLoadingText(value) {
        if (this._loadingText !== value) {
            this._loadingText = value;
            if (this._loading) {
                this.setLoadingText(this._loadingText);
            }
        }
    }
    /**
     * 按钮大小
     * @type xs | sm | md | default | lg
     * @default default
     */
    set thySize(size) {
        this._size = size;
        if (this._initialized) {
            this.updateClasses();
        }
    }
    /**
     * 按钮中显示的图标，支持SVG图标名称，比如`angle-left`，也支持传之前的 wtf 字体，比如: wtf-plus
     */
    set thyIcon(icon) {
        this._icon = icon;
        if (this._icon) {
            if (icon.includes('wtf')) {
                const classes = this._icon.split(' ');
                if (classes.length === 1) {
                    classes.unshift('wtf');
                }
                this.iconClass = classes;
                this.svgIconName = null;
            }
            else {
                this.svgIconName = icon;
            }
        }
        else {
            this.iconClass = null;
            this.svgIconName = null;
        }
    }
    setBtnType(value) {
        if (value) {
            if (value.includes('-square')) {
                this._type = value.replace('-square', '');
                this._isRadiusSquare = true;
            }
            else {
                this._type = value;
            }
            if (this._initialized) {
                this.updateClasses();
            }
        }
    }
    setLoadingText(text) {
        const spanElement = this.nativeElement.querySelector('span');
        if (spanElement) {
            this.renderer.setProperty(spanElement, 'innerText', text);
        }
    }
    setLoadingStatus() {
        const innerText = this._loading ? this._loadingText : this._originalText;
        this.updateClasses();
        if (innerText) {
            this.setLoadingText(innerText);
        }
    }
    updateClasses() {
        let classNames = [];
        if (btnTypeClassesMap[this._type]) {
            classNames = [...btnTypeClassesMap[this._type]];
        }
        else {
            if (this._type) {
                classNames.push(`btn-${this._type}`);
            }
        }
        if (this._size) {
            classNames.push(`btn-${this._size}`);
        }
        if (this._isRadiusSquare) {
            classNames.push('btn-square');
        }
        if (this._loading) {
            classNames.push('loading');
        }
        this.hostRenderer.updateClass(classNames);
    }
    constructor(elementRef, renderer) {
        this.elementRef = elementRef;
        this.renderer = renderer;
        this._initialized = false;
        // 圆角方形
        this._isRadiusSquare = false;
        this.hostRenderer = useHostRenderer();
    }
    ngOnInit() {
        this.updateClasses();
        this._initialized = true;
    }
    ngAfterViewInit() {
        if (assertIconOnly(this.nativeElement)) {
            this.hostRenderer.addClass(iconOnlyClass);
        }
        else {
            this.hostRenderer.removeClass(iconOnlyClass);
        }
        this.wrapSpanForText(this.nativeElement.childNodes);
    }
    wrapSpanForText(nodes) {
        nodes.forEach(node => {
            if (node.nodeName === '#text') {
                const span = this.renderer.createElement('span');
                const parent = this.renderer.parentNode(node);
                this.renderer.addClass(span, 'thy-btn-wrap-span');
                this.renderer.insertBefore(parent, span, node);
                this.renderer.appendChild(span, node);
            }
        });
    }
    static { this.ɵfac = i0.ɵɵngDeclareFactory({ minVersion: "12.0.0", version: "17.2.3", ngImport: i0, type: ThyButton, deps: [{ token: i0.ElementRef }, { token: i0.Renderer2 }], target: i0.ɵɵFactoryTarget.Component }); }
    static { this.ɵcmp = i0.ɵɵngDeclareComponent({ minVersion: "16.1.0", version: "17.2.3", type: ThyButton, isStandalone: true, selector: "thy-button,[thy-button],[thyButton]", inputs: { thyButton: "thyButton", thyType: "thyType", thyLoading: ["thyLoading", "thyLoading", booleanAttribute], thyLoadingText: "thyLoadingText", thySize: "thySize", thyIcon: "thyIcon", thyBlock: ["thyBlock", "thyBlock", booleanAttribute] }, host: { properties: { "class.btn-block": "this.thyBlock" }, classAttribute: "thy-btn btn" }, ngImport: i0, template: "<thy-icon *ngIf=\"svgIconName; else iconFont\" [thyIconName]=\"svgIconName\"></thy-icon>\n<ng-template #iconFont>\n  <i [ngClass]=\"iconClass\" *ngIf=\"iconClass\"></i>\n</ng-template>\n<ng-content></ng-content>\n", dependencies: [{ kind: "directive", type: NgIf, selector: "[ngIf]", inputs: ["ngIf", "ngIfThen", "ngIfElse"] }, { kind: "component", type: ThyIcon, selector: "thy-icon, [thy-icon]", inputs: ["thyIconType", "thyTwotoneColor", "thyIconName", "thyIconRotate", "thyIconSet", "thyIconLegging", "thyIconLinearGradient"] }, { kind: "directive", type: NgClass, selector: "[ngClass]", inputs: ["class", "ngClass"] }], changeDetection: i0.ChangeDetectionStrategy.OnPush, encapsulation: i0.ViewEncapsulation.None }); }
}
i0.ɵɵngDeclareClassMetadata({ minVersion: "12.0.0", version: "17.2.3", ngImport: i0, type: ThyButton, decorators: [{
            type: Component,
            args: [{ selector: 'thy-button,[thy-button],[thyButton]', encapsulation: ViewEncapsulation.None, changeDetection: ChangeDetectionStrategy.OnPush, host: {
                        class: 'thy-btn btn'
                    }, standalone: true, imports: [NgIf, ThyIcon, NgClass], template: "<thy-icon *ngIf=\"svgIconName; else iconFont\" [thyIconName]=\"svgIconName\"></thy-icon>\n<ng-template #iconFont>\n  <i [ngClass]=\"iconClass\" *ngIf=\"iconClass\"></i>\n</ng-template>\n<ng-content></ng-content>\n" }]
        }], ctorParameters: () => [{ type: i0.ElementRef }, { type: i0.Renderer2 }], propDecorators: { thyButton: [{
                type: Input
            }], thyType: [{
                type: Input
            }], thyLoading: [{
                type: Input,
                args: [{ transform: booleanAttribute }]
            }], thyLoadingText: [{
                type: Input
            }], thySize: [{
                type: Input
            }], thyIcon: [{
                type: Input
            }], thyBlock: [{
                type: HostBinding,
                args: [`class.btn-block`]
            }, {
                type: Input,
                args: [{ transform: booleanAttribute }]
            }] } });

class ThyButtonModule {
    static { this.ɵfac = i0.ɵɵngDeclareFactory({ minVersion: "12.0.0", version: "17.2.3", ngImport: i0, type: ThyButtonModule, deps: [], target: i0.ɵɵFactoryTarget.NgModule }); }
    static { this.ɵmod = i0.ɵɵngDeclareNgModule({ minVersion: "14.0.0", version: "17.2.3", ngImport: i0, type: ThyButtonModule, imports: [CommonModule, ThyIconModule, ThyButton, ThyButtonIcon, ThyButtonGroup], exports: [ThyButton, ThyButtonIcon, ThyButtonGroup] }); }
    static { this.ɵinj = i0.ɵɵngDeclareInjector({ minVersion: "12.0.0", version: "17.2.3", ngImport: i0, type: ThyButtonModule, imports: [CommonModule, ThyIconModule, ThyButton, ThyButtonIcon] }); }
}
i0.ɵɵngDeclareClassMetadata({ minVersion: "12.0.0", version: "17.2.3", ngImport: i0, type: ThyButtonModule, decorators: [{
            type: NgModule,
            args: [{
                    imports: [CommonModule, ThyIconModule, ThyButton, ThyButtonIcon, ThyButtonGroup],
                    exports: [ThyButton, ThyButtonIcon, ThyButtonGroup]
                }]
        }] });

/**
 * Generated bundle index. Do not edit.
 */

export { ThyButton, ThyButtonGroup, ThyButtonIcon, ThyButtonModule };
//# sourceMappingURL=ngx-tethys-button.mjs.map
