import * as i0 from '@angular/core';
import { Injectable, Pipe, EventEmitter, booleanAttribute, Component, ChangeDetectionStrategy, HostBinding, Output, Input, ContentChild, ContentChildren, NgModule } from '@angular/core';
import { NgIf, NgClass, NgStyle, NgTemplateOutlet, NgFor, CommonModule } from '@angular/common';
import { isString } from 'ngx-tethys/util';
import { useHostRenderer } from '@tethys/cdk/dom';
import { ThyIcon, ThyIconModule } from 'ngx-tethys/icon';
import { UpdateHostClassService } from 'ngx-tethys/core';
import { Subject } from 'rxjs';
import { startWith, takeUntil } from 'rxjs/operators';

class ThyAvatarService {
    constructor() {
        this.ignoreAvatarSrcPaths = [`default.png`];
    }
}
class ThyDefaultAvatarService extends ThyAvatarService {
    /**
     * @deprecated the avatarSrcTransform method will be deprecated, please use srcTransform.
     */
    avatarSrcTransform(src, size) {
        return src;
    }
    srcTransform(src, size) {
        return src;
    }
    nameTransform(name) {
        return name;
    }
    static { this.ɵfac = i0.ɵɵngDeclareFactory({ minVersion: "12.0.0", version: "17.2.3", ngImport: i0, type: ThyDefaultAvatarService, deps: null, target: i0.ɵɵFactoryTarget.Injectable }); }
    static { this.ɵprov = i0.ɵɵngDeclareInjectable({ minVersion: "12.0.0", version: "17.2.3", ngImport: i0, type: ThyDefaultAvatarService }); }
}
i0.ɵɵngDeclareClassMetadata({ minVersion: "12.0.0", version: "17.2.3", ngImport: i0, type: ThyDefaultAvatarService, decorators: [{
            type: Injectable
        }] });

/**
 * `\u4e00`: https://www.compart.com/en/unicode/U+4E00
 * `\u9fa5`: https://www.compart.com/en/unicode/U+9FA5
 */
const UNIFIED_IDEOGRAPHS_REGEX = /^[\u4e00-\u9fa5]+$/;
const SET_OF_LETTERS_REGEX = /^[a-zA-Z\/ ]+$/;
/**
 * @private
 */
class AvatarShortNamePipe {
    transform(name) {
        if (!name) {
            return '';
        }
        name = name.trim();
        if (UNIFIED_IDEOGRAPHS_REGEX.test(name) && name.length > 2) {
            return name.slice(name.length - 2);
        }
        if (SET_OF_LETTERS_REGEX.test(name) && name.indexOf(' ') > 0) {
            const words = name.split(' ');
            return (words[0].slice(0, 1) + words[1].slice(0, 1)).toUpperCase();
        }
        return name.length > 2 ? name.slice(0, 2).toUpperCase() : name.toUpperCase();
    }
    static { this.ɵfac = i0.ɵɵngDeclareFactory({ minVersion: "12.0.0", version: "17.2.3", ngImport: i0, type: AvatarShortNamePipe, deps: [], target: i0.ɵɵFactoryTarget.Pipe }); }
    static { this.ɵpipe = i0.ɵɵngDeclarePipe({ minVersion: "14.0.0", version: "17.2.3", ngImport: i0, type: AvatarShortNamePipe, isStandalone: true, name: "avatarShortName" }); }
}
i0.ɵɵngDeclareClassMetadata({ minVersion: "12.0.0", version: "17.2.3", ngImport: i0, type: AvatarShortNamePipe, decorators: [{
            type: Pipe,
            args: [{
                    name: 'avatarShortName',
                    standalone: true
                }]
        }] });
/**
 * @private
 */
class AvatarBgColorPipe {
    transform(name) {
        if (!name) {
            return;
        }
        const colors = ['#56abfb', '#5dcfff', '#84e17e', '#73d897', '#ff9f73', '#fa8888', '#fb7fb7', '#9a7ef4', '#868af6'];
        const nameArray = name.split('');
        const code = name && name.length > 0
            ? nameArray.reduce(function (result, item) {
                result.value += item.charCodeAt(0);
                return result;
            }, { value: 0 }).value
            : 0;
        return {
            'background-color': colors[code % 9]
        };
    }
    static { this.ɵfac = i0.ɵɵngDeclareFactory({ minVersion: "12.0.0", version: "17.2.3", ngImport: i0, type: AvatarBgColorPipe, deps: [], target: i0.ɵɵFactoryTarget.Pipe }); }
    static { this.ɵpipe = i0.ɵɵngDeclarePipe({ minVersion: "14.0.0", version: "17.2.3", ngImport: i0, type: AvatarBgColorPipe, isStandalone: true, name: "avatarBgColor" }); }
}
i0.ɵɵngDeclareClassMetadata({ minVersion: "12.0.0", version: "17.2.3", ngImport: i0, type: AvatarBgColorPipe, decorators: [{
            type: Pipe,
            args: [{
                    name: 'avatarBgColor',
                    standalone: true
                }]
        }] });
/**
 * @private
 */
class AvatarSrcPipe {
    constructor(thyAvatarService) {
        this.thyAvatarService = thyAvatarService;
    }
    transform(src, size) {
        return this.thyAvatarService.srcTransform
            ? this.thyAvatarService.srcTransform(src, size)
            : this.thyAvatarService.avatarSrcTransform(src, size);
    }
    static { this.ɵfac = i0.ɵɵngDeclareFactory({ minVersion: "12.0.0", version: "17.2.3", ngImport: i0, type: AvatarSrcPipe, deps: [{ token: ThyAvatarService }], target: i0.ɵɵFactoryTarget.Pipe }); }
    static { this.ɵpipe = i0.ɵɵngDeclarePipe({ minVersion: "14.0.0", version: "17.2.3", ngImport: i0, type: AvatarSrcPipe, isStandalone: true, name: "thyAvatarSrc" }); }
}
i0.ɵɵngDeclareClassMetadata({ minVersion: "12.0.0", version: "17.2.3", ngImport: i0, type: AvatarSrcPipe, decorators: [{
            type: Pipe,
            args: [{
                    name: 'thyAvatarSrc',
                    standalone: true
                }]
        }], ctorParameters: () => [{ type: ThyAvatarService }] });
const AvatarPipes = [AvatarShortNamePipe, AvatarBgColorPipe, AvatarSrcPipe];

const sizeArray = [16, 22, 24, 28, 32, 36, 44, 48, 68, 110, 160];
const DEFAULT_SIZE = 36;
const thyAvatarSizeMap = {
    xxs: 22,
    xs: 24,
    sm: 32,
    md: 36,
    lg: 48
};
/**
 * 头像组件
 * @name thy-avatar
 * @order 10
 */
class ThyAvatar {
    /**
     * 头像路径地址, 默认为全路径，如果不是全路径，可以通过自定义服务 ThyAvatarService，重写 srcTransform 方法实现转换
     *
     */
    set thySrc(value) {
        this._setAvatarSrc(value);
    }
    /**
     * 人员名称（可设置自定义名称，需通过自定义服务 ThyAvatarService，重写 nameTransform 方法去实现转换）
     */
    set thyName(value) {
        // this._name = value;
        this._setAvatarName(value);
    }
    /**
     * 头像大小
     * @type 16 | 22 | 24 | 28 | 32 | 36 | 44 | 48 | 68 | 110 | 160 | xxs(22px) | xs(24px) | sm(32px) | md(36px) | lg(48px)
     * @default md
     */
    set thySize(value) {
        if (thyAvatarSizeMap[value]) {
            this._setAvatarSize(thyAvatarSizeMap[value]);
        }
        else {
            this._setAvatarSize(value * 1);
        }
    }
    /**
     * 已废弃，请使用 thyRemovable
     * @deprecated
     * @default false
     */
    set thyShowRemove(value) {
        this._showRemove = value;
    }
    /**
     * 是否展示移除按钮
     * @default false
     */
    set thyRemovable(value) {
        this._showRemove = value;
    }
    _setAvatarSize(size) {
        if (sizeArray.indexOf(size) > -1) {
            this._size = size;
        }
        else {
            this._size = this.findClosestSize(sizeArray, size);
        }
        this.hostRenderer.updateClass([`thy-avatar-${this._size}`]);
    }
    findClosestSize(sizeArray, currentSize) {
        let closestValue = sizeArray[0];
        let closestDifference = Math.abs(closestValue - currentSize);
        for (let i = 1; i < sizeArray.length; i++) {
            const currentDifference = Math.abs(sizeArray[i] - currentSize);
            if (currentDifference <= closestDifference) {
                closestValue = sizeArray[i];
                closestDifference = currentDifference;
            }
        }
        return closestValue;
    }
    _setAvatarSrc(src) {
        if (src && this.thyAvatarService.ignoreAvatarSrcPaths.indexOf(src) < 0) {
            this._src = src;
        }
        else {
            this._src = null;
        }
    }
    _setAvatarName(value) {
        const name = this.thyAvatarService.nameTransform(value);
        if (isString(name)) {
            this.avatarName = name;
        }
        else {
            this.avatarName = value;
            this.avatarNameSafeHtml = name;
        }
    }
    constructor(thyAvatarService, elementRef) {
        this.thyAvatarService = thyAvatarService;
        this.elementRef = elementRef;
        this._showRemove = false;
        this._isAvatar = true;
        /**
         * * 已废弃，请使用 thyRemove
         * @deprecated
         */
        this.thyOnRemove = new EventEmitter();
        /**
         *  移除按钮的事件, 当 thyRemovable 为 true 时起作用
         */
        this.thyRemove = new EventEmitter();
        /**
         *  头像 img 加载 error 时触发
         */
        this.thyError = new EventEmitter();
        this.hostRenderer = useHostRenderer();
    }
    ngOnInit() {
        if (!this._size) {
            this._setAvatarSize(DEFAULT_SIZE);
        }
        this.hostRenderer.updateClass([`thy-avatar-${this._size}`]);
    }
    remove($event) {
        this.thyOnRemove.emit($event);
        this.thyRemove.emit($event);
    }
    avatarImgError($event) {
        this._setAvatarSrc(null);
        this.thyError.emit($event);
    }
    static { this.ɵfac = i0.ɵɵngDeclareFactory({ minVersion: "12.0.0", version: "17.2.3", ngImport: i0, type: ThyAvatar, deps: [{ token: ThyAvatarService }, { token: i0.ElementRef }], target: i0.ɵɵFactoryTarget.Component }); }
    static { this.ɵcmp = i0.ɵɵngDeclareComponent({ minVersion: "16.1.0", version: "17.2.3", type: ThyAvatar, isStandalone: true, selector: "thy-avatar", inputs: { thyShowName: ["thyShowName", "thyShowName", booleanAttribute], thySrc: "thySrc", thyName: "thyName", thySize: "thySize", thyShowRemove: ["thyShowRemove", "thyShowRemove", booleanAttribute], thyRemovable: ["thyRemovable", "thyRemovable", booleanAttribute], thyImgClass: "thyImgClass", thyDisabled: ["thyDisabled", "thyDisabled", booleanAttribute], thyLoading: "thyLoading", thyFetchPriority: "thyFetchPriority" }, outputs: { thyOnRemove: "thyOnRemove", thyRemove: "thyRemove", thyError: "thyError" }, host: { properties: { "class.thy-avatar": "this._isAvatar" } }, ngImport: i0, template: "<img\n  *ngIf=\"_src; else noSrc\"\n  [src]=\"_src | thyAvatarSrc: _size\"\n  class=\"avatar-avatar\"\n  [ngClass]=\"thyImgClass\"\n  alt=\"{{ avatarName || '' }}\"\n  [attr.loading]=\"thyLoading\"\n  [attr.fetchpriority]=\"thyFetchPriority\"\n  (error)=\"avatarImgError($event)\"\n/>\n\n<ng-template #noSrc>\n  <span class=\"avatar-default\" [ngStyle]=\"avatarName | avatarBgColor\">\n    <div>{{ avatarName | avatarShortName }}</div>\n  </span>\n</ng-template>\n\n<ng-template [ngIf]=\"thyShowName\">\n  <div *ngIf=\"avatarNameSafeHtml; else noAvatarNameSafeHtml\" class=\"avatar-name\" [innerHtml]=\"avatarNameSafeHtml\"></div>\n  <ng-template #noAvatarNameSafeHtml>\n    <div class=\"avatar-name\">{{ avatarName }}</div>\n  </ng-template>\n</ng-template>\n\n<a *ngIf=\"_showRemove\" (click)=\"remove($event)\" href=\"javascript:;\" class=\"remove-link avatar-remove\">\n  <thy-icon class=\"remove-link-icon\" thyIconName=\"close-circle-bold-fill\"></thy-icon>\n</a>\n<div *ngIf=\"thyDisabled\" class=\"thy-avatar-disabled\">\n  <thy-icon class=\"thy-avatar-disabled-icon\" thyIconName=\"ban\"></thy-icon>\n</div>\n", dependencies: [{ kind: "directive", type: NgIf, selector: "[ngIf]", inputs: ["ngIf", "ngIfThen", "ngIfElse"] }, { kind: "directive", type: NgClass, selector: "[ngClass]", inputs: ["class", "ngClass"] }, { kind: "directive", type: NgStyle, selector: "[ngStyle]", inputs: ["ngStyle"] }, { kind: "component", type: ThyIcon, selector: "thy-icon, [thy-icon]", inputs: ["thyIconType", "thyTwotoneColor", "thyIconName", "thyIconRotate", "thyIconSet", "thyIconLegging", "thyIconLinearGradient"] }, { kind: "pipe", type: AvatarShortNamePipe, name: "avatarShortName" }, { kind: "pipe", type: AvatarBgColorPipe, name: "avatarBgColor" }, { kind: "pipe", type: AvatarSrcPipe, name: "thyAvatarSrc" }], changeDetection: i0.ChangeDetectionStrategy.OnPush }); }
}
i0.ɵɵngDeclareClassMetadata({ minVersion: "12.0.0", version: "17.2.3", ngImport: i0, type: ThyAvatar, decorators: [{
            type: Component,
            args: [{ selector: 'thy-avatar', changeDetection: ChangeDetectionStrategy.OnPush, standalone: true, imports: [NgIf, NgClass, NgStyle, ThyIcon, AvatarShortNamePipe, AvatarBgColorPipe, AvatarSrcPipe], template: "<img\n  *ngIf=\"_src; else noSrc\"\n  [src]=\"_src | thyAvatarSrc: _size\"\n  class=\"avatar-avatar\"\n  [ngClass]=\"thyImgClass\"\n  alt=\"{{ avatarName || '' }}\"\n  [attr.loading]=\"thyLoading\"\n  [attr.fetchpriority]=\"thyFetchPriority\"\n  (error)=\"avatarImgError($event)\"\n/>\n\n<ng-template #noSrc>\n  <span class=\"avatar-default\" [ngStyle]=\"avatarName | avatarBgColor\">\n    <div>{{ avatarName | avatarShortName }}</div>\n  </span>\n</ng-template>\n\n<ng-template [ngIf]=\"thyShowName\">\n  <div *ngIf=\"avatarNameSafeHtml; else noAvatarNameSafeHtml\" class=\"avatar-name\" [innerHtml]=\"avatarNameSafeHtml\"></div>\n  <ng-template #noAvatarNameSafeHtml>\n    <div class=\"avatar-name\">{{ avatarName }}</div>\n  </ng-template>\n</ng-template>\n\n<a *ngIf=\"_showRemove\" (click)=\"remove($event)\" href=\"javascript:;\" class=\"remove-link avatar-remove\">\n  <thy-icon class=\"remove-link-icon\" thyIconName=\"close-circle-bold-fill\"></thy-icon>\n</a>\n<div *ngIf=\"thyDisabled\" class=\"thy-avatar-disabled\">\n  <thy-icon class=\"thy-avatar-disabled-icon\" thyIconName=\"ban\"></thy-icon>\n</div>\n" }]
        }], ctorParameters: () => [{ type: ThyAvatarService }, { type: i0.ElementRef }], propDecorators: { _isAvatar: [{
                type: HostBinding,
                args: ['class.thy-avatar']
            }], thyOnRemove: [{
                type: Output
            }], thyRemove: [{
                type: Output
            }], thyError: [{
                type: Output
            }], thyShowName: [{
                type: Input,
                args: [{ transform: booleanAttribute }]
            }], thySrc: [{
                type: Input
            }], thyName: [{
                type: Input
            }], thySize: [{
                type: Input
            }], thyShowRemove: [{
                type: Input,
                args: [{ transform: booleanAttribute }]
            }], thyRemovable: [{
                type: Input,
                args: [{ transform: booleanAttribute }]
            }], thyImgClass: [{
                type: Input
            }], thyDisabled: [{
                type: Input,
                args: [{ transform: booleanAttribute }]
            }], thyLoading: [{
                type: Input
            }], thyFetchPriority: [{
                type: Input
            }] } });

const THY_AVATAR_ITEM_SPACE = 4;
const THY_OVERLAP_AVATAR_ITEM_SPACE = -8;
/**
 * 头像列表组件
 * @name thy-avatar-list
 * @order 20
 */
class ThyAvatarList {
    constructor() {
        this.overlapMode = false;
        this.avatarItems = [];
        this.ngUnsubscribe$ = new Subject();
        /**
         * 头像大小
         * @type 16 | 22 | 24 | 28 | 32 | 36 | 44 | 48 | 68 | 110 | 160 | xxs(22px) | xs(24px) | sm(32px) | md(36px) | lg(48px)
         * @default 36
         */
        this.thyAvatarSize = DEFAULT_SIZE;
    }
    ngOnChanges(changes) {
        if (changes.thyAvatarSize && !changes.thyAvatarSize.firstChange) {
            this.updateAvatarItems();
        }
        if (changes.thyMode) {
            this.overlapMode = changes.thyMode.currentValue === "overlap" /* ThyAvatarListMode.overlap */;
            if (!changes.thyMode.firstChange) {
                this.updateAvatarItems();
            }
        }
    }
    ngAfterContentInit() {
        this.avatarComponents.changes.pipe(startWith(this.avatarComponents), takeUntil(this.ngUnsubscribe$)).subscribe(() => {
            this.updateAvatarItems();
        });
    }
    updateAvatarItems() {
        this.avatarItems = this.avatarComponents.toArray();
        this.avatarItems.forEach((item, index) => {
            item.thySize = this.thyAvatarSize;
            item.elementRef.nativeElement.style.zIndex = this.avatarItems.length - index;
        });
    }
    ngOnDestroy() {
        this.ngUnsubscribe$.next();
        this.ngUnsubscribe$.complete();
    }
    static { this.ɵfac = i0.ɵɵngDeclareFactory({ minVersion: "12.0.0", version: "17.2.3", ngImport: i0, type: ThyAvatarList, deps: [], target: i0.ɵɵFactoryTarget.Component }); }
    static { this.ɵcmp = i0.ɵɵngDeclareComponent({ minVersion: "14.0.0", version: "17.2.3", type: ThyAvatarList, isStandalone: true, selector: "thy-avatar-list", inputs: { thyMode: "thyMode", thyAvatarSize: "thyAvatarSize" }, host: { properties: { "class.thy-avatar-list-overlap": "this.overlapMode" }, classAttribute: "thy-avatar-list" }, providers: [UpdateHostClassService], queries: [{ propertyName: "append", first: true, predicate: ["append"], descendants: true }, { propertyName: "avatarComponents", predicate: ThyAvatar }], usesOnChanges: true, ngImport: i0, template: "<ng-content></ng-content>\n\n<div *ngIf=\"append\" class=\"thy-avatar-append\">\n  <ng-container [ngTemplateOutlet]=\"append\"></ng-container>\n</div>\n", dependencies: [{ kind: "directive", type: NgIf, selector: "[ngIf]", inputs: ["ngIf", "ngIfThen", "ngIfElse"] }, { kind: "directive", type: NgTemplateOutlet, selector: "[ngTemplateOutlet]", inputs: ["ngTemplateOutletContext", "ngTemplateOutlet", "ngTemplateOutletInjector"] }] }); }
}
i0.ɵɵngDeclareClassMetadata({ minVersion: "12.0.0", version: "17.2.3", ngImport: i0, type: ThyAvatarList, decorators: [{
            type: Component,
            args: [{ selector: 'thy-avatar-list', host: {
                        class: 'thy-avatar-list'
                    }, providers: [UpdateHostClassService], standalone: true, imports: [NgFor, NgStyle, ThyAvatar, NgIf, NgClass, NgTemplateOutlet], template: "<ng-content></ng-content>\n\n<div *ngIf=\"append\" class=\"thy-avatar-append\">\n  <ng-container [ngTemplateOutlet]=\"append\"></ng-container>\n</div>\n" }]
        }], ctorParameters: () => [], propDecorators: { overlapMode: [{
                type: HostBinding,
                args: ['class.thy-avatar-list-overlap']
            }], thyMode: [{
                type: Input
            }], thyAvatarSize: [{
                type: Input
            }], append: [{
                type: ContentChild,
                args: ['append', { static: false }]
            }], avatarComponents: [{
                type: ContentChildren,
                args: [ThyAvatar]
            }] } });

class ThyAvatarModule {
    static { this.ɵfac = i0.ɵɵngDeclareFactory({ minVersion: "12.0.0", version: "17.2.3", ngImport: i0, type: ThyAvatarModule, deps: [], target: i0.ɵɵFactoryTarget.NgModule }); }
    static { this.ɵmod = i0.ɵɵngDeclareNgModule({ minVersion: "14.0.0", version: "17.2.3", ngImport: i0, type: ThyAvatarModule, imports: [CommonModule, ThyIconModule, ThyAvatar, ThyAvatarList, AvatarShortNamePipe, AvatarBgColorPipe, AvatarSrcPipe], exports: [ThyAvatar, ThyAvatarList, AvatarShortNamePipe, AvatarBgColorPipe, AvatarSrcPipe] }); }
    static { this.ɵinj = i0.ɵɵngDeclareInjector({ minVersion: "12.0.0", version: "17.2.3", ngImport: i0, type: ThyAvatarModule, providers: [
            {
                provide: ThyAvatarService,
                useClass: ThyDefaultAvatarService
            }
        ], imports: [CommonModule, ThyIconModule, ThyAvatar, ThyAvatarList] }); }
}
i0.ɵɵngDeclareClassMetadata({ minVersion: "12.0.0", version: "17.2.3", ngImport: i0, type: ThyAvatarModule, decorators: [{
            type: NgModule,
            args: [{
                    imports: [CommonModule, ThyIconModule, ThyAvatar, ThyAvatarList, AvatarPipes],
                    providers: [
                        {
                            provide: ThyAvatarService,
                            useClass: ThyDefaultAvatarService
                        }
                    ],
                    exports: [ThyAvatar, ThyAvatarList, AvatarPipes]
                }]
        }] });

/**
 * Generated bundle index. Do not edit.
 */

export { AvatarBgColorPipe, AvatarPipes, AvatarShortNamePipe, AvatarSrcPipe, DEFAULT_SIZE, THY_AVATAR_ITEM_SPACE, THY_OVERLAP_AVATAR_ITEM_SPACE, ThyAvatar, ThyAvatarList, ThyAvatarModule, ThyAvatarService, ThyDefaultAvatarService, thyAvatarSizeMap };
//# sourceMappingURL=ngx-tethys-avatar.mjs.map
