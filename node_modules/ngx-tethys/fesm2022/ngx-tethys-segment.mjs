import * as i0 from '@angular/core';
import { InjectionToken, booleanAttribute, Component, ChangeDetectionStrategy, Optional, Inject, Input, HostBinding, EventEmitter, numberAttribute, ViewEncapsulation, ContentChildren, Output, NgModule } from '@angular/core';
import { NgClass, NgIf, CommonModule } from '@angular/common';
import { ThyIcon, ThyIconModule } from 'ngx-tethys/icon';
import { ThyTooltipModule } from 'ngx-tethys/tooltip';
import { thumbMotion } from 'ngx-tethys/core';
import { assertIconOnly } from 'ngx-tethys/util';
import { Subject, fromEvent } from 'rxjs';
import { takeUntil } from 'rxjs/operators';
import { takeUntilDestroyed } from '@angular/core/rxjs-interop';

const THY_SEGMENTED_COMPONENT = new InjectionToken('THY_SEGMENTED_COMPONENT');

/**
 * 分段控制器的选项组件
 * @name thy-segment-item,[thy-segment-item]
 */
class ThySegmentItem {
    constructor(elementRef, ngZone, cdr, renderer, parent) {
        this.elementRef = elementRef;
        this.ngZone = ngZone;
        this.cdr = cdr;
        this.renderer = renderer;
        this.parent = parent;
        /**
         * 是否禁用该选项
         */
        this.thyDisabled = false;
        this.destroy$ = new Subject();
        ngZone.runOutsideAngular(() => fromEvent(elementRef.nativeElement, 'click')
            .pipe(takeUntil(this.destroy$))
            .subscribe((event) => {
            if (!this.thyDisabled && !this.parent.thyDisabled && this.parent.selectedItem && this.parent.selectedItem !== this) {
                ngZone.run(() => {
                    this.parent.selectedItem.unselect();
                    this.parent.changeSelectedItem(this, event);
                });
            }
        }));
    }
    ngAfterViewInit() {
        const labelDiv = this.elementRef.nativeElement.children[0];
        this.isOnlyIcon = assertIconOnly(labelDiv) && this.parent.thyMode === 'inline';
        this.cdr.detectChanges();
        this.wrapSpanForText(labelDiv.childNodes);
    }
    select() {
        this.elementRef.nativeElement.classList.add('active');
    }
    unselect() {
        this.elementRef.nativeElement.classList.remove('active');
    }
    wrapSpanForText(nodes) {
        nodes.forEach(node => {
            if (node.nodeName === '#text') {
                const span = this.renderer.createElement('span');
                const parent = this.renderer.parentNode(node);
                this.renderer.insertBefore(parent, span, node);
                this.renderer.appendChild(span, node);
            }
            if (node.nodeName === '#text' || node.nodeName === 'SPAN') {
                this.isWithText = true;
                this.cdr.detectChanges();
            }
        });
    }
    ngOnDestroy() {
        this.destroy$.next();
        this.destroy$.complete();
    }
    static { this.ɵfac = i0.ɵɵngDeclareFactory({ minVersion: "12.0.0", version: "17.2.3", ngImport: i0, type: ThySegmentItem, deps: [{ token: i0.ElementRef }, { token: i0.NgZone }, { token: i0.ChangeDetectorRef }, { token: i0.Renderer2 }, { token: THY_SEGMENTED_COMPONENT, optional: true }], target: i0.ɵɵFactoryTarget.Component }); }
    static { this.ɵcmp = i0.ɵɵngDeclareComponent({ minVersion: "16.1.0", version: "17.2.3", type: ThySegmentItem, isStandalone: true, selector: "thy-segment-item,[thy-segment-item]", inputs: { thyValue: "thyValue", thyIcon: "thyIcon", thyDisabled: ["thyDisabled", "thyDisabled", booleanAttribute] }, host: { properties: { "class.disabled": "this.thyDisabled" }, classAttribute: "thy-segment-item" }, ngImport: i0, template: "<div\n  class=\"thy-segment-item-label\"\n  [ngClass]=\"{\n    'thy-segment-item-label-only-icon': isOnlyIcon\n  }\"\n>\n  <thy-icon *ngIf=\"thyIcon\" class=\"thy-segment-item-icon\" [ngClass]=\"{ 'icon-with-text': isWithText }\" [thyIconName]=\"thyIcon\"></thy-icon>\n  <ng-content></ng-content>\n</div>\n", dependencies: [{ kind: "directive", type: NgClass, selector: "[ngClass]", inputs: ["class", "ngClass"] }, { kind: "directive", type: NgIf, selector: "[ngIf]", inputs: ["ngIf", "ngIfThen", "ngIfElse"] }, { kind: "component", type: ThyIcon, selector: "thy-icon, [thy-icon]", inputs: ["thyIconType", "thyTwotoneColor", "thyIconName", "thyIconRotate", "thyIconSet", "thyIconLegging", "thyIconLinearGradient"] }], changeDetection: i0.ChangeDetectionStrategy.OnPush }); }
}
i0.ɵɵngDeclareClassMetadata({ minVersion: "12.0.0", version: "17.2.3", ngImport: i0, type: ThySegmentItem, decorators: [{
            type: Component,
            args: [{ selector: 'thy-segment-item,[thy-segment-item]', changeDetection: ChangeDetectionStrategy.OnPush, host: {
                        class: 'thy-segment-item'
                    }, standalone: true, imports: [NgClass, NgIf, ThyIcon], template: "<div\n  class=\"thy-segment-item-label\"\n  [ngClass]=\"{\n    'thy-segment-item-label-only-icon': isOnlyIcon\n  }\"\n>\n  <thy-icon *ngIf=\"thyIcon\" class=\"thy-segment-item-icon\" [ngClass]=\"{ 'icon-with-text': isWithText }\" [thyIconName]=\"thyIcon\"></thy-icon>\n  <ng-content></ng-content>\n</div>\n" }]
        }], ctorParameters: () => [{ type: i0.ElementRef }, { type: i0.NgZone }, { type: i0.ChangeDetectorRef }, { type: i0.Renderer2 }, { type: undefined, decorators: [{
                    type: Optional
                }, {
                    type: Inject,
                    args: [THY_SEGMENTED_COMPONENT]
                }] }], propDecorators: { thyValue: [{
                type: Input
            }], thyIcon: [{
                type: Input
            }], thyDisabled: [{
                type: Input,
                args: [{ transform: booleanAttribute }]
            }, {
                type: HostBinding,
                args: [`class.disabled`]
            }] } });

/**
 * 分段控制器组件
 * @name thy-segment
 */
class ThySegment {
    /**
     * 选中选项的索引
     */
    set thyActiveIndex(value) {
        this.newActiveIndex = value;
        if (value < 0 || value === this.activeIndex) {
            return;
        }
        setTimeout(() => {
            const selectedItem = this.options?.get(this.activeIndex);
            if (selectedItem) {
                selectedItem.unselect();
                this.changeSelectedItem(this.options.get(value));
            }
            else {
                this.activeIndex = value;
            }
        });
    }
    constructor(cdr, destroyRef) {
        this.cdr = cdr;
        this.destroyRef = destroyRef;
        /**
         * 大小
         * @type xs | sm | md | default
         */
        this.thySize = 'default';
        /**
         * 模式
         * @type block | inline
         */
        this.thyMode = 'block';
        /**
         * 是否禁用分段控制器
         */
        this.thyDisabled = false;
        /**
         * 选项被选中的回调事件
         */
        this.thySelectChange = new EventEmitter();
        this.animationState = null;
        this.transitionedTo = null;
    }
    ngAfterContentInit() {
        this.selectedItem = this.options.get(this.newActiveIndex) || this.options.get(0);
        this.selectedItem?.select();
        this.options.changes.pipe(takeUntilDestroyed(this.destroyRef)).subscribe(() => {
            this.options.forEach(item => {
                item.unselect();
            });
            this.selectedItem = this.options.get(this.newActiveIndex) || this.options.get(0);
            this.selectedItem?.select();
            this.cdr.detectChanges();
        });
    }
    changeSelectedItem(item, event) {
        this.animationState = {
            value: 'from',
            params: getThumbAnimationProps(this.options?.get(this.activeIndex || 0)?.elementRef.nativeElement)
        };
        this.selectedItem = null;
        this.cdr.detectChanges();
        this.animationState = {
            value: 'to',
            params: getThumbAnimationProps(item.elementRef.nativeElement)
        };
        this.transitionedTo = item;
        this.activeIndex = this.options?.toArray().findIndex(option => {
            return option.thyValue === item?.thyValue;
        });
        this.thySelectChange.emit({ event: event, value: item.thyValue, activeIndex: this.activeIndex });
        this.cdr.detectChanges();
    }
    handleThumbAnimationDone(event) {
        if (event.fromState === 'from') {
            this.selectedItem = this.transitionedTo;
            this.selectedItem?.select();
            this.transitionedTo = null;
            this.animationState = null;
            this.cdr.detectChanges();
        }
    }
    static { this.ɵfac = i0.ɵɵngDeclareFactory({ minVersion: "12.0.0", version: "17.2.3", ngImport: i0, type: ThySegment, deps: [{ token: i0.ChangeDetectorRef }, { token: i0.DestroyRef }], target: i0.ɵɵFactoryTarget.Component }); }
    static { this.ɵcmp = i0.ɵɵngDeclareComponent({ minVersion: "16.1.0", version: "17.2.3", type: ThySegment, isStandalone: true, selector: "thy-segment", inputs: { thySize: "thySize", thyMode: "thyMode", thyDisabled: ["thyDisabled", "thyDisabled", booleanAttribute], thyActiveIndex: ["thyActiveIndex", "thyActiveIndex", numberAttribute] }, outputs: { thySelectChange: "thySelectChange" }, host: { properties: { "class.thy-segment-xs": "thySize === 'xs'", "class.thy-segment-sm": "thySize === 'sm'", "class.thy-segment-md": "thySize === 'md'", "class.thy-segment-default": "!thySize || thySize === 'default'", "class.thy-segment-block": "thyMode === 'block'", "class.disabled": "this.thyDisabled" }, classAttribute: "thy-segment" }, providers: [
            {
                provide: THY_SEGMENTED_COMPONENT,
                useExisting: ThySegment
            }
        ], queries: [{ propertyName: "options", predicate: ThySegmentItem }], exportAs: ["thySegment"], ngImport: i0, template: "<div class=\"thy-segment-group\">\n  <div\n    *ngIf=\"animationState\"\n    class=\"thy-segment-thumb thy-segment-thumb-motion\"\n    [@thumbMotion]=\"animationState\"\n    (@thumbMotion.done)=\"handleThumbAnimationDone($event)\"\n  ></div>\n\n  <ng-content></ng-content>\n</div>\n", dependencies: [{ kind: "directive", type: NgIf, selector: "[ngIf]", inputs: ["ngIf", "ngIfThen", "ngIfElse"] }], animations: [thumbMotion], changeDetection: i0.ChangeDetectionStrategy.OnPush, encapsulation: i0.ViewEncapsulation.None }); }
}
i0.ɵɵngDeclareClassMetadata({ minVersion: "12.0.0", version: "17.2.3", ngImport: i0, type: ThySegment, decorators: [{
            type: Component,
            args: [{ selector: 'thy-segment', exportAs: 'thySegment', animations: [thumbMotion], encapsulation: ViewEncapsulation.None, changeDetection: ChangeDetectionStrategy.OnPush, providers: [
                        {
                            provide: THY_SEGMENTED_COMPONENT,
                            useExisting: ThySegment
                        }
                    ], host: {
                        class: 'thy-segment',
                        '[class.thy-segment-xs]': `thySize === 'xs'`,
                        '[class.thy-segment-sm]': `thySize === 'sm'`,
                        '[class.thy-segment-md]': `thySize === 'md'`,
                        '[class.thy-segment-default]': `!thySize || thySize === 'default'`,
                        '[class.thy-segment-block]': `thyMode === 'block'`
                    }, standalone: true, imports: [NgIf], template: "<div class=\"thy-segment-group\">\n  <div\n    *ngIf=\"animationState\"\n    class=\"thy-segment-thumb thy-segment-thumb-motion\"\n    [@thumbMotion]=\"animationState\"\n    (@thumbMotion.done)=\"handleThumbAnimationDone($event)\"\n  ></div>\n\n  <ng-content></ng-content>\n</div>\n" }]
        }], ctorParameters: () => [{ type: i0.ChangeDetectorRef }, { type: i0.DestroyRef }], propDecorators: { options: [{
                type: ContentChildren,
                args: [ThySegmentItem]
            }], thySize: [{
                type: Input
            }], thyMode: [{
                type: Input
            }], thyDisabled: [{
                type: Input,
                args: [{ transform: booleanAttribute }]
            }, {
                type: HostBinding,
                args: [`class.disabled`]
            }], thyActiveIndex: [{
                type: Input,
                args: [{ transform: numberAttribute }]
            }], thySelectChange: [{
                type: Output
            }] } });
function getThumbAnimationProps(element) {
    return {
        transform: element.offsetLeft,
        width: element.clientWidth
    };
}

class ThySegmentModule {
    static { this.ɵfac = i0.ɵɵngDeclareFactory({ minVersion: "12.0.0", version: "17.2.3", ngImport: i0, type: ThySegmentModule, deps: [], target: i0.ɵɵFactoryTarget.NgModule }); }
    static { this.ɵmod = i0.ɵɵngDeclareNgModule({ minVersion: "14.0.0", version: "17.2.3", ngImport: i0, type: ThySegmentModule, imports: [CommonModule, ThyIconModule, ThyTooltipModule, ThySegment, ThySegmentItem], exports: [ThySegment, ThySegmentItem] }); }
    static { this.ɵinj = i0.ɵɵngDeclareInjector({ minVersion: "12.0.0", version: "17.2.3", ngImport: i0, type: ThySegmentModule, imports: [CommonModule, ThyIconModule, ThyTooltipModule, ThySegmentItem] }); }
}
i0.ɵɵngDeclareClassMetadata({ minVersion: "12.0.0", version: "17.2.3", ngImport: i0, type: ThySegmentModule, decorators: [{
            type: NgModule,
            args: [{
                    imports: [CommonModule, ThyIconModule, ThyTooltipModule, ThySegment, ThySegmentItem],
                    exports: [ThySegment, ThySegmentItem]
                }]
        }] });

/**
 * Generated bundle index. Do not edit.
 */

export { ThySegment, ThySegmentItem, ThySegmentModule };
//# sourceMappingURL=ngx-tethys-segment.mjs.map
