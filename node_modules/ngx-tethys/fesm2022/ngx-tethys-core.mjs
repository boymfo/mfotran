import { coerceBooleanProperty, coerceCssPixelValue, coerceNumberValue, dom, isNumber, ESCAPE, coerceArray, concatArray, helpers } from 'ngx-tethys/util';
import { coerceBooleanProperty as coerceBooleanProperty$1, coerceNumberProperty } from '@angular/cdk/coercion';
import { Subject, fromEvent, Observable, of, throwError, BehaviorSubject, from } from 'rxjs';
import { FocusMonitor } from '@angular/cdk/a11y';
import * as i0 from '@angular/core';
import { inject, ElementRef, Injectable, Inject, EventEmitter, TemplateRef, Directive, InjectionToken, isDevMode } from '@angular/core';
import { takeUntil, take, finalize, filter, catchError, shareReplay, exhaustMap, map, distinctUntilChanged } from 'rxjs/operators';
import { ThyClickDispatcher } from '@tethys/cdk/event';
export * from '@tethys/cdk/event';
import { DOCUMENT } from '@angular/common';
import { trigger, transition, style, animate, state } from '@angular/animations';
import { normalizePassiveListenerOptions } from '@angular/cdk/platform';
import { GlobalPositionStrategy, OverlayConfig } from '@angular/cdk/overlay';
import { TemplatePortal, ComponentPortal, CdkPortalOutlet } from '@angular/cdk/portal';
import { __decorate, __metadata } from 'tslib';

class AnonymousClass {
}
class MixinBase {
}

/**
 * @deprecated Please use {transform: booleanAttribute} in Input instead
 */
function InputBoolean() {
    return makePropDecorator('InputBoolean', coerceBooleanProperty);
}
function InputCssPixel() {
    return makePropDecorator('InputCssPixel', coerceCssPixelValue);
}
/**
 * @deprecated Please use {transform: numberAttribute} in Input instead
 */
function InputNumber() {
    return makePropDecorator('InputNumber', (value) => {
        return coerceNumberValue(value, null);
    });
}
function makePropDecorator(name, transform, initialize) {
    function propDecorator(target, propName, originalDescriptor) {
        initialize && initialize(target, propName);
        const privatePropName = `$$__${propName}`;
        if (typeof ngDevMode === 'undefined' || ngDevMode) {
            if (Object.prototype.hasOwnProperty.call(target, privatePropName)) {
                console.warn(`The property "${privatePropName}" is already exist, it will be overrided by ${name} decorator.`);
            }
        }
        Object.defineProperty(target, privatePropName, {
            configurable: true,
            writable: true
        });
        return {
            get() {
                return originalDescriptor && originalDescriptor.get ? originalDescriptor.get.bind(this)() : this[privatePropName];
            },
            set(value) {
                const finalValue = transform(value, target);
                if (originalDescriptor && originalDescriptor.set) {
                    originalDescriptor.set.bind(this)(finalValue);
                }
                this[privatePropName] = finalValue;
            }
        };
    }
    return propDecorator;
}

/** Mixin to augment a directive with a `disable` property. */
function mixinDisabled(base) {
    return class extends base {
        get thyDisabled() {
            return this._thyDisabled;
        }
        set thyDisabled(value) {
            this._thyDisabled = coerceBooleanProperty$1(value);
        }
        constructor(...args) {
            super(...args);
            this._thyDisabled = false;
        }
    };
}

function mixinInitialized(base) {
    return class Mixin extends base {
        constructor(...args) {
            super(...args);
            this.initialized = false;
        }
        ngOnInit() {
            this.initialized = true;
        }
    };
}

function mixinLoadingDone(base) {
    return class Mixin extends base {
        setLoadingDone(value) {
            this.loadingDone = value;
        }
        constructor(...args) {
            super(...args);
            this.loadingDone = false;
        }
    };
}

/** Mixin to augment a directive with a `tabIndex` property. */
function mixinTabIndex(base, defaultTabIndex = 0) {
    return class extends base {
        get tabIndex() {
            return this.thyDisabled ? -1 : this._tabIndex;
        }
        set tabIndex(value) {
            this._tabIndex = value != null ? coerceNumberProperty(value) : this.defaultTabIndex;
        }
        constructor(...args) {
            super(...args);
            this._tabIndex = defaultTabIndex;
            this.defaultTabIndex = defaultTabIndex;
        }
    };
}
const TabIndexMixinBase = mixinTabIndex(mixinDisabled(MixinBase));

/**
 * Mixin to augment a directive with a `disableRipple` property.
 * @deprecated please use `takeUntilDestroyed`
 */
function mixinUnsubscribe(base) {
    return class Mixin extends base {
        constructor(...args) {
            super(...args);
            this.ngUnsubscribe$ = new Subject();
        }
        ngUnsubscribeReinitialize() {
            this.ngUnsubscribe$.next();
            this.ngUnsubscribe$.complete();
            this.ngUnsubscribe$ = new Subject();
        }
        ngOnDestroy() {
            this.ngUnsubscribe$.next();
            this.ngUnsubscribe$.complete();
        }
    };
}

class AbstractControlValueAccessor {
    constructor() {
        this.onChangeFn = () => { };
        this.onTouchedFn = () => { };
        this.__onBlurValidation = () => { };
    }
    registerOnChange(fn) {
        this.onChangeFn = fn;
    }
    registerOnTouched(fn) {
        this.onTouchedFn = () => {
            fn();
            this.__onBlurValidation();
        };
    }
}

class HostFocusControl {
    constructor(checkChildren = true, element) {
        this.ngUnsubscribe$ = new Subject();
        this.elementRef = inject(ElementRef);
        this.focusMonitor = inject(FocusMonitor);
        this.focusChanged = () => { };
        this.monitorElement = element || this.elementRef.nativeElement;
        this.focusMonitor
            .monitor(this.monitorElement, checkChildren)
            .pipe(takeUntil(this.ngUnsubscribe$))
            .subscribe((origin) => {
            this.focusChanged(origin);
        });
    }
    focusVia(element, origin, options) {
        this.focusMonitor.focusVia(element, origin, options);
    }
    destroy() {
        this.ngUnsubscribe$.next();
        this.ngUnsubscribe$.complete();
        this.focusMonitor.stopMonitoring(this.monitorElement);
    }
}
function useHostFocusControl(checkChildren, element) {
    return new HostFocusControl(checkChildren, element);
}

const TabIndexDisabledControlValueAccessorMixin = mixinTabIndex(mixinDisabled(AbstractControlValueAccessor));

class ScrollToService {
    static _customScrollToElement(targetElement, containerElement) {
        const containerOffset = dom.getElementOffset(containerElement);
        const targetOffset = dom.getElementOffset(targetElement);
        // 选择项在顶部隐藏的偏移量 > 0 表示被顶部遮住隐藏
        const topOffset = containerOffset.top - targetOffset.top;
        // 选择项在底部隐藏的偏移量 > 0 表示被底部遮住隐藏
        const bottomOffset = targetOffset.top + targetOffset.height - (containerOffset.top + containerOffset.height);
        // 隐藏后滚动展示 targetElement 的偏移量，避免紧挨着头部或者底部
        const viewOffset = targetOffset.height;
        if (topOffset > 0) {
            containerElement.scrollTop = containerElement.scrollTop - topOffset - viewOffset;
        }
        else if (bottomOffset > 0) {
            containerElement.scrollTop = containerElement.scrollTop + bottomOffset + viewOffset;
        }
    }
    static scrollToElement(targetElement, containerElement) {
        if (targetElement) {
            this._customScrollToElement(targetElement, containerElement);
            // if (targetElement.scrollIntoView) {
            //     targetElement.scrollIntoView({
            //         behavior: 'smooth',
            //         block: 'center'
            //     });
            // } else {
            //     this._customScrollToElement(targetElement, containerElement);
            // }
        }
    }
}

class ThyClickPositioner {
    constructor(clickDispatcher) {
        this.clickDispatcher = clickDispatcher;
        this.lastPosition = null;
        this.initialized = false;
    }
    get lastClickPosition() {
        return this.lastPosition;
    }
    runTaskUseLastPosition(task) {
        setTimeout(() => {
            task(this.lastClickPosition);
        });
    }
    initialize() {
        if (this.initialized) {
            return;
        }
        this.initialized = true;
        this.clickDispatcher.clicked(0).subscribe((event) => {
            this.lastPosition = { x: event.clientX, y: event.clientY };
        });
    }
    static { this.ɵfac = i0.ɵɵngDeclareFactory({ minVersion: "12.0.0", version: "17.2.3", ngImport: i0, type: ThyClickPositioner, deps: [{ token: ThyClickDispatcher }], target: i0.ɵɵFactoryTarget.Injectable }); }
    static { this.ɵprov = i0.ɵɵngDeclareInjectable({ minVersion: "12.0.0", version: "17.2.3", ngImport: i0, type: ThyClickPositioner, providedIn: 'root' }); }
}
i0.ɵɵngDeclareClassMetadata({ minVersion: "12.0.0", version: "17.2.3", ngImport: i0, type: ThyClickPositioner, decorators: [{
            type: Injectable,
            args: [{
                    providedIn: 'root'
                }]
        }], ctorParameters: () => [{ type: ThyClickDispatcher }] });

const availablePrefixes = ['moz', 'ms', 'webkit'];
function requestAnimationFramePolyfill() {
    let lastTime = 0;
    return function (callback) {
        const currTime = new Date().getTime();
        const timeToCall = Math.max(0, 16 - (currTime - lastTime));
        const id = setTimeout(() => {
            callback(currTime + timeToCall);
        }, timeToCall); // setTimeout type warn
        lastTime = currTime + timeToCall;
        return id;
    };
}
function getRequestAnimationFrame() {
    if (typeof window === 'undefined') {
        return () => 0;
    }
    if (window.requestAnimationFrame) {
        // https://github.com/vuejs/vue/issues/4465
        return window.requestAnimationFrame.bind(window);
    }
    const prefix = availablePrefixes.filter(key => `${key}RequestAnimationFrame` in window)[0];
    return prefix ? window[`${prefix}RequestAnimationFrame`] : requestAnimationFramePolyfill();
}
function cancelRequestAnimationFrame(id) {
    if (typeof window === 'undefined') {
        return null;
    }
    if (window.cancelAnimationFrame) {
        return window.cancelAnimationFrame(id);
    }
    const prefix = availablePrefixes.filter(key => `${key}CancelAnimationFrame` in window || `${key}CancelRequestAnimationFrame` in window)[0];
    return prefix
        ? (window[`${prefix}CancelAnimationFrame`] || window[`${prefix}CancelRequestAnimationFrame`])
            // @ts-ignore
            .call(this, id)
        : clearTimeout(id);
}
const reqAnimFrame = getRequestAnimationFrame();

function easeInOutCubic(t, b, c, d) {
    const cc = c - b;
    let tt = t / (d / 2);
    if (tt < 1) {
        return (cc / 2) * tt * tt * tt + b;
    }
    else {
        return (cc / 2) * ((tt -= 2) * tt * tt + 2) + b;
    }
}
class ThyScrollService {
    constructor(document, ngZone) {
        this.ngZone = ngZone;
        this.document = document;
    }
    /** Set the position of the scroll bar of `element`. */
    setScrollTop(element, topValue = 0) {
        if (element === window) {
            this.document.body.scrollTop = topValue;
            this.document.documentElement.scrollTop = topValue;
        }
        else {
            element.scrollTop = topValue;
        }
    }
    /** Get the position of the scoll bar of `element`. */
    getScroll(element, top = true) {
        const target = element ? element : window;
        const prop = top ? 'pageYOffset' : 'pageXOffset';
        const method = top ? 'scrollTop' : 'scrollLeft';
        const isWindow = target === window;
        let ret = isWindow ? target[prop] : target[method];
        if (isWindow && typeof ret !== 'number') {
            ret = this.document.documentElement[method];
        }
        return ret;
    }
    /**
     * Scroll `element` to some position with animation.
     *
     * @param container container, `window` by default
     * @param topValue Scroll to `top`, 0 by default
     * @param easing Transition curve, `easeInOutCubic` by default
     * @param callback callback invoked when transition is done
     */
    scrollTo(container, topValue = 0, easing, callback) {
        const target = container ? container : window;
        const scrollTop = this.getScroll(target);
        const startTime = Date.now();
        const frameFunc = () => {
            const timestamp = Date.now();
            const time = timestamp - startTime;
            this.setScrollTop(target, (easing || easeInOutCubic)(time, scrollTop, topValue, 450));
            if (time < 450) {
                this.ngZone.runOutsideAngular(() => reqAnimFrame(frameFunc));
            }
            else {
                if (callback) {
                    // The `frameFunc` is called within the `<root>` zone, but we have to re-enter
                    // the Angular zone when calling custom callback to be backwards-compatible.
                    this.ngZone.run(() => callback());
                }
            }
        };
        // The `requestAnimationFrame` triggers change detection, but setting
        // `document.scrollTop` doesn't require Angular to run `ApplicationRef.tick()`.
        this.ngZone.runOutsideAngular(() => reqAnimFrame(frameFunc));
    }
    static { this.ɵfac = i0.ɵɵngDeclareFactory({ minVersion: "12.0.0", version: "17.2.3", ngImport: i0, type: ThyScrollService, deps: [{ token: DOCUMENT }, { token: i0.NgZone }], target: i0.ɵɵFactoryTarget.Injectable }); }
    static { this.ɵprov = i0.ɵɵngDeclareInjectable({ minVersion: "12.0.0", version: "17.2.3", ngImport: i0, type: ThyScrollService, providedIn: 'root' }); }
}
i0.ɵɵngDeclareClassMetadata({ minVersion: "12.0.0", version: "17.2.3", ngImport: i0, type: ThyScrollService, decorators: [{
            type: Injectable,
            args: [{
                    providedIn: 'root'
                }]
        }], ctorParameters: () => [{ type: undefined, decorators: [{
                    type: Inject,
                    args: [DOCUMENT]
                }] }, { type: i0.NgZone }] });

class AnimationDuration {
    static { this.SLOW = '0.3s'; } // Modal
    static { this.BASE = '0.2s'; }
    static { this.FAST = '0.1s'; } // Tooltip
}
class AnimationCurves {
    static { this.EASE_BASE_OUT = 'cubic-bezier(0.7, 0.3, 0.1, 1)'; }
    static { this.EASE_BASE_IN = 'cubic-bezier(0.9, 0, 0.3, 0.7)'; }
    static { this.EASE_OUT = 'cubic-bezier(0.215, 0.61, 0.355, 1)'; }
    static { this.EASE_IN = 'cubic-bezier(0.55, 0.055, 0.675, 0.19)'; }
    static { this.EASE_IN_OUT = 'cubic-bezier(0.645, 0.045, 0.355, 1)'; }
    static { this.EASE_OUT_BACK = 'cubic-bezier(0.12, 0.4, 0.29, 1.46)'; }
    static { this.EASE_IN_BACK = 'cubic-bezier(0.71, -0.46, 0.88, 0.6)'; }
    static { this.EASE_IN_OUT_BACK = 'cubic-bezier(0.71, -0.46, 0.29, 1.46)'; }
    static { this.EASE_OUT_CIRC = 'cubic-bezier(0.08, 0.82, 0.17, 1)'; }
    static { this.EASE_IN_CIRC = 'cubic-bezier(0.6, 0.04, 0.98, 0.34)'; }
    static { this.EASE_IN_OUT_CIRC = 'cubic-bezier(0.78, 0.14, 0.15, 0.86)'; }
    static { this.EASE_OUT_QUINT = 'cubic-bezier(0.23, 1, 0.32, 1)'; }
    static { this.EASE_IN_QUINT = 'cubic-bezier(0.755, 0.05, 0.855, 0.06)'; }
    static { this.EASE_IN_OUT_QUINT = 'cubic-bezier(0.86, 0, 0.07, 1)'; }
    static { this.EASE_IN_OUT_STANDARD = 'cubic-bezier(0.34, 0.69, 0.1, 1)'; }
}

const fadeMotion = trigger('fadeMotion', [
    transition(':enter', [style({ opacity: 0 }), animate(`${AnimationDuration.BASE}`, style({ opacity: 1 }))]),
    transition(':leave', [style({ opacity: 1 }), animate(`${AnimationDuration.BASE}`, style({ opacity: 0 }))])
]);

const collapseMotion = trigger('collapseMotion', [
    state('expanded', style({ height: '*' })),
    state('collapsed', style({ height: 0, overflow: 'hidden' })),
    state('hidden', style({ height: 0, overflow: 'hidden', borderTopWidth: '0' })),
    transition('expanded => collapsed', animate(`150ms ${AnimationCurves.EASE_IN_OUT}`)),
    transition('expanded => hidden', animate(`150ms ${AnimationCurves.EASE_IN_OUT}`)),
    transition('collapsed => expanded', animate(`150ms ${AnimationCurves.EASE_IN_OUT}`)),
    transition('hidden => expanded', animate(`150ms ${AnimationCurves.EASE_IN_OUT}`))
]);

const thumbMotion = trigger('thumbMotion', [
    state('from', style({ transform: 'translateX({{ transform }}px)', width: '{{ width }}px' }), {
        params: { transform: 0, width: 0 }
    }),
    state('to', style({ transform: 'translateX({{ transform }}px)', width: '{{ width }}px' }), {
        params: { transform: 100, width: 0 }
    }),
    transition('from => to', animate(`300ms ${AnimationCurves.EASE_IN_OUT}`))
]);

// The animation starts from the line and eases in and out to the left or right.
const scaleXMotion = trigger('scaleXMotion', [
    transition('* => enter', [
        style({
            opacity: 0,
            transform: 'scaleX(0.9)'
        }),
        animate(`${AnimationDuration.BASE} ${AnimationCurves.EASE_IN_OUT_STANDARD}`, style({
            opacity: 1,
            transform: 'scaleX(1)'
        }))
    ]),
    transition('enter => *', [
        style({
            opacity: 1,
            transform: 'scaleX(1)'
        }),
        animate(`${AnimationDuration.BASE} ${AnimationCurves.EASE_IN_OUT_STANDARD}`, style({
            opacity: 0,
            transform: 'scaleX(0.9)'
        }))
    ])
]);
// The animation starts from the line and eases in and out upward or downward.
const scaleYMotion = trigger('scaleYMotion', [
    transition('* => enter', [
        style({
            opacity: 0,
            transform: 'scaleY(0.9)'
        }),
        animate(`${AnimationDuration.BASE} ${AnimationCurves.EASE_IN_OUT_STANDARD}`, style({
            opacity: 1,
            transform: 'scaleY(1)'
        }))
    ]),
    transition('enter => *', [
        style({
            opacity: 1,
            transform: 'scaleY(1)'
        }),
        animate(`${AnimationDuration.BASE} ${AnimationCurves.EASE_IN_OUT_STANDARD}`, style({
            opacity: 0,
            transform: 'scaleY(0.9)'
        }))
    ])
]);
// The animation starts from the point and eases in and out.
const scaleMotion = trigger('scaleMotion', [
    transition('* => enter', [
        style({ opacity: 0, transform: 'scale(0.9, 0.9)' }),
        animate(`${AnimationDuration.BASE} ${AnimationCurves.EASE_IN_OUT_STANDARD}`, style({
            opacity: 1,
            transform: 'scale(1, 1)'
        }))
    ]),
    transition('enter => *', [
        style({ opacity: 1, transform: 'scale(1, 1)' }),
        animate(`${AnimationDuration.BASE} ${AnimationCurves.EASE_IN_OUT_STANDARD}`, style({
            opacity: 0,
            transform: 'scale(0.9, 0.9)'
        }))
    ])
]);

const POSITION_MAP = {
    top: {
        originX: 'center',
        originY: 'top',
        overlayX: 'center',
        overlayY: 'bottom'
    },
    topCenter: {
        originX: 'center',
        originY: 'top',
        overlayX: 'center',
        overlayY: 'bottom'
    },
    topLeft: {
        originX: 'start',
        originY: 'top',
        overlayX: 'start',
        overlayY: 'bottom'
    },
    topRight: {
        originX: 'end',
        originY: 'top',
        overlayX: 'end',
        overlayY: 'bottom'
    },
    right: {
        originX: 'end',
        originY: 'center',
        overlayX: 'start',
        overlayY: 'center'
    },
    rightTop: {
        originX: 'end',
        originY: 'top',
        overlayX: 'start',
        overlayY: 'top'
    },
    rightBottom: {
        originX: 'end',
        originY: 'bottom',
        overlayX: 'start',
        overlayY: 'bottom'
    },
    bottom: {
        originX: 'center',
        originY: 'bottom',
        overlayX: 'center',
        overlayY: 'top'
    },
    bottomCenter: {
        originX: 'center',
        originY: 'bottom',
        overlayX: 'center',
        overlayY: 'top'
    },
    bottomLeft: {
        originX: 'start',
        originY: 'bottom',
        overlayX: 'start',
        overlayY: 'top'
    },
    bottomRight: {
        originX: 'end',
        originY: 'bottom',
        overlayX: 'end',
        overlayY: 'top'
    },
    left: {
        originX: 'start',
        originY: 'center',
        overlayX: 'end',
        overlayY: 'center'
    },
    leftTop: {
        originX: 'start',
        originY: 'top',
        overlayX: 'end',
        overlayY: 'top'
    },
    leftBottom: {
        originX: 'start',
        originY: 'bottom',
        overlayX: 'end',
        overlayY: 'bottom'
    }
};
const EXPANDED_DROPDOWN_POSITIONS = [POSITION_MAP.bottomLeft, POSITION_MAP.bottomRight, POSITION_MAP.topLeft, POSITION_MAP.topRight];

const connectionFallbackPositionsMap = {
    top: ['topLeft', 'topRight', 'bottom', 'bottomLeft', 'bottomRight'],
    topLeft: ['top', 'topRight', 'bottomLeft', 'bottom', 'bottomRight'],
    topRight: ['top', 'topLeft', 'bottomRight', 'bottom', 'bottomLeft'],
    bottom: ['bottomLeft', 'bottomRight', 'top', 'topLeft', 'topRight'],
    bottomLeft: ['bottom', 'bottomRight', 'topLeft', 'top', 'topRight'],
    bottomRight: ['bottom', 'bottomLeft', 'topRight', 'top', 'topLeft'],
    left: ['leftTop', 'leftBottom', 'right', 'rightTop', 'rightBottom'],
    leftTop: ['left', 'leftBottom', 'rightTop', 'right', 'rightBottom'],
    leftBottom: ['left', 'leftTop', 'rightBottom', 'right', 'rightTop'],
    right: ['rightTop', 'rightBottom', 'left', 'leftTop', 'leftBottom'],
    rightTop: ['right', 'rightBottom', 'leftTop', 'left', 'leftBottom'],
    rightBottom: ['right', 'rightTop', 'leftBottom', 'left', 'leftTop']
};
function buildConnectedPositionOffset(placement, offset) {
    const connectedPositionOffset = {};
    if (placement.startsWith('top')) {
        connectedPositionOffset.offsetY = -offset;
    }
    else if (placement.startsWith('bottom')) {
        connectedPositionOffset.offsetY = offset;
    }
    else if (placement.startsWith('left')) {
        connectedPositionOffset.offsetX = -offset;
    }
    else if (placement.startsWith('right')) {
        connectedPositionOffset.offsetX = offset;
    }
    else {
        // do nothings
    }
    return connectedPositionOffset;
}
function buildConnectedPositionPair(placement, offset, panelClassPrefix) {
    const position = {
        ...POSITION_MAP[placement],
        ...(offset ? buildConnectedPositionOffset(placement, offset) : null)
    };
    if (panelClassPrefix) {
        position.panelClass = `${panelClassPrefix}-${placement}`;
    }
    return position;
}
function getFallbackPlacements(placement) {
    return connectionFallbackPositionsMap[placement] || [];
}
/**
 * get flexible positions by placement, return placement position and it's fallback connection position
 * @example
 * getFlexiblePositions('top', 10, 'thy-overlay')
 * [{topPosition}, {topLeftPosition}, {topRightPosition}, {bottomPosition}, {bottomLeftPosition}, {bottomRightPosition},]
 * @returns [ConnectionPositionPair]
 */
function getFlexiblePositions(placement, offset, panelClassPrefix) {
    const fallbackPlacements = getFallbackPlacements(placement);
    return [placement, ...fallbackPlacements].map(placementName => {
        return buildConnectedPositionPair(placementName, offset, panelClassPrefix);
    });
}
function getPlacementByPosition(position) {
    const keyList = ['originX', 'originY', 'overlayX', 'overlayY'];
    for (const placement in POSITION_MAP) {
        if (keyList.every(key => position[key] === POSITION_MAP[placement][key])) {
            return placement;
        }
    }
}

const passiveEventListenerOptions = normalizePassiveListenerOptions({ passive: true });
const longPressTime = 500;
class ThyOverlayDirectiveBase {
    get trigger() {
        return this._trigger;
    }
    set trigger(value) {
        this._trigger = value;
        // Trigger reinitialize when trigger changed which can't contain first
        if (this.initialized) {
            this.clearEventListeners();
            this.initialize();
        }
    }
    clearEventListeners() {
        this.manualListeners.forEach((listener, event) => {
            this.elementRef.nativeElement.removeEventListener(event, listener);
        });
        this.manualListeners.clear();
        this.focusMonitor.stopMonitoring(this.elementRef);
    }
    clearTimer() {
        if (this.showTimeoutId) {
            clearTimeout(this.showTimeoutId);
        }
        if (this.hideTimeoutId) {
            clearTimeout(this.hideTimeoutId);
        }
        if (this.longPressTimeoutId) {
            clearTimeout(this.longPressTimeoutId);
        }
    }
    constructor(elementRef, platform, focusMonitor, ngZone, overlayPin, changeDetectorRef) {
        this.initialized = false;
        /** Trigger Overlay */
        this._trigger = 'click';
        this.manualListeners = new Map();
        this.ngUnsubscribe$ = new Subject();
        this.showDelay = 100;
        this.hideDelay = 100;
        this.touchendHideDelay = 0;
        this.disabled = false;
        this.isAutoCloseOnMobileTouch = false;
        this.isTouchMoving = false;
        this.elementRef = elementRef;
        this.platform = platform;
        this.focusMonitor = focusMonitor;
        this.ngZone = ngZone;
        this.overlayPin = overlayPin;
        this.changeDetectorRef = changeDetectorRef;
    }
    initialize() {
        this.initialized = true;
        const element = this.elementRef.nativeElement;
        if (!this.platform.IOS && !this.platform.ANDROID) {
            if (this.trigger === 'hover') {
                this.manualListeners
                    .set('mouseenter', () => {
                    this.show();
                })
                    .set('mouseleave', (event) => {
                    // Delay 100ms to avoid the overlay being closed immediately when the cursor is moved to the overlay container
                    this.hide();
                    const overlayElement = this.overlayRef && this.overlayRef.overlayElement;
                    if (overlayElement && this.overlayPin) {
                        fromEvent(overlayElement, 'mouseenter')
                            .pipe(take(1))
                            .subscribe(() => {
                            this.clearTimer();
                            fromEvent(overlayElement, 'mouseleave')
                                .pipe(take(1))
                                .subscribe(() => {
                                this.hide();
                            });
                        });
                    }
                    // if showDelay is too long and mouseleave immediately, overlayRef is not exist, we should clearTimeout
                    if (!this.overlayRef) {
                        this.clearTimer();
                    }
                });
            }
            else if (this.trigger === 'focus') {
                this.focusMonitor
                    .monitor(this.elementRef)
                    .pipe(takeUntil(this.ngUnsubscribe$))
                    .subscribe(origin => {
                    // Note that the focus monitor runs outside the Angular zone.
                    if (!origin) {
                        this.ngZone.run(() => this.hide(0));
                    }
                    else {
                        this.ngZone.run(() => this.show());
                    }
                });
                // this.manualListeners.set('focus', () => this.show());
                // this.manualListeners.set('blur', () => this.hide());
            }
            else if (this.trigger === 'click') {
                this.manualListeners.set('click', () => {
                    this.show();
                });
            }
            else if (typeof ngDevMode === 'undefined' || ngDevMode) {
                throw new Error(`${this.trigger} is not supporteed, possible values are: hover | focus | click.`);
            }
        }
        else {
            const touchendListener = () => {
                const touchEndTime = Date.now();
                const touchDuration = touchEndTime - this.touchStartTime;
                if (touchDuration < longPressTime && !this.isTouchMoving) {
                    // tap
                    this.handleTouch();
                }
                clearTimeout(this.longPressTimeoutId);
                this.isTouchMoving = false;
            };
            // Reserve extensions for mobile in the future
            this.manualListeners
                .set('touchend', touchendListener)
                .set('touchcancel', touchendListener)
                .set('touchmove', () => {
                this.isTouchMoving = true;
                clearTimeout(this.longPressTimeoutId);
            })
                .set('touchstart', () => {
                this.touchStartTime = Date.now();
                this.isTouchMoving = false;
                // 设置一个定时器，如果在一定时间内没有触发 touchend 事件，则判断为长按
                this.longPressTimeoutId = setTimeout(() => {
                    // long press
                    if (!this.isTouchMoving) {
                        this.handleTouch();
                    }
                }, longPressTime);
            });
        }
        this.manualListeners.forEach((listener, event) => 
        // Note: since Chrome 56 defaults document level `touchstart` listener to passive.
        // Element touch listeners are not passive by default.
        // We never call `preventDefault()` on events, so we're safe making them passive.
        element.addEventListener(event, listener, passiveEventListenerOptions));
    }
    handleTouch() {
        this.show();
        if (this.isAutoCloseOnMobileTouch) {
            setTimeout(() => {
                this.hide(0);
            }, this.touchendHideDelay + (isNumber(this.showDelay) ? this.showDelay : 0));
        }
    }
    /**
     * Marks that the overlay needs to be checked in the next change detection run.
     * Mainly used for rendering before positioning a overlay, which
     * can be problematic in components with OnPush change detection.
     */
    markForCheck() {
        this.changeDetectorRef?.markForCheck();
    }
    dispose() {
        this.ngUnsubscribe$.next();
        this.ngUnsubscribe$.complete();
        if (this.overlayRef) {
            this.overlayRef.dispose();
        }
        this.clearEventListeners();
        this.clearTimer();
    }
}

class ThyAbstractOverlayRef {
}
// Counter for unique overlay ids.
const uniqueIdMap = {};
function getUniqueId(name) {
    if (uniqueIdMap[name] !== undefined) {
        uniqueIdMap[name] = uniqueIdMap[name] + 1;
    }
    else {
        uniqueIdMap[name] = 0;
    }
    return uniqueIdMap[name];
}
class ThyAbstractInternalOverlayRef extends ThyAbstractOverlayRef {
    /** Fetches the position strategy object from the overlay ref. */
    getPositionStrategy() {
        return this.overlayRef.getConfig().positionStrategy;
    }
    constructor(options, overlayRef, containerInstance, config) {
        super();
        this.options = options;
        this.overlayRef = overlayRef;
        this.config = config;
        /** Whether the user is allowed to close the dialog. */
        this.backdropClosable = this.config.backdropClosable;
        /** Whether the user is not allowed to close the dialog. */
        this.disableClose = this.config.disableClose;
        /** Subject for notifying the user that the dialog has finished opening. */
        this._afterOpened = new Subject();
        /** Subject for notifying the user that the dialog has finished closing. */
        this._afterClosed = new Subject();
        /** Subject for notifying the user that the dialog has started closing. */
        this._beforeClosed = new Subject();
        this.containerInstance = containerInstance;
        // Pass the id along to the container.
        this.id = containerInstance.id = config.id ? config.id : `thy-${this.options.name}-${getUniqueId(this.options.name)}`;
        // Emit when opening animation completes
        containerInstance.animationOpeningDone.pipe(take(1)).subscribe(() => {
            this._afterOpened.next();
            if (this.options.disposeWhenClose) {
                this._afterOpened.complete();
            }
        });
        // Dispose overlay when closing animation is complete
        containerInstance.animationClosingDone.pipe(take(1)).subscribe(() => {
            if (this.options.disposeWhenClose) {
                this.overlayRef.dispose();
            }
        });
        // Dispose overlay when container after destroy
        containerInstance.containerDestroy.pipe(take(1)).subscribe(() => {
            if (this.options.disposeWhenClose) {
                // component element has not been deleted when the component destroy, so use promise wait for component element destroyed
                Promise.resolve().then(() => {
                    this.overlayRef.dispose();
                });
            }
        });
        overlayRef
            .detachments()
            .pipe(finalize(() => {
            this.overlayRef.dispose();
        }))
            .subscribe(() => {
            this._beforeClosed.next(this._result);
            this._beforeClosed.complete();
            this._afterClosed.next(this._result);
            this._afterClosed.complete();
            this.componentInstance = null;
        });
        // ESC close
        overlayRef
            .keydownEvents()
            .pipe(filter(event => event.keyCode === ESCAPE))
            .subscribe(() => {
            if ((this.disableClose !== undefined && !this.disableClose) || this.backdropClosable) {
                this.close();
            }
        });
    }
    /**
     * Close the overlay.
     * @param overlayResult Optional result to return to the dialog opener.
     */
    close(overlayResult, force) {
        if (force || !this.config.canClose || !!this.config.canClose(overlayResult)) {
            this._result = overlayResult;
            // Transition the backdrop in parallel to the overlay.
            this._beforeClosed.next(overlayResult);
            if (this.options.disposeWhenClose) {
                this._beforeClosed.complete();
            }
            this.overlayRef.detachBackdrop();
            this.containerInstance.startExitAnimation();
        }
    }
    /**
     * Gets an observable that is notified when the dialog is finished opening.
     */
    afterOpened() {
        return this._afterOpened.asObservable();
    }
    /**
     * Gets an observable that is notified when the dialog is finished closing.
     */
    afterClosed() {
        return this._afterClosed.asObservable();
    }
    /**
     * Gets an observable that is notified when the dialog has started closing.
     */
    beforeClosed() {
        return this._beforeClosed.asObservable();
    }
    /**
     * Gets an observable that emits when the overlay's backdrop has been clicked.
     */
    backdropClick() {
        return this.overlayRef.backdropClick();
    }
    /**
     * Gets an observable that emits when keydown events are targeted on the overlay.
     */
    keydownEvents() {
        return this.overlayRef.keydownEvents();
    }
    /** Get overlay ref */
    getOverlayRef() {
        return this.overlayRef;
    }
    /**
     * Updates the overlay's position when is GlobalPositionStrategy
     * @param position New overlay position.
     */
    updateGlobalPosition(position) {
        const strategy = this.getPositionStrategy();
        if ((typeof ngDevMode === 'undefined' || ngDevMode) && !(strategy instanceof GlobalPositionStrategy)) {
            throw new Error(`current strategy is not GlobalPositionStrategy`);
        }
        if (position && (position.left || position.right)) {
            position.left ? strategy.left(position.left) : strategy.right(position.right);
        }
        else {
            strategy.centerHorizontally();
        }
        if (position && (position.top || position.bottom)) {
            position.top ? strategy.top(position.top) : strategy.bottom(position.bottom);
        }
        else {
            strategy.centerVertically();
        }
        this.overlayRef.updatePosition();
        return this;
    }
}

class ThyAbstractOverlayConfig {
    constructor() {
        /**
         * overlay panel 类名
         * @description.en-us Custom class for the overlay pane.
         */
        this.panelClass = '';
        /**
         * 是否有幕布
         * @description.en-us Whether the overlay has a backdrop.
         */
        this.hasBackdrop = true;
        /**
         * 自定义幕布的样式
         * @description.en-us Custom class for the backdrop.
         */
        this.backdropClass = '';
        /**
         * @description 点击幕布或者按ESC键是否自动关闭弹出框，hasBackdrop=true 时该参数起作用
         * @description.en-us Whether the user can use escape or clicking on the backdrop to close the overlay.
         */
        this.backdropClosable = true;
        /** Whether the user can use escape or clicking on the backdrop to close the overlay. */
        this.disableClose = false;
        /**
         * 自定义弹出框的宽度
         * @default 660px
         * @description.en-us Width of the overlay.
         */
        this.width = '';
        /**
         * 自定义弹出框的高度
         * @default 85vh
         * @description.en-us Height of the overlay.
         */
        this.height = '';
        /**
         * 传入的初始化状态，弹出组件的变量会自动赋值，在 ngOnInit 生命周期钩子可以获取到，构造函数获取不到
         * @description.en-us Data being injected into the child component.
         */
        this.initialState = null;
        /** ID of the element that describes the overlay. */
        this.ariaDescribedBy = null;
        /** Aria label to assign to the overlay element */
        this.ariaLabel = null;
        /** Whether the overlay should focus the first focusable element on open. */
        this.autoFocus = true;
        /**
         * Whether the overlay should restore focus to the
         * previously-focused element, after it's closed.
         */
        this.restoreFocus = true;
        /**
         * Control the previously-focused element focusing process.
         */
        this.restoreFocusOptions = { preventScroll: true };
        /**
         * 切换浏览器导航是否自动关闭弹出框
         * @description.en-us Whether the overlay should close when the user goes backwards/forwards in history.Note that this usually doesn't include clicking on links (unless the user is using the `HashLocationStrategy`).
         */
        this.closeOnNavigation = true;
    }
}

function throwPopoverContentAlreadyAttachedError(name) {
    throw Error(`Attempting to attach ${name} content after content is already attached`);
}
class ThyAbstractOverlayContainer {
    /** Before detach content*/
    beforeDetachPortal() { }
    constructor(options, changeDetectorRef) {
        this.options = options;
        this.changeDetectorRef = changeDetectorRef;
        this.animationStateChanged = new EventEmitter();
        this.containerDestroy = new Subject();
    }
    /**
     * Attach a TemplatePortal as content to this overlay container.
     * @param portal Portal to be attached as the overlay content.
     */
    attachTemplatePortal(portal) {
        if (this.portalOutlet.hasAttached()) {
            throwPopoverContentAlreadyAttachedError(this.options.name);
        }
        this.beforeAttachPortal();
        return this.portalOutlet.attachTemplatePortal(portal);
    }
    /**
     * Attach a ComponentPortal as content to this overlay container.
     * @param portal Portal to be attached as the overlay content.
     */
    attachComponentPortal(portal) {
        if (this.portalOutlet.hasAttached()) {
            throwPopoverContentAlreadyAttachedError(this.options.name);
        }
        this.beforeAttachPortal();
        return this.portalOutlet.attachComponentPortal(portal);
    }
    startExitAnimation() {
        if (this.options.animationEnabled) {
            this.animationState = 'exit';
        }
        else {
            // this.animationClosingDone.
        }
        this.beforeDetachPortal();
        // Mark the container for check so it can react if the
        // view container is using OnPush change detection.
        this.changeDetectorRef.markForCheck();
    }
    destroy() {
        this.containerDestroy.next();
    }
}

class ThyAbstractOverlayService {
    constructor(options, // component name, e.g: dialog | popover | slide
    overlay, injector, defaultConfig, scrollStrategy) {
        this.options = options;
        this.overlay = overlay;
        this.injector = injector;
        this.defaultConfig = defaultConfig;
        this.scrollStrategy = scrollStrategy;
        this.openedOverlays = [];
        this._afterAllClosed = new Subject();
        this._afterOpened = new Subject();
    }
    /** Attach component or template ref to overlay container */
    attachOverlayContent(componentOrTemplateRef, containerInstance, overlayRef, config) {
        // Create a reference to the overlay we're creating in order to give the user a handle
        // to modify and close it.
        const abstractOverlayRef = this.createAbstractOverlayRef(overlayRef, containerInstance, config);
        // When the backdrop is clicked, we want to close it.
        if (config.hasBackdrop) {
            overlayRef.backdropClick().subscribe(() => {
                if ((abstractOverlayRef.disableClose !== undefined && !abstractOverlayRef.disableClose) ||
                    abstractOverlayRef.backdropClosable) {
                    this.close();
                }
            });
        }
        if (componentOrTemplateRef instanceof TemplateRef) {
            containerInstance.attachTemplatePortal(new TemplatePortal(componentOrTemplateRef, null, {
                $implicit: config.initialState,
                [`${this.options.name}Ref`]: abstractOverlayRef
            }));
        }
        else {
            const injector = this.createInjector(config, abstractOverlayRef, containerInstance);
            const contentRef = containerInstance.attachComponentPortal(new ComponentPortal(componentOrTemplateRef, config.viewContainerRef, injector));
            if (config.initialState) {
                Object.assign(contentRef.instance, config.initialState);
            }
            if (config.hostClass) {
                const hostClass = coerceArray(config.hostClass);
                contentRef.location.nativeElement.classList.add(...hostClass);
            }
            abstractOverlayRef.componentInstance = contentRef.instance;
        }
        return abstractOverlayRef;
    }
    removeOpenedOverlay(overlayRef) {
        const index = this.openedOverlays.indexOf(overlayRef);
        if (index > -1) {
            this.openedOverlays.splice(index, 1);
            if (!this.openedOverlays.length) {
                this._afterAllClosed.next();
            }
        }
    }
    getAbstractOverlayById(id) {
        return this.openedOverlays.find(overlay => overlay.id === id);
    }
    getAbstractOverlays() {
        return this.openedOverlays;
    }
    buildBaseOverlayConfig(config, defaultPanelClass) {
        const overlayConfig = new OverlayConfig({
            positionStrategy: this.overlay.position().global(),
            hasBackdrop: config.hasBackdrop,
            direction: config.direction,
            width: config.width,
            height: config.height,
            minWidth: config.minWidth,
            minHeight: config.minHeight,
            maxWidth: config.maxWidth,
            maxHeight: config.maxHeight,
            disposeOnNavigation: config.closeOnNavigation
        });
        if (config.backdropClass) {
            overlayConfig.backdropClass = config.backdropClass;
        }
        overlayConfig.panelClass = concatArray(config.panelClass, defaultPanelClass);
        return overlayConfig;
    }
    openOverlay(componentOrTemplateRef, config) {
        config = { ...this.defaultConfig, ...config };
        if (config.id && this.getAbstractOverlayById(config.id)) {
            throw Error(`${this.options.name} with id ${config.id} exists already. The ${this.options.name} id must be unique.`);
        }
        const overlayConfig = this.buildOverlayConfig(config);
        const overlayRef = this.overlay.create(overlayConfig);
        const overlayContainer = this.attachOverlayContainer(overlayRef, config);
        const abstractOverlayRef = this.attachOverlayContent(componentOrTemplateRef, overlayContainer, overlayRef, config);
        this.openedOverlays.push(abstractOverlayRef);
        abstractOverlayRef.afterClosed().subscribe(() => {
            this.removeOpenedOverlay(abstractOverlayRef);
        });
        this._afterOpened.next(abstractOverlayRef);
        return abstractOverlayRef;
    }
    /**
     * 所有弹出框完全关闭后的回调
     * @returns
     */
    afterAllClosed() {
        return this._afterAllClosed;
    }
    /**
     * 打开弹出框之后的回调
     * @returns
     */
    afterOpened() {
        return this._afterOpened;
    }
    /**
     * 关闭弹出框，若force为true，则canClose无效，强制关闭
     * @param result
     * @param force
     */
    close(result, force) {
        if (this.openedOverlays.length > 0) {
            const lastOverlayRef = this.openedOverlays[this.openedOverlays.length - 1];
            if (lastOverlayRef) {
                lastOverlayRef.close(result, force);
            }
        }
    }
    /**
     * 关闭所有打开的弹出框
     */
    closeAll() {
        let i = this.openedOverlays.length;
        while (i--) {
            // 不需要操作 openedOverlays, 因为 close 会触发 afterClosed 的订阅
            // 触发订阅后会自动从 openedOverlays 中移除
            this.openedOverlays[i].close();
        }
    }
    dispose() {
        this.closeAll();
        this._afterAllClosed.complete();
        this._afterOpened.complete();
    }
}

/* eslint-disable @angular-eslint/no-inputs-metadata-property */
/* eslint-disable @angular-eslint/directive-class-suffix */
class ThyPortalOutlet extends CdkPortalOutlet {
    /**
     * Attach the given ComponentPortal to this PortalOutlet using the ComponentFactoryResolver.
     *
     * @param portal Portal to be attached to the portal outlet.
     * @returns Reference to the created component.
     */
    attachComponentPortal(portal) {
        portal.setAttachedHost(this);
        // If the portal specifies an origin, use that as the logical location of the component
        // in the application tree. Otherwise use the location of this PortalOutlet.
        const viewContainerRef = portal.viewContainerRef != null ? portal.viewContainerRef : this['_viewContainerRef'];
        const ref = viewContainerRef.createComponent(portal.component, {
            index: viewContainerRef.length,
            injector: portal.injector || viewContainerRef.injector,
            projectableNodes: portal.projectableNodes || undefined
        });
        // If we're using a view container that's different from the injected one (e.g. when the portal
        // specifies its own) we need to move the component into the outlet, otherwise it'll be rendered
        // inside of the alternate view container.
        if (viewContainerRef !== this['_viewContainerRef']) {
            this['_getRootNode']().appendChild(ref.hostView.rootNodes[0]);
        }
        super.setDisposeFn(() => ref.destroy());
        this._attachedPortal = portal;
        this['_attachedRef'] = ref;
        this.attached.emit(ref);
        return ref;
    }
    static { this.ɵfac = i0.ɵɵngDeclareFactory({ minVersion: "12.0.0", version: "17.2.3", ngImport: i0, type: ThyPortalOutlet, deps: null, target: i0.ɵɵFactoryTarget.Directive }); }
    static { this.ɵdir = i0.ɵɵngDeclareDirective({ minVersion: "14.0.0", version: "17.2.3", type: ThyPortalOutlet, isStandalone: true, selector: "[thyPortalOutlet]", inputs: { portal: ["thyPortalOutlet", "portal"] }, exportAs: ["thyPortalOutlet"], usesInheritance: true, ngImport: i0 }); }
}
i0.ɵɵngDeclareClassMetadata({ minVersion: "12.0.0", version: "17.2.3", ngImport: i0, type: ThyPortalOutlet, decorators: [{
            type: Directive,
            args: [{
                    selector: '[thyPortalOutlet]',
                    exportAs: 'thyPortalOutlet',
                    standalone: true,
                    inputs: ['portal: thyPortalOutlet']
                }]
        }] });

class UpdateHostClassService {
    constructor(renderer) {
        this.renderer = renderer;
        this._classNames = [];
    }
    initializeElement(element) {
        if (element instanceof ElementRef) {
            this._hostElement = element.nativeElement;
        }
        else {
            this._hostElement = element;
        }
        return this;
    }
    updateClass(classNames) {
        if (this._classNames) {
            this._classNames.forEach(className => {
                if (classNames.indexOf(className) < 0) {
                    this.removeClass(className);
                }
            });
        }
        const newClasses = [];
        classNames.forEach(className => {
            if (className) {
                newClasses.push(className);
                if (this._classNames.indexOf(className) < 0) {
                    this.addClass(className);
                }
            }
        });
        this._classNames = newClasses;
        return this;
    }
    updateClassByMap(classMap) {
        const newClasses = [];
        for (const key in classMap) {
            if (classMap.hasOwnProperty(key) && classMap[key]) {
                newClasses.push(key);
            }
        }
        this.updateClass(newClasses);
    }
    addClass(className) {
        this.renderer.addClass(this._hostElement, className);
        return this;
    }
    removeClass(className) {
        this.renderer.removeClass(this._hostElement, className);
        return this;
    }
    static { this.ɵfac = i0.ɵɵngDeclareFactory({ minVersion: "12.0.0", version: "17.2.3", ngImport: i0, type: UpdateHostClassService, deps: [{ token: i0.Renderer2 }], target: i0.ɵɵFactoryTarget.Injectable }); }
    static { this.ɵprov = i0.ɵɵngDeclareInjectable({ minVersion: "12.0.0", version: "17.2.3", ngImport: i0, type: UpdateHostClassService }); }
}
i0.ɵɵngDeclareClassMetadata({ minVersion: "12.0.0", version: "17.2.3", ngImport: i0, type: UpdateHostClassService, decorators: [{
            type: Injectable
        }], ctorParameters: () => [{ type: i0.Renderer2 }] });

class ThyTranslate {
    instant(key, interpolateParams) {
        return key;
    }
    get(key, interpolateParams) {
        return new Observable((observer) => {
            observer.next(key);
        });
    }
    static { this.ɵfac = i0.ɵɵngDeclareFactory({ minVersion: "12.0.0", version: "17.2.3", ngImport: i0, type: ThyTranslate, deps: [], target: i0.ɵɵFactoryTarget.Injectable }); }
    static { this.ɵprov = i0.ɵɵngDeclareInjectable({ minVersion: "12.0.0", version: "17.2.3", ngImport: i0, type: ThyTranslate, providedIn: 'root' }); }
}
i0.ɵɵngDeclareClassMetadata({ minVersion: "12.0.0", version: "17.2.3", ngImport: i0, type: ThyTranslate, decorators: [{
            type: Injectable,
            args: [{
                    providedIn: 'root'
                }]
        }] });

const META_KEY = '__THY_META__';
const ROOT_STATE_TOKEN = new InjectionToken('ROOT_STATE_TOKEN');
const FEATURE_STATE_TOKEN = new InjectionToken('FEATURE_STATE_TOKEN');
// export type Newable<T> = { new (...args: any[]): T };

function findAndCreateStoreMetadata(target) {
    if (!target.hasOwnProperty(META_KEY)) {
        const defaultMetadata = {
            actions: {},
            path: null,
            children: [],
            instance: null
        };
        target[META_KEY] = defaultMetadata;
    }
    return target[META_KEY];
}

// @dynamic
class MiniActionState {
    static { this.actionName = ''; }
    static changeAction(actionName) {
        this.actionName = actionName;
    }
    static getActionName() {
        return this.actionName;
    }
}

/**
 * Decorates a method with a action information.
 */
function MiniAction() {
    return function (target, name, descriptor) {
        const meta = findAndCreateStoreMetadata(target);
        let action;
        // default use function name as action type
        if (!action) {
            action = {
                type: name
            };
        }
        const type = action.type;
        if ((typeof ngDevMode === 'undefined' || ngDevMode) && !action.type) {
            throw new Error(`Action ${action.type} is missing a static "type" property`);
        }
        const originalFn = descriptor.value;
        meta.actions[type] = {
            fn: name,
            originalFn: originalFn,
            type
        };
        descriptor.value = function (...args) {
            MiniActionState.changeAction(`${target.constructor.name}-${name}`);
            let result = originalFn.call(this, ...args);
            if (result instanceof Observable) {
                result = result.pipe(catchError(error => {
                    return of({ status: "ERRORED" /* ActionStatus.Errored */, action: action, error: error });
                }), shareReplay(), exhaustMap((result) => {
                    if (result && result.status === "ERRORED" /* ActionStatus.Errored */) {
                        return throwError(result.error);
                    }
                    else {
                        return of(result);
                    }
                }));
                result.subscribe();
            }
            return result;
        };
    };
}

class MiniStore {
    constructor(initialState) {
        this.reduxToolEnabled = isDevMode();
        this._defaultStoreInstanceId = this._getClassName();
        this.state$ = new BehaviorSubject(initialState);
        this.initialStateCache = { ...initialState };
    }
    get snapshot() {
        return this.state$.getValue();
    }
    dispatch(type, payload) {
        MiniActionState.changeAction(`${this._defaultStoreInstanceId}-${type}`);
        const result = this._dispatch({
            type: type,
            payload: payload
        });
        result.subscribe();
        return result;
    }
    _dispatch(action) {
        const meta = this[META_KEY];
        if ((typeof ngDevMode === 'undefined' || ngDevMode) && !meta) {
            throw new Error(`${META_KEY} is not found, current store has not action`);
        }
        const actionMeta = meta.actions[action.type];
        if ((typeof ngDevMode === 'undefined' || ngDevMode) && !actionMeta) {
            throw new Error(`${action.type} is not found`);
        }
        // let result: any = this[actionMeta.fn](this.snapshot, action.payload);
        let result = actionMeta.originalFn.call(this, this.snapshot, action.payload);
        if (result instanceof Promise) {
            result = from(result);
        }
        if (result instanceof Observable) {
            result = result.pipe(map(r => r));
        }
        else {
            result = new Observable((observer) => {
                observer.next({});
            });
        }
        return result.pipe(shareReplay());
    }
    select(selector) {
        return this.state$.pipe(map(selector), distinctUntilChanged());
    }
    next(state) {
        this.state$.next(state);
    }
    error(error) {
        this.state$.error(error);
    }
    complete() {
        this.state$.complete();
    }
    subscribe(next, error, complete) {
        return this.state$.subscribe(next, error, complete);
    }
    /**
     * set store new state
     *
     * @example
     * this.setState(newState);
     * this.setState({ users: produce(this.snapshot.users).add(user) });
     * this.setState((state) => {
     *    return {
     *        users: produce(state.users).add(user)
     *    }
     * });
     * @param fn
     */
    setState(fn) {
        if (helpers.isFunction(fn)) {
            this.next({
                ...this.snapshot,
                ...fn(this.snapshot)
            });
        }
        else {
            this.next({
                ...this.snapshot,
                ...fn
            });
        }
    }
    getState() {
        return this.snapshot;
    }
    clearState() {
        this.setState(this.initialStateCache);
    }
    ngOnDestroy() { }
    /**
     * You can override this method if you want to give your container instance a custom id.
     * The returned id must be unique in the application.
     */
    getStoreInstanceId() {
        return this._defaultStoreInstanceId;
    }
    _getClassName() {
        const name = this.constructor.name || /function (.+)\(/.exec(this.constructor + '')[1];
        return name;
    }
    static { this.ɵfac = i0.ɵɵngDeclareFactory({ minVersion: "12.0.0", version: "17.2.3", ngImport: i0, type: MiniStore, deps: "invalid", target: i0.ɵɵFactoryTarget.Directive }); }
    static { this.ɵdir = i0.ɵɵngDeclareDirective({ minVersion: "14.0.0", version: "17.2.3", type: MiniStore, ngImport: i0 }); }
}
__decorate([
    MiniAction(),
    __metadata("design:type", Function),
    __metadata("design:paramtypes", []),
    __metadata("design:returntype", void 0)
], MiniStore.prototype, "clearState", null);
i0.ɵɵngDeclareClassMetadata({ minVersion: "12.0.0", version: "17.2.3", ngImport: i0, type: MiniStore, decorators: [{
            type: Directive
        }], ctorParameters: () => [{ type: undefined }], propDecorators: { clearState: [] } });

const presetThemeColors = ['primary', 'success', 'info', 'warning', 'danger', 'default', 'light'];
function isThemeColor(color) {
    return presetThemeColors.includes(color);
}
const presetTextColors = [...presetThemeColors, 'secondary', 'muted', 'desc', 'placeholder', 'white', 'body'];
function isTextColor(color) {
    return presetTextColors.includes(color);
}
const presetBgColors = [...presetThemeColors, 'secondary', 'dark', 'lighter', 'bright', 'content', 'white', 'transparent'];
function isBgColor(color) {
    return presetBgColors.includes(color);
}

const SPACING_SIZES_MAP = {
    zero: 0,
    xxs: 4,
    xs: 8,
    sm: 12,
    md: 16,
    lg: 20,
    xlg: 24
};
function getNumericSize(size, defaultSize) {
    if (isNumber(size)) {
        return size;
    }
    return SPACING_SIZES_MAP[size] !== undefined ? SPACING_SIZES_MAP[size] : SPACING_SIZES_MAP[defaultSize];
}

/**
 * Generated bundle index. Do not edit.
 */

export { AbstractControlValueAccessor, AnimationCurves, AnimationDuration, AnonymousClass, EXPANDED_DROPDOWN_POSITIONS, InputBoolean, InputCssPixel, InputNumber, MiniAction, MiniActionState, MiniStore, MixinBase, POSITION_MAP, ScrollToService, TabIndexDisabledControlValueAccessorMixin, TabIndexMixinBase, ThyAbstractInternalOverlayRef, ThyAbstractOverlayConfig, ThyAbstractOverlayContainer, ThyAbstractOverlayRef, ThyAbstractOverlayService, ThyClickPositioner, ThyOverlayDirectiveBase, ThyPortalOutlet, ThyScrollService, ThyTranslate, UpdateHostClassService, buildConnectedPositionOffset, buildConnectedPositionPair, cancelRequestAnimationFrame, collapseMotion, fadeMotion, getFallbackPlacements, getFlexiblePositions, getNumericSize, getPlacementByPosition, isBgColor, isTextColor, isThemeColor, mixinDisabled, mixinInitialized, mixinLoadingDone, mixinTabIndex, mixinUnsubscribe, presetBgColors, presetTextColors, presetThemeColors, reqAnimFrame, scaleMotion, scaleXMotion, scaleYMotion, throwPopoverContentAlreadyAttachedError, thumbMotion, useHostFocusControl };
//# sourceMappingURL=ngx-tethys-core.mjs.map
