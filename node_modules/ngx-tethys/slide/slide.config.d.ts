import { ThyAbstractOverlayConfig, ThyAbstractOverlayOptions } from 'ngx-tethys/core';
import { ElementRef, InjectionToken } from '@angular/core';
export type ThySlideFromTypes = 'left' | 'right' | 'top' | 'bottom' | 'void' | 'exit' | 'offsetLeft' | 'offsetRight' | 'offsetTop' | 'offsetBottom';
export type ThySlideMode = 'over' | 'push' | 'side';
/**
 * @public
 * @order 20
 */
export declare class ThySlideConfig<TData = unknown> extends ThyAbstractOverlayConfig<TData> {
    /**
     * Origin Element, for overlay flexible connected to
     * @type ElementRef<any> | HTMLElement | any
     */
    origin?: ElementRef<any> | HTMLElement | any;
    /**
     * slide 进场的方向，可选 left | right | top | bottom
     * @description.en-us set the direction when slide enter
     * @default right
     */
    from?: ThySlideFromTypes;
    /**
     * 从距离屏幕边缘 offset 出滑入滑出，单位 px
     */
    offset?: number;
    /**
     * set class to trigger element.
     * @type string | string[]
     */
    originActiveClass?: string | string[];
    /**
     * slide 弹出的容器，可传入id、HTMLElement 或 ElementRef<HTMLElement>
     * @description.en-us set the drawerContainer by id、HTMLElement or ElementRef<HTMLElement>. e.g. drawerContainer = '#offset-host'
     * @type string | HTMLElement | ElementRef<HTMLElement>
     */
    drawerContainer?: string | HTMLElement | ElementRef<HTMLElement>;
    /**
     * slide 进场的方向，可选  push | over | slide
     * @description.en-us set the render way. one of 'over' or 'push'. Default is 'over'
     * @default over
     */
    mode?: ThySlideMode;
    /**
     * 是否禁用关闭上一个已打开的 slideRef
     * @description.en-us set whether to disable close the latest item. Default is false
     * @default false
     */
    disableCloseLatest?: boolean;
}
export declare class ThySlideOption extends ThySlideConfig {
}
export declare const THY_SLIDE_DEFAULT_CONFIG: InjectionToken<ThySlideConfig<unknown>>;
export declare const slideAbstractOverlayOptions: ThyAbstractOverlayOptions;
export declare const slideDefaultConfigValue: {
    hasBackdrop: boolean;
    backdropClass: string;
    backdropClosable: boolean;
    closeOnNavigation: boolean;
    autoFocus: boolean;
    restoreFocus: boolean;
    from: string;
    panelClass: string;
    containerClass: string;
    role: string;
    offset: number;
    originActiveClass: string;
    mode: string;
    drawerContainer: string;
    disableCloseLatest: boolean;
};
export declare const THY_SLIDE_DEFAULT_CONFIG_PROVIDER: {
    provide: InjectionToken<ThySlideConfig<unknown>>;
    useValue: {};
};
