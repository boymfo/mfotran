import { ThyAbstractOverlayContainer, ThyClickPositioner, ThyPortalOutlet } from 'ngx-tethys/core';
import { Observable } from 'rxjs';
import { AnimationEvent } from '@angular/animations';
import { FocusTrapFactory } from '@angular/cdk/a11y';
import { ChangeDetectorRef, ElementRef, EventEmitter, NgZone, OnDestroy, Renderer2 } from '@angular/core';
import { ThyDialogConfig } from './dialog.config';
import * as i0 from "@angular/core";
/**
 * @private
 */
export declare class ThyDialogContainer extends ThyAbstractOverlayContainer implements OnDestroy {
    private elementRef;
    private document;
    config: ThyDialogConfig;
    private clickPositioner;
    private focusTrapFactory;
    private ngZone;
    private renderer;
    animationOpeningDone: Observable<AnimationEvent>;
    animationClosingDone: Observable<AnimationEvent>;
    portalOutlet: ThyPortalOutlet;
    id: string;
    /** State of the dialog animation. */
    animationState: 'void' | 'enter' | 'exit';
    /** Emits when an animation state changes. */
    animationStateChanged: EventEmitter<AnimationEvent>;
    /** ID of the element that should be considered as the dialog's label. */
    ariaLabelledBy: string | null;
    /** Element that was focused before the dialog was opened. Save this to restore upon close. */
    private elementFocusedBeforeDialogWasOpened;
    /** The class that traps and manages focus within the dialog. */
    private focusTrap;
    private savePreviouslyFocusedElement;
    /** Moves the focus inside the focus trap. */
    private trapFocus;
    private restoreFocus;
    private setTransformOrigin;
    constructor(elementRef: ElementRef, document: any, config: ThyDialogConfig, changeDetectorRef: ChangeDetectorRef, clickPositioner: ThyClickPositioner, focusTrapFactory: FocusTrapFactory, ngZone: NgZone, renderer: Renderer2);
    beforeAttachPortal(): void;
    /** Callback, invoked whenever an animation on the host completes. */
    onAnimationDone(event: AnimationEvent): void;
    /** Callback, invoked when an animation on the host starts. */
    onAnimationStart(event: AnimationEvent): void;
    ngOnDestroy(): void;
    static ɵfac: i0.ɵɵFactoryDeclaration<ThyDialogContainer, never>;
    static ɵcmp: i0.ɵɵComponentDeclaration<ThyDialogContainer, "thy-dialog-container", never, {}, {}, never, never, true, never>;
}
