import { ThyAbstractOverlayRef, ThyAbstractOverlayService, ThyClickPositioner } from 'ngx-tethys/core';
import { Overlay, OverlayConfig, OverlayRef } from '@angular/cdk/overlay';
import { ComponentType } from '@angular/cdk/portal';
import { Injector, OnDestroy, TemplateRef } from '@angular/core';
import { ThyConfirmConfig } from './confirm.config';
import { ThyConfirmAbstractComponent } from './confirm/token';
import { ThyDialogContainer } from './dialog-container.component';
import { ThyDialogRef } from './dialog-ref';
import { ThyDialogConfig } from './dialog.config';
import * as i0 from "@angular/core";
/**
 * @public
 * @order 10
 */
export declare class ThyDialog extends ThyAbstractOverlayService<ThyDialogConfig, ThyDialogContainer> implements OnDestroy {
    private confirmComponentType;
    protected buildOverlayConfig(config: ThyDialogConfig<any>): OverlayConfig;
    protected attachOverlayContainer(overlay: OverlayRef, config: ThyDialogConfig<any>): ThyDialogContainer;
    protected createAbstractOverlayRef<T, TResult>(overlayRef: OverlayRef, containerInstance: ThyDialogContainer, config: ThyDialogConfig<any>): ThyAbstractOverlayRef<T, ThyDialogContainer, TResult>;
    protected createInjector<T>(config: ThyDialogConfig, dialogRef: ThyDialogRef<T>, dialogContainer: ThyDialogContainer): Injector;
    constructor(overlay: Overlay, injector: Injector, defaultConfig: ThyDialogConfig, clickPositioner: ThyClickPositioner, confirmComponentType: ComponentType<ThyConfirmAbstractComponent>);
    /**
     * 打开 Dialog
     */
    open<T, TData = unknown, TResult = unknown>(componentOrTemplateRef: ComponentType<T> | TemplateRef<T>, config?: ThyDialogConfig<TData>): ThyDialogRef<T, TResult>;
    /**
     * 打开 Confirm
     */
    confirm(options: ThyConfirmConfig): ThyDialogRef<ThyConfirmAbstractComponent, unknown>;
    /**
     * 根据 id 获取 Dialog
     */
    getDialogById(id: string): ThyDialogRef<any> | undefined;
    /**
     * 获取所有打开的 Dialog
     */
    getOpenedDialogs(): ThyDialogRef<any>[];
    /**
     * @description.en-us Finds the closest ThyDialogRef to an element by looking at the DOM.
     * @description 获取与指定元素最接近的 ThyDialogRef
     */
    getClosestDialog(element: HTMLElement): ThyDialogRef<any> | undefined;
    ngOnDestroy(): void;
    static ɵfac: i0.ɵɵFactoryDeclaration<ThyDialog, [null, null, { optional: true; }, null, null]>;
    static ɵprov: i0.ɵɵInjectableDeclaration<ThyDialog>;
}
