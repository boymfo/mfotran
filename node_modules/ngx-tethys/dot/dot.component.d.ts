import { ThyThemeColor } from 'ngx-tethys/core';
import { ElementRef, Renderer2 } from '@angular/core';
import * as i0 from "@angular/core";
export type ThyColorType = ThyThemeColor | string;
export type ThySizeType = 'xs' | 'sm' | 'md' | 'lg' | 'xlg';
export type ThyThemeType = 'outline' | 'fill';
export type ThyShapeType = 'square' | 'circle';
export declare const COMPONENT_CLASS_NAME = "thy-dot";
export declare const DEFAULT_COLOR_NAME = "primary";
export declare const DEFAULT_SIZE_NAME = "sm";
export declare const DEFAULT_THEME_NAME = "fill";
export declare const DEFAULT_SHAPE_NAME = "circle";
/**
 * 显示一个点的组件
 * @name thy-dot,[thy-dot],[thyDot]
 * @order 10
 */
export declare class ThyDot {
    private el;
    private renderer;
    size: ThySizeType;
    theme: ThyThemeType;
    shape: ThyShapeType;
    color: ThyColorType;
    private nativeElement;
    constructor(el: ElementRef, renderer: Renderer2);
    /**
     * 颜色，可选值为：`primary` `success` `info` `warning` `danger` `default` `light`和自定义颜色，如`#2cccda` `red`  `rgb(153, 153, 153)`
     * @type ThyThemeColor | string
     * @default primary
     */
    set thyColor(value: ThyColorType);
    /**
     * 大小
     * @type xs | sm | md | lg | xlg
     * @default sm
     */
    set thySize(value: ThySizeType);
    /**
     * 主题
     * @type outline(线框) | fill(填充)
     * @default fill
     */
    set thyTheme(value: ThyThemeType);
    /**
     * 形状
     * @type circle(圆形) | square(方形)
     * @default circle
     */
    set thyShape(value: ThyShapeType);
    updateColorStyle(): void;
    static ɵfac: i0.ɵɵFactoryDeclaration<ThyDot, never>;
    static ɵcmp: i0.ɵɵComponentDeclaration<ThyDot, "thy-dot,[thy-dot],[thyDot]", never, { "thyColor": { "alias": "thyColor"; "required": false; }; "thySize": { "alias": "thySize"; "required": false; }; "thyTheme": { "alias": "thyTheme"; "required": false; }; "thyShape": { "alias": "thyShape"; "required": false; }; }, {}, never, never, true, never>;
}
