import { InjectionToken } from '@angular/core';
import { Subject } from 'rxjs';
export declare abstract class ThyGridToken {
    xGap: number;
    gridItemPropValueChange$: Subject<void>;
}
export declare const THY_GRID_COMPONENT: InjectionToken<ThyGridToken>;
