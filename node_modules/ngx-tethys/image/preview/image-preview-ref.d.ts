import { ThyImagePreview } from './image-preview.component';
import { ThyDialogRef } from 'ngx-tethys/dialog';
import { ThyImageInfo, ThyImagePreviewOptions } from '../image.class';
import { Observable } from 'rxjs';
export declare class ThyImagePreviewRef {
    previewInstance: ThyImagePreview;
    private config;
    private dialogRef;
    get componentInstance(): ThyImagePreview;
    constructor(previewInstance: ThyImagePreview, config: ThyImagePreviewOptions, dialogRef: ThyDialogRef<ThyImagePreview>);
    next(): void;
    prev(): void;
    downloadClicked(): Observable<ThyImageInfo>;
}
