import { ThyRgba } from './rgba.class';
export declare class ThyColor {
    hue: number;
    saturation: number;
    value: number;
    alpha: number;
    enableAlpha: boolean;
    format: string;
    displayValue: string;
    rgba: ThyRgba;
    constructor(color?: string | {
        h: number;
        s: number;
        v: number;
        alpha: number;
    });
    private stringToColor;
    setRgba(red?: number, green?: number, blue?: number, alpha?: number): this;
    rgbTohsv(r: number, g: number, b: number): {
        h: number;
        s: number;
        v: number;
    };
    getRgba(): ThyRgba;
    private hslaToRgba;
    private hueToRgb;
    private roundNumber;
    updateColor(h: number, s: number, v: number, alpha: number, updateRgba: boolean, enableAlpha?: boolean, format?: string): void;
    toHexString(alpha?: boolean): string;
    hsvaToRgba(h: number, s: number, v: number, alpha: number): ThyRgba;
    getFinalValue(): void;
}
