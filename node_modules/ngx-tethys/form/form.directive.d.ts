import { AfterViewInit, ElementRef, NgZone, OnDestroy, OnInit, QueryList, Renderer2 } from '@angular/core';
import { ControlContainer, NgControl } from '@angular/forms';
import { ThyFormValidatorService } from './form-validator.service';
import { ThyFormConfig, ThyFormLayout, ThyFormValidatorConfig } from './form.class';
import * as i0 from "@angular/core";
export declare enum ThyEnterKeyMode {
    submit = "submit",
    alwaysSubmit = "alwaysSubmit",
    forbidSubmit = "forbidSubmit"
}
/**
 * 表单
 * @name thyForm,[thy-form]
 * @order 10
 */
export declare class ThyFormDirective implements OnInit, AfterViewInit, OnDestroy {
    private ngForm;
    private elementRef;
    private renderer;
    private ngZone;
    validator: ThyFormValidatorService;
    private config;
    private layout;
    private initialized;
    private hostRenderer;
    /**
     * 布局，默认水平居中 horizontal，其他2种布局待开发
     * @type horizontal | vertical | inline
     * @default horizontal
     */
    set thyLayout(value: ThyFormLayout);
    get thyLayout(): ThyFormLayout;
    get isHorizontal(): boolean;
    /**
     * Enter 键提交模式。`submit`: Textarea 需要 Ctrl | Command + Enter 提交，其他元素直接 Enter 提交； `alwaysSubmit`: 不管是什么元素 Enter 都提交； `forbidSubmit`: Enter 不提交
     * @type submit | alwaysSubmit | forbidSubmit
     * @default submit
     */
    thyEnterKeyMode: ThyEnterKeyMode;
    /**
     * 表单验证规则配置项 （更多内容查看：thyFormValidatorConfig）
     */
    set thyFormValidatorConfig(config: ThyFormValidatorConfig);
    wasValidated: boolean;
    onSubmitSuccess: ($event: any) => void;
    private _unsubscribe;
    controls: QueryList<NgControl>;
    constructor(ngForm: ControlContainer, elementRef: ElementRef, renderer: Renderer2, ngZone: NgZone, validator: ThyFormValidatorService, config: ThyFormConfig);
    ngOnInit(): void;
    ngAfterViewInit(): void;
    submit($event: Event): void;
    updateClasses(): void;
    submitRunInZone($event: any): void;
    onKeydown($event: KeyboardEvent): void;
    ngOnDestroy(): void;
    static ɵfac: i0.ɵɵFactoryDeclaration<ThyFormDirective, never>;
    static ɵdir: i0.ɵɵDirectiveDeclaration<ThyFormDirective, "[thyForm],[thy-form]", ["thyForm"], { "thyLayout": { "alias": "thyLayout"; "required": false; }; "thyEnterKeyMode": { "alias": "thyEnterKeyMode"; "required": false; }; "thyFormValidatorConfig": { "alias": "thyFormValidatorConfig"; "required": false; }; }, {}, ["controls"], never, true, never>;
}
