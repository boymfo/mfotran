import { Overlay, OverlayRef, ScrollDispatcher } from '@angular/cdk/overlay';
import { ElementRef, NgZone, TemplateRef } from '@angular/core';
import { SafeAny } from 'ngx-tethys/types';
import { ThyTooltipContent } from './interface';
import { ThyTooltipConfig } from './tooltip.config';
export declare class ThyTooltipRef {
    private host;
    private config;
    private overlay;
    private scrollDispatcher;
    private ngZone;
    private overlayRef;
    private tooltipInstance;
    private scrollStrategy;
    private portal;
    private readonly dispose$;
    constructor(host: ElementRef<HTMLElement> | HTMLElement, config: ThyTooltipConfig, overlay: Overlay, scrollDispatcher: ScrollDispatcher, ngZone: NgZone);
    /** Create the overlay config and position strategy */
    private createOverlay;
    /** Updates the position of the current tooltip. */
    private updatePosition;
    private detach;
    show(content: ThyTooltipContent, delay?: number): void;
    show<T extends Record<SafeAny, SafeAny>>(content: ThyTooltipContent, data: T, delay?: number): void;
    hide(delay?: number): void;
    getOverlayRef(): OverlayRef;
    setTooltipClass(tooltipClass: string | string[]): void;
    updateTooltipContent(content: string | TemplateRef<any>, data?: any): void;
    isTooltipVisible(): boolean;
    dispose(): void;
}
