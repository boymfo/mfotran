import { SelectionModel } from '@angular/cdk/collections';
import { SelectOptionBase } from 'ngx-tethys/shared';
import { Subject } from 'rxjs';
import { ThyCascaderOption, ThyCascaderSearchOption } from './types';
import * as i0 from "@angular/core";
/**
 * @internal
 */
export declare class ThyCascaderService {
    selectionModel: SelectionModel<SelectOptionBase>;
    columns: ThyCascaderOption[][];
    customOptions: ThyCascaderOption[];
    cascaderOptions: {
        labelProperty?: string;
        valueProperty?: string;
        isMultiple?: boolean;
        isOnlySelectLeaf?: boolean;
        isLabelRenderTemplate?: boolean;
        loadData?: (node: ThyCascaderOption, index?: number) => PromiseLike<any>;
    };
    selectedOptions: ThyCascaderOption[];
    activatedOptions: ThyCascaderOption[];
    labelRenderText: string;
    flattenOptions: ThyCascaderSearchOption[];
    leafNodes: ThyCascaderSearchOption[];
    isLoading: boolean;
    searchResultList: ThyCascaderSearchOption[];
    defaultValue: any[];
    value: any[];
    private prevSelectedOptions;
    valueChange$: Subject<unknown>;
    cascaderValueChange(): import("rxjs").Observable<{
        value: any;
        isValueEqual: boolean;
        isSelectionModelEmpty: boolean;
    }>;
    setCascaderOptions(options: {
        labelProperty?: string;
        valueProperty?: string;
        isMultiple?: boolean;
        isOnlySelectLeaf?: boolean;
        isLabelRenderTemplate?: boolean;
        loadData?: (node: ThyCascaderOption, index?: number) => PromiseLike<any>;
    }): void;
    initSelectionModel(isMultiple?: boolean): void;
    initColumns(columns: ThyCascaderOption[][]): void;
    initActivatedOptions(menuVisible: boolean): void;
    initOptions(index: number): void;
    clickOption(option: ThyCascaderOption, index: number, event: Event | boolean, selectFn?: (option: ThyCascaderOption, index: number) => void): void;
    setActiveOption(option: ThyCascaderOption, index: number, select: boolean, loadChildren?: boolean, selectFn?: (option: ThyCascaderOption, index: number) => void): void;
    private loadChildren;
    private handleLoadDataSuccess;
    private handleLoadDataFailure;
    private activateOnInit;
    isSelectedOption(option: ThyCascaderOption, index: number): boolean;
    private toggleAllChildren;
    private getAllLeafs;
    searchInLocal(searchText: string): void;
    private forEachColumns;
    private setSearchResultList;
    /**
     * 检查所有所有子项的选择状态, 有一个不符合预期，就直接返回 false
     * @param option
     * @param trueOrFalse
     * @private
     */
    private checkSelectedStatus;
    private findOption;
    private setColumnData;
    private getSubmitValue;
    removeSelectedItem(item: SelectOptionBase): void;
    resetSearch(): void;
    clearSelection(): void;
    private deselectAllSelected;
    private deselectOption;
    selectOption(option: ThyCascaderOption, index: number): void;
    private addSelectedState;
    private addParentSelectedState;
    private buildDisplayLabel;
    writeCascaderValue(value: any): void;
    private afterWriteValue;
    isActivatedOption(option: ThyCascaderOption, index: number): boolean;
    isHalfSelectedOption(option: ThyCascaderOption, index: number): boolean;
    getValues(): any;
    private updatePrevSelectedOptions;
    private handleActivateInit;
    private isSelected;
    private clearPrevSelectedOptions;
    private getOptionLabel;
    private getOptionValue;
    removeCustomOption(): void;
    private getSelectedCustomOptions;
    toArray<T>(value: T | T[]): T[];
    arrayEquals<T>(array1: T[], array2: T[]): boolean;
    static ɵfac: i0.ɵɵFactoryDeclaration<ThyCascaderService, never>;
    static ɵprov: i0.ɵɵInjectableDeclaration<ThyCascaderService>;
}
