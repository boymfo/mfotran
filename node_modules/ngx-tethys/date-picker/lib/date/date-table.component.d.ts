import { TinyDate } from 'ngx-tethys/util';
import { EventEmitter, OnChanges } from '@angular/core';
import { DateHelperService } from '../../date-helper.service';
import { DateCell, DateBodyRow } from './types';
import { CalendarTable } from '../calendar/calendar-table.component';
import { ThyDatePickerConfigService } from '../../date-picker.service';
import * as i0 from "@angular/core";
/**
 * @private
 */
export declare class DateTable extends CalendarTable implements OnChanges {
    private dateHelper;
    private datePickerConfigService;
    readonly dayHover: EventEmitter<TinyDate>;
    constructor(dateHelper: DateHelperService, datePickerConfigService: ThyDatePickerConfigService);
    private chooseDate;
    makeHeadRow(): DateCell[];
    private getVeryShortWeekFormat;
    makeBodyRows(): DateBodyRow[];
    addCellProperty(cell: DateCell, date: TinyDate): void;
    getClassMap(cell: DateCell): {
        [key: string]: boolean;
    };
    static ɵfac: i0.ɵɵFactoryDeclaration<DateTable, never>;
    static ɵcmp: i0.ɵɵComponentDeclaration<DateTable, "date-table", ["dateTable"], {}, { "dayHover": "dayHover"; }, never, never, true, never>;
}
