import { ElementRef } from '@angular/core';
export declare function fallbackMatches(el: Element | Node, selector: string): boolean;
/**
 * Match `el` to `selector`.
 */
export declare function match(el: Element | Node, selector: string): any;
export declare function isDocument(element: any): element is Document;
export declare function isElement(element: any): boolean;
export declare function getWindow(elem: any): any;
export declare function getElementOffset(elem: HTMLElement): any;
export declare function getOffset(element: HTMLElement, container: HTMLElement | Window): {
    left: number;
    top: number;
} | null;
export declare function getClientSize(): {
    width: number;
    height: number;
};
export type ElementSelector = HTMLElement | ElementRef | string;
export declare function getHTMLElementBySelector(selector: ElementSelector, defaultElementRef: ElementRef): HTMLElement;
export declare function isInputOrTextarea(element: HTMLElement): boolean;
export declare function isWindow(container: Element | Window): container is Window;
export interface SimpleRect {
    top: number;
    left: number;
    width?: number;
    height?: number;
    bottom?: number;
}
export declare function getContainerRect(container: Element | Window): SimpleRect;
export declare function getStyleAsText(styles?: any): string;
export declare function isTouchEvent(event: MouseEvent | TouchEvent): event is TouchEvent;
/**
 * Assert wrapper element whether only contains icon.
 */
export declare function assertIconOnly(wrapperElement: Element): boolean;
/**
 *
 * calc position x,y point
 *
 * CASE (width <= clientWidth && height <= clientHeight):
 *
 * ------------- clientWidth -------------
 * |                                     |
 * |        ------ width ------          |
 * |        |                 |          |
 * |        |                 |          |
 * client   height            |          |
 * Height   |                 |          |
 * |        |                 |          |
 * |        -------------------          |
 * |                                     |
 * |                                     |
 * ---------------------------------------
 * fixedPosition = { x: 0, y: 0 }
 *
 *
 *
 * CASE (width > clientWidth || height > clientHeight):
 *
 * ------------- clientWidth -------------
 * |        |                            |
 * |        top                          |
 * |        |                            |
 * |--left--|--------------- width -----------------
 * |        |                                      |
 * client   |                                      |
 * Height   |                                      |
 * |        |                                      |
 * |        |                                      |
 * |        height                                 |
 * |        |                                      |
 * ---------|                                      |
 *          |                                      |
 *          |                                      |
 *          |                                      |
 *          ----------------------------------------
 *
 *
 * - left || top > 0
 *   left -> 0 || top -> 0
 *
 * - (left + width) < clientWidth || (top + height) < clientHeight
 * - left | top + width | height < clientWidth | clientHeight -> Back left | top + width | height === clientWidth | clientHeight
 *
 * DEFAULT:
 * - hold position
 *
 */
export declare function getFitContentPosition(params: {
    width: number;
    height: number;
    left: number;
    top: number;
    clientWidth: number;
    clientHeight: number;
}): {
    x?: number;
    y?: number;
};
export declare function elementMatchClosest(element: HTMLElement, selectors: string | string[]): boolean;
