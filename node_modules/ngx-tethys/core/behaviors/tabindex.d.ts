import { Constructor, MixinBase } from './constructor';
import { ThyCanDisable } from './disabled';
export interface ThyHasTabIndex {
    tabIndex: number;
    defaultTabIndex: number;
}
type HasTabIndexCtor = Constructor<ThyHasTabIndex>;
/** Mixin to augment a directive with a `tabIndex` property. */
export declare function mixinTabIndex<T extends Constructor<ThyCanDisable>>(base: T, defaultTabIndex?: number): HasTabIndexCtor & T;
export declare const TabIndexMixinBase: Constructor<ThyHasTabIndex> & Constructor<ThyCanDisable> & typeof MixinBase;
export {};
