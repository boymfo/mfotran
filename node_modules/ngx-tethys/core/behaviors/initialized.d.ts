import { OnInit } from '@angular/core';
import { Constructor } from './constructor';
export interface ThyInitialized extends OnInit {
    initialized: boolean;
}
export declare function mixinInitialized<T extends Constructor<{}>>(base: T): Constructor<ThyInitialized> & T;
